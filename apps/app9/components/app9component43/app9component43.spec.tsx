import { render } from '@testing-library/react';

import App9component43 from './app9component43';

describe('App9component43', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component43 />);
    expect(baseElement).toBeTruthy();
  });
});
