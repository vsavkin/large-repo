import { render } from '@testing-library/react';

import App9component92 from './app9component92';

describe('App9component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component92 />);
    expect(baseElement).toBeTruthy();
  });
});
