import { render } from '@testing-library/react';

import App9component86 from './app9component86';

describe('App9component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component86 />);
    expect(baseElement).toBeTruthy();
  });
});
