import { render } from '@testing-library/react';

import App9component96 from './app9component96';

describe('App9component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component96 />);
    expect(baseElement).toBeTruthy();
  });
});
