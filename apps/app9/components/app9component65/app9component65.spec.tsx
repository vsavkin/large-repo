import { render } from '@testing-library/react';

import App9component65 from './app9component65';

describe('App9component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component65 />);
    expect(baseElement).toBeTruthy();
  });
});
