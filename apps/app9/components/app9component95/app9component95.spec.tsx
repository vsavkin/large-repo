import { render } from '@testing-library/react';

import App9component95 from './app9component95';

describe('App9component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component95 />);
    expect(baseElement).toBeTruthy();
  });
});
