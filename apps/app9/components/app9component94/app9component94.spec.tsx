import { render } from '@testing-library/react';

import App9component94 from './app9component94';

describe('App9component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component94 />);
    expect(baseElement).toBeTruthy();
  });
});
