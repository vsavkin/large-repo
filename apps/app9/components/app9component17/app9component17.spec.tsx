import { render } from '@testing-library/react';

import App9component17 from './app9component17';

describe('App9component17', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component17 />);
    expect(baseElement).toBeTruthy();
  });
});
