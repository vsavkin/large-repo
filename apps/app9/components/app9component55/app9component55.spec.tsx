import { render } from '@testing-library/react';

import App9component55 from './app9component55';

describe('App9component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component55 />);
    expect(baseElement).toBeTruthy();
  });
});
