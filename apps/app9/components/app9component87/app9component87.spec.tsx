import { render } from '@testing-library/react';

import App9component87 from './app9component87';

describe('App9component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component87 />);
    expect(baseElement).toBeTruthy();
  });
});
