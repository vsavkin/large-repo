import { render } from '@testing-library/react';

import App9component37 from './app9component37';

describe('App9component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component37 />);
    expect(baseElement).toBeTruthy();
  });
});
