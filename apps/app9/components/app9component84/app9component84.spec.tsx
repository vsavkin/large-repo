import { render } from '@testing-library/react';

import App9component84 from './app9component84';

describe('App9component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component84 />);
    expect(baseElement).toBeTruthy();
  });
});
