import { render } from '@testing-library/react';

import App9component62 from './app9component62';

describe('App9component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component62 />);
    expect(baseElement).toBeTruthy();
  });
});
