import { render } from '@testing-library/react';

import App9component27 from './app9component27';

describe('App9component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component27 />);
    expect(baseElement).toBeTruthy();
  });
});
