import { render } from '@testing-library/react';

import App9component72 from './app9component72';

describe('App9component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component72 />);
    expect(baseElement).toBeTruthy();
  });
});
