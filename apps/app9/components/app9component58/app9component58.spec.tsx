import { render } from '@testing-library/react';

import App9component58 from './app9component58';

describe('App9component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component58 />);
    expect(baseElement).toBeTruthy();
  });
});
