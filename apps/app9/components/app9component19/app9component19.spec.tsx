import { render } from '@testing-library/react';

import App9component19 from './app9component19';

describe('App9component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component19 />);
    expect(baseElement).toBeTruthy();
  });
});
