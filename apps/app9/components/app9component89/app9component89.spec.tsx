import { render } from '@testing-library/react';

import App9component89 from './app9component89';

describe('App9component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component89 />);
    expect(baseElement).toBeTruthy();
  });
});
