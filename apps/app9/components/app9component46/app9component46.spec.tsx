import { render } from '@testing-library/react';

import App9component46 from './app9component46';

describe('App9component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component46 />);
    expect(baseElement).toBeTruthy();
  });
});
