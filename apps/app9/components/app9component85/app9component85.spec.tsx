import { render } from '@testing-library/react';

import App9component85 from './app9component85';

describe('App9component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component85 />);
    expect(baseElement).toBeTruthy();
  });
});
