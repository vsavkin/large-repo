import { render } from '@testing-library/react';

import App9component15 from './app9component15';

describe('App9component15', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component15 />);
    expect(baseElement).toBeTruthy();
  });
});
