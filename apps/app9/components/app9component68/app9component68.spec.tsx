import { render } from '@testing-library/react';

import App9component68 from './app9component68';

describe('App9component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component68 />);
    expect(baseElement).toBeTruthy();
  });
});
