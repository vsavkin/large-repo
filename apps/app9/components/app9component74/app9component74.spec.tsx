import { render } from '@testing-library/react';

import App9component74 from './app9component74';

describe('App9component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component74 />);
    expect(baseElement).toBeTruthy();
  });
});
