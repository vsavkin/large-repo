import { render } from '@testing-library/react';

import App9component63 from './app9component63';

describe('App9component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component63 />);
    expect(baseElement).toBeTruthy();
  });
});
