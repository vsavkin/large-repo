import { render } from '@testing-library/react';

import App9component61 from './app9component61';

describe('App9component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component61 />);
    expect(baseElement).toBeTruthy();
  });
});
