import { render } from '@testing-library/react';

import App9component49 from './app9component49';

describe('App9component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component49 />);
    expect(baseElement).toBeTruthy();
  });
});
