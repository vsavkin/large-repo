import { render } from '@testing-library/react';

import App9component88 from './app9component88';

describe('App9component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component88 />);
    expect(baseElement).toBeTruthy();
  });
});
