import { render } from '@testing-library/react';

import App9component33 from './app9component33';

describe('App9component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component33 />);
    expect(baseElement).toBeTruthy();
  });
});
