import { render } from '@testing-library/react';

import App9component44 from './app9component44';

describe('App9component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component44 />);
    expect(baseElement).toBeTruthy();
  });
});
