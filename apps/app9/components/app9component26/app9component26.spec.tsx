import { render } from '@testing-library/react';

import App9component26 from './app9component26';

describe('App9component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component26 />);
    expect(baseElement).toBeTruthy();
  });
});
