import { render } from '@testing-library/react';

import App9component67 from './app9component67';

describe('App9component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component67 />);
    expect(baseElement).toBeTruthy();
  });
});
