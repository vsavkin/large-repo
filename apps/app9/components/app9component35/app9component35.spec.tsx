import { render } from '@testing-library/react';

import App9component35 from './app9component35';

describe('App9component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component35 />);
    expect(baseElement).toBeTruthy();
  });
});
