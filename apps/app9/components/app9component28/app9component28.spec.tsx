import { render } from '@testing-library/react';

import App9component28 from './app9component28';

describe('App9component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component28 />);
    expect(baseElement).toBeTruthy();
  });
});
