import { render } from '@testing-library/react';

import App9component29 from './app9component29';

describe('App9component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component29 />);
    expect(baseElement).toBeTruthy();
  });
});
