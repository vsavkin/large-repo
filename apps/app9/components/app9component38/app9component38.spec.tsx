import { render } from '@testing-library/react';

import App9component38 from './app9component38';

describe('App9component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component38 />);
    expect(baseElement).toBeTruthy();
  });
});
