import { render } from '@testing-library/react';

import App9component14 from './app9component14';

describe('App9component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component14 />);
    expect(baseElement).toBeTruthy();
  });
});
