import { render } from '@testing-library/react';

import App9component93 from './app9component93';

describe('App9component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component93 />);
    expect(baseElement).toBeTruthy();
  });
});
