import { render } from '@testing-library/react';

import App9component76 from './app9component76';

describe('App9component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component76 />);
    expect(baseElement).toBeTruthy();
  });
});
