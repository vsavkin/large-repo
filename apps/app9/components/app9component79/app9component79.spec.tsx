import { render } from '@testing-library/react';

import App9component79 from './app9component79';

describe('App9component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component79 />);
    expect(baseElement).toBeTruthy();
  });
});
