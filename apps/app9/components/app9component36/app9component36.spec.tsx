import { render } from '@testing-library/react';

import App9component36 from './app9component36';

describe('App9component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component36 />);
    expect(baseElement).toBeTruthy();
  });
});
