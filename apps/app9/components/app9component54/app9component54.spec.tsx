import { render } from '@testing-library/react';

import App9component54 from './app9component54';

describe('App9component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component54 />);
    expect(baseElement).toBeTruthy();
  });
});
