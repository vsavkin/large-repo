import { render } from '@testing-library/react';

import App9component97 from './app9component97';

describe('App9component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component97 />);
    expect(baseElement).toBeTruthy();
  });
});
