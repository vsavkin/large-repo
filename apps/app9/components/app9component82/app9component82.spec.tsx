import { render } from '@testing-library/react';

import App9component82 from './app9component82';

describe('App9component82', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component82 />);
    expect(baseElement).toBeTruthy();
  });
});
