import { render } from '@testing-library/react';

import App9component98 from './app9component98';

describe('App9component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component98 />);
    expect(baseElement).toBeTruthy();
  });
});
