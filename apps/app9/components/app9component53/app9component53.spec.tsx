import { render } from '@testing-library/react';

import App9component53 from './app9component53';

describe('App9component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component53 />);
    expect(baseElement).toBeTruthy();
  });
});
