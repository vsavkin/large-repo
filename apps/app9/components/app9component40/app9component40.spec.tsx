import { render } from '@testing-library/react';

import App9component40 from './app9component40';

describe('App9component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component40 />);
    expect(baseElement).toBeTruthy();
  });
});
