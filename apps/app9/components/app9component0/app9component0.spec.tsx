import { render } from '@testing-library/react';

import App9component0 from './app9component0';

describe('App9component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component0 />);
    expect(baseElement).toBeTruthy();
  });
});
