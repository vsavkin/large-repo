import { render } from '@testing-library/react';

import App9component71 from './app9component71';

describe('App9component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component71 />);
    expect(baseElement).toBeTruthy();
  });
});
