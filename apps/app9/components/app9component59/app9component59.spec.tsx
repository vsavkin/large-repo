import { render } from '@testing-library/react';

import App9component59 from './app9component59';

describe('App9component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component59 />);
    expect(baseElement).toBeTruthy();
  });
});
