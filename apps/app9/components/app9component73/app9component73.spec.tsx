import { render } from '@testing-library/react';

import App9component73 from './app9component73';

describe('App9component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component73 />);
    expect(baseElement).toBeTruthy();
  });
});
