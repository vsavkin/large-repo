import { render } from '@testing-library/react';

import App9component47 from './app9component47';

describe('App9component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component47 />);
    expect(baseElement).toBeTruthy();
  });
});
