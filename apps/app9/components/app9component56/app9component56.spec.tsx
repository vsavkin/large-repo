import { render } from '@testing-library/react';

import App9component56 from './app9component56';

describe('App9component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component56 />);
    expect(baseElement).toBeTruthy();
  });
});
