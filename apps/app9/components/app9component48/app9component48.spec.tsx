import { render } from '@testing-library/react';

import App9component48 from './app9component48';

describe('App9component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component48 />);
    expect(baseElement).toBeTruthy();
  });
});
