import { render } from '@testing-library/react';

import App9component66 from './app9component66';

describe('App9component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component66 />);
    expect(baseElement).toBeTruthy();
  });
});
