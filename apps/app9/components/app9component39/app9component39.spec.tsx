import { render } from '@testing-library/react';

import App9component39 from './app9component39';

describe('App9component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component39 />);
    expect(baseElement).toBeTruthy();
  });
});
