import { render } from '@testing-library/react';

import App9component51 from './app9component51';

describe('App9component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component51 />);
    expect(baseElement).toBeTruthy();
  });
});
