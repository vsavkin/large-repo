import { render } from '@testing-library/react';

import App9component34 from './app9component34';

describe('App9component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component34 />);
    expect(baseElement).toBeTruthy();
  });
});
