import { render } from '@testing-library/react';

import App9component45 from './app9component45';

describe('App9component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component45 />);
    expect(baseElement).toBeTruthy();
  });
});
