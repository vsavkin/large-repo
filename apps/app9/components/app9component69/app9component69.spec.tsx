import { render } from '@testing-library/react';

import App9component69 from './app9component69';

describe('App9component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component69 />);
    expect(baseElement).toBeTruthy();
  });
});
