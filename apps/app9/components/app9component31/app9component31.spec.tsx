import { render } from '@testing-library/react';

import App9component31 from './app9component31';

describe('App9component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component31 />);
    expect(baseElement).toBeTruthy();
  });
});
