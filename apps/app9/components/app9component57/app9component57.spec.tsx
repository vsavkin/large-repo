import { render } from '@testing-library/react';

import App9component57 from './app9component57';

describe('App9component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component57 />);
    expect(baseElement).toBeTruthy();
  });
});
