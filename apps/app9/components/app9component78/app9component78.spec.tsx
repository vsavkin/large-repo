import { render } from '@testing-library/react';

import App9component78 from './app9component78';

describe('App9component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component78 />);
    expect(baseElement).toBeTruthy();
  });
});
