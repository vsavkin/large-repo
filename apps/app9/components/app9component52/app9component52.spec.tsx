import { render } from '@testing-library/react';

import App9component52 from './app9component52';

describe('App9component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component52 />);
    expect(baseElement).toBeTruthy();
  });
});
