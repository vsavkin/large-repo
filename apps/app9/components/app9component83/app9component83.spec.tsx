import { render } from '@testing-library/react';

import App9component83 from './app9component83';

describe('App9component83', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component83 />);
    expect(baseElement).toBeTruthy();
  });
});
