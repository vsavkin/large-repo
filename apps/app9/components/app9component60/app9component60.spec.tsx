import { render } from '@testing-library/react';

import App9component60 from './app9component60';

describe('App9component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App9component60 />);
    expect(baseElement).toBeTruthy();
  });
});
