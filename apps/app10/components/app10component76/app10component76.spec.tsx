import { render } from '@testing-library/react';

import App10component76 from './app10component76';

describe('App10component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component76 />);
    expect(baseElement).toBeTruthy();
  });
});
