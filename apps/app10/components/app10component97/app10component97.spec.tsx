import { render } from '@testing-library/react';

import App10component97 from './app10component97';

describe('App10component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component97 />);
    expect(baseElement).toBeTruthy();
  });
});
