import { render } from '@testing-library/react';

import App10component74 from './app10component74';

describe('App10component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component74 />);
    expect(baseElement).toBeTruthy();
  });
});
