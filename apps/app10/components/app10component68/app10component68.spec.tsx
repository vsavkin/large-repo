import { render } from '@testing-library/react';

import App10component68 from './app10component68';

describe('App10component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component68 />);
    expect(baseElement).toBeTruthy();
  });
});
