import { render } from '@testing-library/react';

import App10component73 from './app10component73';

describe('App10component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component73 />);
    expect(baseElement).toBeTruthy();
  });
});
