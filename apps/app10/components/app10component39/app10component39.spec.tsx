import { render } from '@testing-library/react';

import App10component39 from './app10component39';

describe('App10component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component39 />);
    expect(baseElement).toBeTruthy();
  });
});
