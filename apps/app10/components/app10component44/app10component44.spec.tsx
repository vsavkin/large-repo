import { render } from '@testing-library/react';

import App10component44 from './app10component44';

describe('App10component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component44 />);
    expect(baseElement).toBeTruthy();
  });
});
