import { render } from '@testing-library/react';

import App10component29 from './app10component29';

describe('App10component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component29 />);
    expect(baseElement).toBeTruthy();
  });
});
