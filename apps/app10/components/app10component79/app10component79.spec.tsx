import { render } from '@testing-library/react';

import App10component79 from './app10component79';

describe('App10component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component79 />);
    expect(baseElement).toBeTruthy();
  });
});
