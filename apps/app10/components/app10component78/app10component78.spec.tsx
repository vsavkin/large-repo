import { render } from '@testing-library/react';

import App10component78 from './app10component78';

describe('App10component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component78 />);
    expect(baseElement).toBeTruthy();
  });
});
