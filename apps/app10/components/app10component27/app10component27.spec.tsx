import { render } from '@testing-library/react';

import App10component27 from './app10component27';

describe('App10component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component27 />);
    expect(baseElement).toBeTruthy();
  });
});
