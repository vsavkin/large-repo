import { render } from '@testing-library/react';

import App10component93 from './app10component93';

describe('App10component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component93 />);
    expect(baseElement).toBeTruthy();
  });
});
