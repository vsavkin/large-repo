import { render } from '@testing-library/react';

import App10component58 from './app10component58';

describe('App10component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component58 />);
    expect(baseElement).toBeTruthy();
  });
});
