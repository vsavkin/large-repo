import { render } from '@testing-library/react';

import App10component28 from './app10component28';

describe('App10component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component28 />);
    expect(baseElement).toBeTruthy();
  });
});
