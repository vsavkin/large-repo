import { render } from '@testing-library/react';

import App10component86 from './app10component86';

describe('App10component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component86 />);
    expect(baseElement).toBeTruthy();
  });
});
