import { render } from '@testing-library/react';

import App10component35 from './app10component35';

describe('App10component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component35 />);
    expect(baseElement).toBeTruthy();
  });
});
