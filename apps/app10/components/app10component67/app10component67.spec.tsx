import { render } from '@testing-library/react';

import App10component67 from './app10component67';

describe('App10component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component67 />);
    expect(baseElement).toBeTruthy();
  });
});
