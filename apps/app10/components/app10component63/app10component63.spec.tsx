import { render } from '@testing-library/react';

import App10component63 from './app10component63';

describe('App10component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component63 />);
    expect(baseElement).toBeTruthy();
  });
});
