import { render } from '@testing-library/react';

import App10component46 from './app10component46';

describe('App10component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component46 />);
    expect(baseElement).toBeTruthy();
  });
});
