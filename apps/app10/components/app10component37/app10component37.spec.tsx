import { render } from '@testing-library/react';

import App10component37 from './app10component37';

describe('App10component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component37 />);
    expect(baseElement).toBeTruthy();
  });
});
