import { render } from '@testing-library/react';

import App10component88 from './app10component88';

describe('App10component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component88 />);
    expect(baseElement).toBeTruthy();
  });
});
