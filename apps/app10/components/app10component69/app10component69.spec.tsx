import { render } from '@testing-library/react';

import App10component69 from './app10component69';

describe('App10component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component69 />);
    expect(baseElement).toBeTruthy();
  });
});
