import { render } from '@testing-library/react';

import App10component94 from './app10component94';

describe('App10component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component94 />);
    expect(baseElement).toBeTruthy();
  });
});
