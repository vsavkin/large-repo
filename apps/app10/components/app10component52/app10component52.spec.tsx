import { render } from '@testing-library/react';

import App10component52 from './app10component52';

describe('App10component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component52 />);
    expect(baseElement).toBeTruthy();
  });
});
