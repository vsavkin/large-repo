import { render } from '@testing-library/react';

import App10component47 from './app10component47';

describe('App10component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component47 />);
    expect(baseElement).toBeTruthy();
  });
});
