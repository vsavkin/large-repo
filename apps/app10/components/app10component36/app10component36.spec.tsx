import { render } from '@testing-library/react';

import App10component36 from './app10component36';

describe('App10component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component36 />);
    expect(baseElement).toBeTruthy();
  });
});
