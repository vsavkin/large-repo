import { render } from '@testing-library/react';

import App10component53 from './app10component53';

describe('App10component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component53 />);
    expect(baseElement).toBeTruthy();
  });
});
