import { render } from '@testing-library/react';

import App10component38 from './app10component38';

describe('App10component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component38 />);
    expect(baseElement).toBeTruthy();
  });
});
