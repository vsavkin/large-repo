import { render } from '@testing-library/react';

import App10component87 from './app10component87';

describe('App10component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component87 />);
    expect(baseElement).toBeTruthy();
  });
});
