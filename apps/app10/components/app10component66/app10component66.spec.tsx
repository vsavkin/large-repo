import { render } from '@testing-library/react';

import App10component66 from './app10component66';

describe('App10component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App10component66 />);
    expect(baseElement).toBeTruthy();
  });
});
