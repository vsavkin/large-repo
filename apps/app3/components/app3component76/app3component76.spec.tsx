import { render } from '@testing-library/react';

import App3component76 from './app3component76';

describe('App3component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component76 />);
    expect(baseElement).toBeTruthy();
  });
});
