import { render } from '@testing-library/react';

import App3component73 from './app3component73';

describe('App3component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component73 />);
    expect(baseElement).toBeTruthy();
  });
});
