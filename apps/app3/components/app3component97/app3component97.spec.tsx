import { render } from '@testing-library/react';

import App3component97 from './app3component97';

describe('App3component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component97 />);
    expect(baseElement).toBeTruthy();
  });
});
