import { render } from '@testing-library/react';

import App3component46 from './app3component46';

describe('App3component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component46 />);
    expect(baseElement).toBeTruthy();
  });
});
