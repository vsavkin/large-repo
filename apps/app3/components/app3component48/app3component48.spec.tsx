import { render } from '@testing-library/react';

import App3component48 from './app3component48';

describe('App3component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component48 />);
    expect(baseElement).toBeTruthy();
  });
});
