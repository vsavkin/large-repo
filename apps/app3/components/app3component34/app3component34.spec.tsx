import { render } from '@testing-library/react';

import App3component34 from './app3component34';

describe('App3component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component34 />);
    expect(baseElement).toBeTruthy();
  });
});
