import { render } from '@testing-library/react';

import App3component37 from './app3component37';

describe('App3component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component37 />);
    expect(baseElement).toBeTruthy();
  });
});
