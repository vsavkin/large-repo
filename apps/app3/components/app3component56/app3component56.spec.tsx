import { render } from '@testing-library/react';

import App3component56 from './app3component56';

describe('App3component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component56 />);
    expect(baseElement).toBeTruthy();
  });
});
