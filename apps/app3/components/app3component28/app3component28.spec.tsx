import { render } from '@testing-library/react';

import App3component28 from './app3component28';

describe('App3component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component28 />);
    expect(baseElement).toBeTruthy();
  });
});
