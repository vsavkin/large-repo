import { render } from '@testing-library/react';

import App3component63 from './app3component63';

describe('App3component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component63 />);
    expect(baseElement).toBeTruthy();
  });
});
