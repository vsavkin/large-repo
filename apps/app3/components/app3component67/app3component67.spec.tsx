import { render } from '@testing-library/react';

import App3component67 from './app3component67';

describe('App3component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component67 />);
    expect(baseElement).toBeTruthy();
  });
});
