import { render } from '@testing-library/react';

import App3component35 from './app3component35';

describe('App3component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component35 />);
    expect(baseElement).toBeTruthy();
  });
});
