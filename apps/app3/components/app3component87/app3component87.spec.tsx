import { render } from '@testing-library/react';

import App3component87 from './app3component87';

describe('App3component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component87 />);
    expect(baseElement).toBeTruthy();
  });
});
