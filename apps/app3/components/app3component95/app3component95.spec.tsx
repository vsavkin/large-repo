import { render } from '@testing-library/react';

import App3component95 from './app3component95';

describe('App3component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component95 />);
    expect(baseElement).toBeTruthy();
  });
});
