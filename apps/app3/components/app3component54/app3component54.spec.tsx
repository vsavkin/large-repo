import { render } from '@testing-library/react';

import App3component54 from './app3component54';

describe('App3component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component54 />);
    expect(baseElement).toBeTruthy();
  });
});
