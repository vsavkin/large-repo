import { render } from '@testing-library/react';

import App3component72 from './app3component72';

describe('App3component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component72 />);
    expect(baseElement).toBeTruthy();
  });
});
