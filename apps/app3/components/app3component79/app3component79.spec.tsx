import { render } from '@testing-library/react';

import App3component79 from './app3component79';

describe('App3component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component79 />);
    expect(baseElement).toBeTruthy();
  });
});
