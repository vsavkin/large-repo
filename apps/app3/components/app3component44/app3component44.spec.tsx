import { render } from '@testing-library/react';

import App3component44 from './app3component44';

describe('App3component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component44 />);
    expect(baseElement).toBeTruthy();
  });
});
