import { render } from '@testing-library/react';

import App3component39 from './app3component39';

describe('App3component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component39 />);
    expect(baseElement).toBeTruthy();
  });
});
