import { render } from '@testing-library/react';

import App3component74 from './app3component74';

describe('App3component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component74 />);
    expect(baseElement).toBeTruthy();
  });
});
