import { render } from '@testing-library/react';

import App3component29 from './app3component29';

describe('App3component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component29 />);
    expect(baseElement).toBeTruthy();
  });
});
