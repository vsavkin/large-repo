import { render } from '@testing-library/react';

import App3component66 from './app3component66';

describe('App3component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component66 />);
    expect(baseElement).toBeTruthy();
  });
});
