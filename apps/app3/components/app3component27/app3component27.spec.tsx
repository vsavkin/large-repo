import { render } from '@testing-library/react';

import App3component27 from './app3component27';

describe('App3component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component27 />);
    expect(baseElement).toBeTruthy();
  });
});
