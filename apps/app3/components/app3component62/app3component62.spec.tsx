import { render } from '@testing-library/react';

import App3component62 from './app3component62';

describe('App3component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component62 />);
    expect(baseElement).toBeTruthy();
  });
});
