import { render } from '@testing-library/react';

import App3component38 from './app3component38';

describe('App3component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component38 />);
    expect(baseElement).toBeTruthy();
  });
});
