import { render } from '@testing-library/react';

import App3component49 from './app3component49';

describe('App3component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component49 />);
    expect(baseElement).toBeTruthy();
  });
});
