import { render } from '@testing-library/react';

import App3component88 from './app3component88';

describe('App3component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component88 />);
    expect(baseElement).toBeTruthy();
  });
});
