import { render } from '@testing-library/react';

import App3component58 from './app3component58';

describe('App3component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component58 />);
    expect(baseElement).toBeTruthy();
  });
});
