import { render } from '@testing-library/react';

import App3component52 from './app3component52';

describe('App3component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component52 />);
    expect(baseElement).toBeTruthy();
  });
});
