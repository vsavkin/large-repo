import { render } from '@testing-library/react';

import App3component93 from './app3component93';

describe('App3component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component93 />);
    expect(baseElement).toBeTruthy();
  });
});
