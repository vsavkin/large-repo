import { render } from '@testing-library/react';

import App3component57 from './app3component57';

describe('App3component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component57 />);
    expect(baseElement).toBeTruthy();
  });
});
