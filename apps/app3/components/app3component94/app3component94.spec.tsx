import { render } from '@testing-library/react';

import App3component94 from './app3component94';

describe('App3component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component94 />);
    expect(baseElement).toBeTruthy();
  });
});
