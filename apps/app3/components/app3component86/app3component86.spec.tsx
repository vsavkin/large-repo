import { render } from '@testing-library/react';

import App3component86 from './app3component86';

describe('App3component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component86 />);
    expect(baseElement).toBeTruthy();
  });
});
