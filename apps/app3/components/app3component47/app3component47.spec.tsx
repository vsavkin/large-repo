import { render } from '@testing-library/react';

import App3component47 from './app3component47';

describe('App3component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component47 />);
    expect(baseElement).toBeTruthy();
  });
});
