import { render } from '@testing-library/react';

import App3component53 from './app3component53';

describe('App3component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component53 />);
    expect(baseElement).toBeTruthy();
  });
});
