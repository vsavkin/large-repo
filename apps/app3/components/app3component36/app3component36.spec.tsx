import { render } from '@testing-library/react';

import App3component36 from './app3component36';

describe('App3component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component36 />);
    expect(baseElement).toBeTruthy();
  });
});
