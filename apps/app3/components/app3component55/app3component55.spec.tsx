import { render } from '@testing-library/react';

import App3component55 from './app3component55';

describe('App3component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component55 />);
    expect(baseElement).toBeTruthy();
  });
});
