import { render } from '@testing-library/react';

import App3component59 from './app3component59';

describe('App3component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App3component59 />);
    expect(baseElement).toBeTruthy();
  });
});
