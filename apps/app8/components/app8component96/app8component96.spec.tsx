import { render } from '@testing-library/react';

import App8component96 from './app8component96';

describe('App8component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component96 />);
    expect(baseElement).toBeTruthy();
  });
});
