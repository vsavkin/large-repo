import { render } from '@testing-library/react';

import App8component58 from './app8component58';

describe('App8component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component58 />);
    expect(baseElement).toBeTruthy();
  });
});
