import { render } from '@testing-library/react';

import App8component71 from './app8component71';

describe('App8component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component71 />);
    expect(baseElement).toBeTruthy();
  });
});
