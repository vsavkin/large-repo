import { render } from '@testing-library/react';

import App8component55 from './app8component55';

describe('App8component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component55 />);
    expect(baseElement).toBeTruthy();
  });
});
