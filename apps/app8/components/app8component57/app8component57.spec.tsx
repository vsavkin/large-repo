import { render } from '@testing-library/react';

import App8component57 from './app8component57';

describe('App8component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component57 />);
    expect(baseElement).toBeTruthy();
  });
});
