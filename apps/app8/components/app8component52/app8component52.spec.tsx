import { render } from '@testing-library/react';

import App8component52 from './app8component52';

describe('App8component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component52 />);
    expect(baseElement).toBeTruthy();
  });
});
