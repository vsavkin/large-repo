import { render } from '@testing-library/react';

import App8component56 from './app8component56';

describe('App8component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component56 />);
    expect(baseElement).toBeTruthy();
  });
});
