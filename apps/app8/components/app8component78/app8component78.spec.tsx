import { render } from '@testing-library/react';

import App8component78 from './app8component78';

describe('App8component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component78 />);
    expect(baseElement).toBeTruthy();
  });
});
