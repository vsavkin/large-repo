import { render } from '@testing-library/react';

import App8component38 from './app8component38';

describe('App8component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component38 />);
    expect(baseElement).toBeTruthy();
  });
});
