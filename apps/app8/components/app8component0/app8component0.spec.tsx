import { render } from '@testing-library/react';

import App8component0 from './app8component0';

describe('App8component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component0 />);
    expect(baseElement).toBeTruthy();
  });
});
