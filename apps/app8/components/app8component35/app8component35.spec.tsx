import { render } from '@testing-library/react';

import App8component35 from './app8component35';

describe('App8component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component35 />);
    expect(baseElement).toBeTruthy();
  });
});
