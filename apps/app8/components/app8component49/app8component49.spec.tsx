import { render } from '@testing-library/react';

import App8component49 from './app8component49';

describe('App8component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component49 />);
    expect(baseElement).toBeTruthy();
  });
});
