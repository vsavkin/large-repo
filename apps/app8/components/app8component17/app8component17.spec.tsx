import { render } from '@testing-library/react';

import App8component17 from './app8component17';

describe('App8component17', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component17 />);
    expect(baseElement).toBeTruthy();
  });
});
