import { render } from '@testing-library/react';

import App8component47 from './app8component47';

describe('App8component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component47 />);
    expect(baseElement).toBeTruthy();
  });
});
