import { render } from '@testing-library/react';

import App8component39 from './app8component39';

describe('App8component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component39 />);
    expect(baseElement).toBeTruthy();
  });
});
