import { render } from '@testing-library/react';

import App8component53 from './app8component53';

describe('App8component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component53 />);
    expect(baseElement).toBeTruthy();
  });
});
