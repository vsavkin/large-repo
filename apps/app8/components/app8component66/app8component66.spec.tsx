import { render } from '@testing-library/react';

import App8component66 from './app8component66';

describe('App8component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component66 />);
    expect(baseElement).toBeTruthy();
  });
});
