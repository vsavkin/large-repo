import { render } from '@testing-library/react';

import App8component67 from './app8component67';

describe('App8component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component67 />);
    expect(baseElement).toBeTruthy();
  });
});
