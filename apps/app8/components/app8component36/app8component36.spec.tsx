import { render } from '@testing-library/react';

import App8component36 from './app8component36';

describe('App8component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component36 />);
    expect(baseElement).toBeTruthy();
  });
});
