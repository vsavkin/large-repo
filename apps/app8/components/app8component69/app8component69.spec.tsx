import { render } from '@testing-library/react';

import App8component69 from './app8component69';

describe('App8component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component69 />);
    expect(baseElement).toBeTruthy();
  });
});
