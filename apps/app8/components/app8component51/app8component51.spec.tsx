import { render } from '@testing-library/react';

import App8component51 from './app8component51';

describe('App8component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component51 />);
    expect(baseElement).toBeTruthy();
  });
});
