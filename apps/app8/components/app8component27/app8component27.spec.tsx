import { render } from '@testing-library/react';

import App8component27 from './app8component27';

describe('App8component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component27 />);
    expect(baseElement).toBeTruthy();
  });
});
