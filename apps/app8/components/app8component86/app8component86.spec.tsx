import { render } from '@testing-library/react';

import App8component86 from './app8component86';

describe('App8component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component86 />);
    expect(baseElement).toBeTruthy();
  });
});
