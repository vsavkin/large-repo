import { render } from '@testing-library/react';

import App8component72 from './app8component72';

describe('App8component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component72 />);
    expect(baseElement).toBeTruthy();
  });
});
