import { render } from '@testing-library/react';

import App8component29 from './app8component29';

describe('App8component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component29 />);
    expect(baseElement).toBeTruthy();
  });
});
