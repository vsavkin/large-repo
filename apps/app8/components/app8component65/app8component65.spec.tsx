import { render } from '@testing-library/react';

import App8component65 from './app8component65';

describe('App8component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component65 />);
    expect(baseElement).toBeTruthy();
  });
});
