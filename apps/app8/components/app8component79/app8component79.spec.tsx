import { render } from '@testing-library/react';

import App8component79 from './app8component79';

describe('App8component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component79 />);
    expect(baseElement).toBeTruthy();
  });
});
