import { render } from '@testing-library/react';

import App8component93 from './app8component93';

describe('App8component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component93 />);
    expect(baseElement).toBeTruthy();
  });
});
