import { render } from '@testing-library/react';

import App8component68 from './app8component68';

describe('App8component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component68 />);
    expect(baseElement).toBeTruthy();
  });
});
