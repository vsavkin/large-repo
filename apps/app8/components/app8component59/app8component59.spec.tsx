import { render } from '@testing-library/react';

import App8component59 from './app8component59';

describe('App8component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component59 />);
    expect(baseElement).toBeTruthy();
  });
});
