import { render } from '@testing-library/react';

import App8component60 from './app8component60';

describe('App8component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component60 />);
    expect(baseElement).toBeTruthy();
  });
});
