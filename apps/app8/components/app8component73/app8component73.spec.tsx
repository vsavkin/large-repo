import { render } from '@testing-library/react';

import App8component73 from './app8component73';

describe('App8component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component73 />);
    expect(baseElement).toBeTruthy();
  });
});
