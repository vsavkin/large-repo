import { render } from '@testing-library/react';

import App8component90 from './app8component90';

describe('App8component90', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component90 />);
    expect(baseElement).toBeTruthy();
  });
});
