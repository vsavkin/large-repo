import { render } from '@testing-library/react';

import App8component37 from './app8component37';

describe('App8component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component37 />);
    expect(baseElement).toBeTruthy();
  });
});
