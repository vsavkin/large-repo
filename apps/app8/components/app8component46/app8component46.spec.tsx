import { render } from '@testing-library/react';

import App8component46 from './app8component46';

describe('App8component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component46 />);
    expect(baseElement).toBeTruthy();
  });
});
