import { render } from '@testing-library/react';

import App8component26 from './app8component26';

describe('App8component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component26 />);
    expect(baseElement).toBeTruthy();
  });
});
