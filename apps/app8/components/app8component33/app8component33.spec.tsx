import { render } from '@testing-library/react';

import App8component33 from './app8component33';

describe('App8component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component33 />);
    expect(baseElement).toBeTruthy();
  });
});
