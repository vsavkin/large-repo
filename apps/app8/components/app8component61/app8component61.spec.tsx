import { render } from '@testing-library/react';

import App8component61 from './app8component61';

describe('App8component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component61 />);
    expect(baseElement).toBeTruthy();
  });
});
