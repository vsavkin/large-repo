import { render } from '@testing-library/react';

import App8component34 from './app8component34';

describe('App8component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component34 />);
    expect(baseElement).toBeTruthy();
  });
});
