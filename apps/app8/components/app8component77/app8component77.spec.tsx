import { render } from '@testing-library/react';

import App8component77 from './app8component77';

describe('App8component77', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component77 />);
    expect(baseElement).toBeTruthy();
  });
});
