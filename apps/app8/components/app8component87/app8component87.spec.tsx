import { render } from '@testing-library/react';

import App8component87 from './app8component87';

describe('App8component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component87 />);
    expect(baseElement).toBeTruthy();
  });
});
