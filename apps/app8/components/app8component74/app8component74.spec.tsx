import { render } from '@testing-library/react';

import App8component74 from './app8component74';

describe('App8component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component74 />);
    expect(baseElement).toBeTruthy();
  });
});
