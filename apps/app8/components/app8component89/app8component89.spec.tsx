import { render } from '@testing-library/react';

import App8component89 from './app8component89';

describe('App8component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component89 />);
    expect(baseElement).toBeTruthy();
  });
});
