import { render } from '@testing-library/react';

import App8component63 from './app8component63';

describe('App8component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component63 />);
    expect(baseElement).toBeTruthy();
  });
});
