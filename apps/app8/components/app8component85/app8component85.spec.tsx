import { render } from '@testing-library/react';

import App8component85 from './app8component85';

describe('App8component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component85 />);
    expect(baseElement).toBeTruthy();
  });
});
