import { render } from '@testing-library/react';

import App8component94 from './app8component94';

describe('App8component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component94 />);
    expect(baseElement).toBeTruthy();
  });
});
