import { render } from '@testing-library/react';

import App8component92 from './app8component92';

describe('App8component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component92 />);
    expect(baseElement).toBeTruthy();
  });
});
