import { render } from '@testing-library/react';

import App8component30 from './app8component30';

describe('App8component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component30 />);
    expect(baseElement).toBeTruthy();
  });
});
