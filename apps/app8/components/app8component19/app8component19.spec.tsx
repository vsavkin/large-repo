import { render } from '@testing-library/react';

import App8component19 from './app8component19';

describe('App8component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component19 />);
    expect(baseElement).toBeTruthy();
  });
});
