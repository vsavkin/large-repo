import { render } from '@testing-library/react';

import App8component76 from './app8component76';

describe('App8component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component76 />);
    expect(baseElement).toBeTruthy();
  });
});
