import { render } from '@testing-library/react';

import App8component40 from './app8component40';

describe('App8component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component40 />);
    expect(baseElement).toBeTruthy();
  });
});
