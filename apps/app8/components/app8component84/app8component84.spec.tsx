import { render } from '@testing-library/react';

import App8component84 from './app8component84';

describe('App8component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component84 />);
    expect(baseElement).toBeTruthy();
  });
});
