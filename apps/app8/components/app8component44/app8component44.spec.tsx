import { render } from '@testing-library/react';

import App8component44 from './app8component44';

describe('App8component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component44 />);
    expect(baseElement).toBeTruthy();
  });
});
