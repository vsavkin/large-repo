import { render } from '@testing-library/react';

import App8component97 from './app8component97';

describe('App8component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component97 />);
    expect(baseElement).toBeTruthy();
  });
});
