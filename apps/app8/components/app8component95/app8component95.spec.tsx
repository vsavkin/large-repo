import { render } from '@testing-library/react';

import App8component95 from './app8component95';

describe('App8component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component95 />);
    expect(baseElement).toBeTruthy();
  });
});
