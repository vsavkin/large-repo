import { render } from '@testing-library/react';

import App8component45 from './app8component45';

describe('App8component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component45 />);
    expect(baseElement).toBeTruthy();
  });
});
