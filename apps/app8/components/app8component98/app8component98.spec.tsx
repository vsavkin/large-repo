import { render } from '@testing-library/react';

import App8component98 from './app8component98';

describe('App8component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component98 />);
    expect(baseElement).toBeTruthy();
  });
});
