import { render } from '@testing-library/react';

import App8component54 from './app8component54';

describe('App8component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component54 />);
    expect(baseElement).toBeTruthy();
  });
});
