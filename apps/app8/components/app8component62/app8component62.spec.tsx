import { render } from '@testing-library/react';

import App8component62 from './app8component62';

describe('App8component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component62 />);
    expect(baseElement).toBeTruthy();
  });
});
