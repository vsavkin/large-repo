import { render } from '@testing-library/react';

import App8component91 from './app8component91';

describe('App8component91', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component91 />);
    expect(baseElement).toBeTruthy();
  });
});
