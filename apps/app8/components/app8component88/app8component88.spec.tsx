import { render } from '@testing-library/react';

import App8component88 from './app8component88';

describe('App8component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component88 />);
    expect(baseElement).toBeTruthy();
  });
});
