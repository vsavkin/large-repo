import { render } from '@testing-library/react';

import App8component28 from './app8component28';

describe('App8component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component28 />);
    expect(baseElement).toBeTruthy();
  });
});
