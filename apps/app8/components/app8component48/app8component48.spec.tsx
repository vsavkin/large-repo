import { render } from '@testing-library/react';

import App8component48 from './app8component48';

describe('App8component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component48 />);
    expect(baseElement).toBeTruthy();
  });
});
