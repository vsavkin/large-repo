import { render } from '@testing-library/react';

import App8component31 from './app8component31';

describe('App8component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App8component31 />);
    expect(baseElement).toBeTruthy();
  });
});
