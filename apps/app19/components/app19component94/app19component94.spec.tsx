import { render } from '@testing-library/react';

import App19component94 from './app19component94';

describe('App19component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component94 />);
    expect(baseElement).toBeTruthy();
  });
});
