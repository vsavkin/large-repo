import { render } from '@testing-library/react';

import App19component0 from './app19component0';

describe('App19component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component0 />);
    expect(baseElement).toBeTruthy();
  });
});
