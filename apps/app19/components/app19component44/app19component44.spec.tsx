import { render } from '@testing-library/react';

import App19component44 from './app19component44';

describe('App19component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component44 />);
    expect(baseElement).toBeTruthy();
  });
});
