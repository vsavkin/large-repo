import { render } from '@testing-library/react';

import App19component51 from './app19component51';

describe('App19component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component51 />);
    expect(baseElement).toBeTruthy();
  });
});
