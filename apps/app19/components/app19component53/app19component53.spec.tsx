import { render } from '@testing-library/react';

import App19component53 from './app19component53';

describe('App19component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component53 />);
    expect(baseElement).toBeTruthy();
  });
});
