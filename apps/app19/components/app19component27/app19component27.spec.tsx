import { render } from '@testing-library/react';

import App19component27 from './app19component27';

describe('App19component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component27 />);
    expect(baseElement).toBeTruthy();
  });
});
