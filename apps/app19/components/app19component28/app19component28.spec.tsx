import { render } from '@testing-library/react';

import App19component28 from './app19component28';

describe('App19component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component28 />);
    expect(baseElement).toBeTruthy();
  });
});
