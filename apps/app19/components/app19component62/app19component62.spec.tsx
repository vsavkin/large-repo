import { render } from '@testing-library/react';

import App19component62 from './app19component62';

describe('App19component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component62 />);
    expect(baseElement).toBeTruthy();
  });
});
