import { render } from '@testing-library/react';

import App19component77 from './app19component77';

describe('App19component77', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component77 />);
    expect(baseElement).toBeTruthy();
  });
});
