import { render } from '@testing-library/react';

import App19component20 from './app19component20';

describe('App19component20', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component20 />);
    expect(baseElement).toBeTruthy();
  });
});
