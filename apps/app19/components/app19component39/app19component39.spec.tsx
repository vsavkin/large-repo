import { render } from '@testing-library/react';

import App19component39 from './app19component39';

describe('App19component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component39 />);
    expect(baseElement).toBeTruthy();
  });
});
