import { render } from '@testing-library/react';

import App19component38 from './app19component38';

describe('App19component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component38 />);
    expect(baseElement).toBeTruthy();
  });
});
