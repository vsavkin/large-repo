import { render } from '@testing-library/react';

import App19component40 from './app19component40';

describe('App19component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component40 />);
    expect(baseElement).toBeTruthy();
  });
});
