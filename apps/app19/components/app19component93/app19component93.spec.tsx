import { render } from '@testing-library/react';

import App19component93 from './app19component93';

describe('App19component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component93 />);
    expect(baseElement).toBeTruthy();
  });
});
