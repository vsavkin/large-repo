import { render } from '@testing-library/react';

import App19component69 from './app19component69';

describe('App19component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component69 />);
    expect(baseElement).toBeTruthy();
  });
});
