import { render } from '@testing-library/react';

import App19component33 from './app19component33';

describe('App19component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component33 />);
    expect(baseElement).toBeTruthy();
  });
});
