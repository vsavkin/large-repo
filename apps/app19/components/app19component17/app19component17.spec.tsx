import { render } from '@testing-library/react';

import App19component17 from './app19component17';

describe('App19component17', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component17 />);
    expect(baseElement).toBeTruthy();
  });
});
