import { render } from '@testing-library/react';

import App19component31 from './app19component31';

describe('App19component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component31 />);
    expect(baseElement).toBeTruthy();
  });
});
