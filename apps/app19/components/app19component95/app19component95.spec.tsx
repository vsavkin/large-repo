import { render } from '@testing-library/react';

import App19component95 from './app19component95';

describe('App19component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component95 />);
    expect(baseElement).toBeTruthy();
  });
});
