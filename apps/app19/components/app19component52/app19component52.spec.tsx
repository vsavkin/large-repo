import { render } from '@testing-library/react';

import App19component52 from './app19component52';

describe('App19component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component52 />);
    expect(baseElement).toBeTruthy();
  });
});
