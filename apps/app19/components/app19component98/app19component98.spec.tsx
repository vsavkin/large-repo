import { render } from '@testing-library/react';

import App19component98 from './app19component98';

describe('App19component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component98 />);
    expect(baseElement).toBeTruthy();
  });
});
