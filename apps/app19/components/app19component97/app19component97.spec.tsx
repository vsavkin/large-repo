import { render } from '@testing-library/react';

import App19component97 from './app19component97';

describe('App19component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component97 />);
    expect(baseElement).toBeTruthy();
  });
});
