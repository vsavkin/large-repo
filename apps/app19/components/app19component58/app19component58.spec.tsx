import { render } from '@testing-library/react';

import App19component58 from './app19component58';

describe('App19component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component58 />);
    expect(baseElement).toBeTruthy();
  });
});
