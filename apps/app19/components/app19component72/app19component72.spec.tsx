import { render } from '@testing-library/react';

import App19component72 from './app19component72';

describe('App19component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component72 />);
    expect(baseElement).toBeTruthy();
  });
});
