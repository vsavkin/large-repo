import { render } from '@testing-library/react';

import App19component29 from './app19component29';

describe('App19component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component29 />);
    expect(baseElement).toBeTruthy();
  });
});
