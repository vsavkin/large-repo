import { render } from '@testing-library/react';

import App19component66 from './app19component66';

describe('App19component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component66 />);
    expect(baseElement).toBeTruthy();
  });
});
