import { render } from '@testing-library/react';

import App19component43 from './app19component43';

describe('App19component43', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component43 />);
    expect(baseElement).toBeTruthy();
  });
});
