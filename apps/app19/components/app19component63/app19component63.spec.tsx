import { render } from '@testing-library/react';

import App19component63 from './app19component63';

describe('App19component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component63 />);
    expect(baseElement).toBeTruthy();
  });
});
