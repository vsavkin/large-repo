import { render } from '@testing-library/react';

import App19component18 from './app19component18';

describe('App19component18', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component18 />);
    expect(baseElement).toBeTruthy();
  });
});
