import { render } from '@testing-library/react';

import App19component46 from './app19component46';

describe('App19component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component46 />);
    expect(baseElement).toBeTruthy();
  });
});
