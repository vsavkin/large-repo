import { render } from '@testing-library/react';

import App19component84 from './app19component84';

describe('App19component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component84 />);
    expect(baseElement).toBeTruthy();
  });
});
