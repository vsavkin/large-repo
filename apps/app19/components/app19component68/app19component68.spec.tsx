import { render } from '@testing-library/react';

import App19component68 from './app19component68';

describe('App19component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component68 />);
    expect(baseElement).toBeTruthy();
  });
});
