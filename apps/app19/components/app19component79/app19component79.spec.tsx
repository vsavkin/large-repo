import { render } from '@testing-library/react';

import App19component79 from './app19component79';

describe('App19component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component79 />);
    expect(baseElement).toBeTruthy();
  });
});
