import { render } from '@testing-library/react';

import App19component67 from './app19component67';

describe('App19component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component67 />);
    expect(baseElement).toBeTruthy();
  });
});
