import { render } from '@testing-library/react';

import App19component21 from './app19component21';

describe('App19component21', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component21 />);
    expect(baseElement).toBeTruthy();
  });
});
