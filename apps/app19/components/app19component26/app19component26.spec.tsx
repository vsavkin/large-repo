import { render } from '@testing-library/react';

import App19component26 from './app19component26';

describe('App19component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component26 />);
    expect(baseElement).toBeTruthy();
  });
});
