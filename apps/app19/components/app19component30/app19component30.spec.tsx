import { render } from '@testing-library/react';

import App19component30 from './app19component30';

describe('App19component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component30 />);
    expect(baseElement).toBeTruthy();
  });
});
