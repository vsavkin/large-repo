import { render } from '@testing-library/react';

import App19component92 from './app19component92';

describe('App19component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component92 />);
    expect(baseElement).toBeTruthy();
  });
});
