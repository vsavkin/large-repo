import { render } from '@testing-library/react';

import App19component70 from './app19component70';

describe('App19component70', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component70 />);
    expect(baseElement).toBeTruthy();
  });
});
