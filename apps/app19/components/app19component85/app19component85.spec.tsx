import { render } from '@testing-library/react';

import App19component85 from './app19component85';

describe('App19component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component85 />);
    expect(baseElement).toBeTruthy();
  });
});
