import { render } from '@testing-library/react';

import App19component34 from './app19component34';

describe('App19component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component34 />);
    expect(baseElement).toBeTruthy();
  });
});
