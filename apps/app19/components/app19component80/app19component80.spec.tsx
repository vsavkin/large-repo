import { render } from '@testing-library/react';

import App19component80 from './app19component80';

describe('App19component80', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component80 />);
    expect(baseElement).toBeTruthy();
  });
});
