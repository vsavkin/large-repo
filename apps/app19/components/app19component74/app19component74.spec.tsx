import { render } from '@testing-library/react';

import App19component74 from './app19component74';

describe('App19component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component74 />);
    expect(baseElement).toBeTruthy();
  });
});
