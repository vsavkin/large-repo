import { render } from '@testing-library/react';

import App19component86 from './app19component86';

describe('App19component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component86 />);
    expect(baseElement).toBeTruthy();
  });
});
