import { render } from '@testing-library/react';

import App19component22 from './app19component22';

describe('App19component22', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component22 />);
    expect(baseElement).toBeTruthy();
  });
});
