import { render } from '@testing-library/react';

import App19component61 from './app19component61';

describe('App19component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component61 />);
    expect(baseElement).toBeTruthy();
  });
});
