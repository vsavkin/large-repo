import { render } from '@testing-library/react';

import App19component89 from './app19component89';

describe('App19component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component89 />);
    expect(baseElement).toBeTruthy();
  });
});
