import { render } from '@testing-library/react';

import App19component14 from './app19component14';

describe('App19component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component14 />);
    expect(baseElement).toBeTruthy();
  });
});
