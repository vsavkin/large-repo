import { render } from '@testing-library/react';

import App19component88 from './app19component88';

describe('App19component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component88 />);
    expect(baseElement).toBeTruthy();
  });
});
