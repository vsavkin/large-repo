import { render } from '@testing-library/react';

import App19component91 from './app19component91';

describe('App19component91', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component91 />);
    expect(baseElement).toBeTruthy();
  });
});
