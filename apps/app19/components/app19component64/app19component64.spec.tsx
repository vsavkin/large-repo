import { render } from '@testing-library/react';

import App19component64 from './app19component64';

describe('App19component64', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component64 />);
    expect(baseElement).toBeTruthy();
  });
});
