import { render } from '@testing-library/react';

import App19component47 from './app19component47';

describe('App19component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component47 />);
    expect(baseElement).toBeTruthy();
  });
});
