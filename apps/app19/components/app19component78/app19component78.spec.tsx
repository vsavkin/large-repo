import { render } from '@testing-library/react';

import App19component78 from './app19component78';

describe('App19component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component78 />);
    expect(baseElement).toBeTruthy();
  });
});
