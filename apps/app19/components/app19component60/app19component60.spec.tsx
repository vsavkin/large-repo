import { render } from '@testing-library/react';

import App19component60 from './app19component60';

describe('App19component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component60 />);
    expect(baseElement).toBeTruthy();
  });
});
