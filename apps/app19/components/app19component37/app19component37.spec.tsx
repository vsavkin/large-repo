import { render } from '@testing-library/react';

import App19component37 from './app19component37';

describe('App19component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component37 />);
    expect(baseElement).toBeTruthy();
  });
});
