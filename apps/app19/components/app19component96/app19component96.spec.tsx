import { render } from '@testing-library/react';

import App19component96 from './app19component96';

describe('App19component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component96 />);
    expect(baseElement).toBeTruthy();
  });
});
