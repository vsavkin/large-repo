import { render } from '@testing-library/react';

import App19component76 from './app19component76';

describe('App19component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component76 />);
    expect(baseElement).toBeTruthy();
  });
});
