import { render } from '@testing-library/react';

import App19component73 from './app19component73';

describe('App19component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component73 />);
    expect(baseElement).toBeTruthy();
  });
});
