import { render } from '@testing-library/react';

import App19component35 from './app19component35';

describe('App19component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component35 />);
    expect(baseElement).toBeTruthy();
  });
});
