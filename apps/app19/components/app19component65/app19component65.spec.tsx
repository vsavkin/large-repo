import { render } from '@testing-library/react';

import App19component65 from './app19component65';

describe('App19component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component65 />);
    expect(baseElement).toBeTruthy();
  });
});
