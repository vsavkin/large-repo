import { render } from '@testing-library/react';

import App19component75 from './app19component75';

describe('App19component75', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component75 />);
    expect(baseElement).toBeTruthy();
  });
});
