import { render } from '@testing-library/react';

import App19component36 from './app19component36';

describe('App19component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component36 />);
    expect(baseElement).toBeTruthy();
  });
});
