import { render } from '@testing-library/react';

import App19component87 from './app19component87';

describe('App19component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component87 />);
    expect(baseElement).toBeTruthy();
  });
});
