import { render } from '@testing-library/react';

import App19component82 from './app19component82';

describe('App19component82', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component82 />);
    expect(baseElement).toBeTruthy();
  });
});
