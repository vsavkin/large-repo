import { render } from '@testing-library/react';

import App19component71 from './app19component71';

describe('App19component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component71 />);
    expect(baseElement).toBeTruthy();
  });
});
