import { render } from '@testing-library/react';

import App19component83 from './app19component83';

describe('App19component83', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App19component83 />);
    expect(baseElement).toBeTruthy();
  });
});
