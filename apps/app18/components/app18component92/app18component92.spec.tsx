import { render } from '@testing-library/react';

import App18component92 from './app18component92';

describe('App18component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component92 />);
    expect(baseElement).toBeTruthy();
  });
});
