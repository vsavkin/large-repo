import { render } from '@testing-library/react';

import App18component72 from './app18component72';

describe('App18component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component72 />);
    expect(baseElement).toBeTruthy();
  });
});
