import { render } from '@testing-library/react';

import App18component97 from './app18component97';

describe('App18component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component97 />);
    expect(baseElement).toBeTruthy();
  });
});
