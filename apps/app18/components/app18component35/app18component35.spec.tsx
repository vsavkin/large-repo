import { render } from '@testing-library/react';

import App18component35 from './app18component35';

describe('App18component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component35 />);
    expect(baseElement).toBeTruthy();
  });
});
