import { render } from '@testing-library/react';

import App18component76 from './app18component76';

describe('App18component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component76 />);
    expect(baseElement).toBeTruthy();
  });
});
