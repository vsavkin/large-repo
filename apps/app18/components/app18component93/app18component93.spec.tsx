import { render } from '@testing-library/react';

import App18component93 from './app18component93';

describe('App18component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component93 />);
    expect(baseElement).toBeTruthy();
  });
});
