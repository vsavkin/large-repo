import { render } from '@testing-library/react';

import App18component53 from './app18component53';

describe('App18component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component53 />);
    expect(baseElement).toBeTruthy();
  });
});
