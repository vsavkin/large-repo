import { render } from '@testing-library/react';

import App18component31 from './app18component31';

describe('App18component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component31 />);
    expect(baseElement).toBeTruthy();
  });
});
