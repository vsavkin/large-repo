import { render } from '@testing-library/react';

import App18component58 from './app18component58';

describe('App18component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component58 />);
    expect(baseElement).toBeTruthy();
  });
});
