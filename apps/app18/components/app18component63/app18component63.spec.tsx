import { render } from '@testing-library/react';

import App18component63 from './app18component63';

describe('App18component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component63 />);
    expect(baseElement).toBeTruthy();
  });
});
