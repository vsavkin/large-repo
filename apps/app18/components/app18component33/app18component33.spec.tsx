import { render } from '@testing-library/react';

import App18component33 from './app18component33';

describe('App18component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component33 />);
    expect(baseElement).toBeTruthy();
  });
});
