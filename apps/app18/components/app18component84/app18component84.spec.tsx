import { render } from '@testing-library/react';

import App18component84 from './app18component84';

describe('App18component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component84 />);
    expect(baseElement).toBeTruthy();
  });
});
