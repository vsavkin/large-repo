import { render } from '@testing-library/react';

import App18component52 from './app18component52';

describe('App18component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component52 />);
    expect(baseElement).toBeTruthy();
  });
});
