import { render } from '@testing-library/react';

import App18component66 from './app18component66';

describe('App18component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component66 />);
    expect(baseElement).toBeTruthy();
  });
});
