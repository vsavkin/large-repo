import { render } from '@testing-library/react';

import App18component56 from './app18component56';

describe('App18component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component56 />);
    expect(baseElement).toBeTruthy();
  });
});
