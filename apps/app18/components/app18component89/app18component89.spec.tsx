import { render } from '@testing-library/react';

import App18component89 from './app18component89';

describe('App18component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component89 />);
    expect(baseElement).toBeTruthy();
  });
});
