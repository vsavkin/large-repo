import { render } from '@testing-library/react';

import App18component67 from './app18component67';

describe('App18component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component67 />);
    expect(baseElement).toBeTruthy();
  });
});
