import { render } from '@testing-library/react';

import App18component68 from './app18component68';

describe('App18component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component68 />);
    expect(baseElement).toBeTruthy();
  });
});
