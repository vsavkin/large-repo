import { render } from '@testing-library/react';

import App18component74 from './app18component74';

describe('App18component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component74 />);
    expect(baseElement).toBeTruthy();
  });
});
