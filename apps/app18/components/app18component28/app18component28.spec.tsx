import { render } from '@testing-library/react';

import App18component28 from './app18component28';

describe('App18component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component28 />);
    expect(baseElement).toBeTruthy();
  });
});
