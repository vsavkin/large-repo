import { render } from '@testing-library/react';

import App18component57 from './app18component57';

describe('App18component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component57 />);
    expect(baseElement).toBeTruthy();
  });
});
