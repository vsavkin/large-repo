import { render } from '@testing-library/react';

import App18component45 from './app18component45';

describe('App18component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component45 />);
    expect(baseElement).toBeTruthy();
  });
});
