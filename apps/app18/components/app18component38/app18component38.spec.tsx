import { render } from '@testing-library/react';

import App18component38 from './app18component38';

describe('App18component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component38 />);
    expect(baseElement).toBeTruthy();
  });
});
