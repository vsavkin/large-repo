import { render } from '@testing-library/react';

import App18component87 from './app18component87';

describe('App18component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component87 />);
    expect(baseElement).toBeTruthy();
  });
});
