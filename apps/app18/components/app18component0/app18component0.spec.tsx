import { render } from '@testing-library/react';

import App18component0 from './app18component0';

describe('App18component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component0 />);
    expect(baseElement).toBeTruthy();
  });
});
