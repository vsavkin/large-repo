import { render } from '@testing-library/react';

import App18component36 from './app18component36';

describe('App18component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component36 />);
    expect(baseElement).toBeTruthy();
  });
});
