import { render } from '@testing-library/react';

import App18component49 from './app18component49';

describe('App18component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component49 />);
    expect(baseElement).toBeTruthy();
  });
});
