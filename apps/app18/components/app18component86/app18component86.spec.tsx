import { render } from '@testing-library/react';

import App18component86 from './app18component86';

describe('App18component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component86 />);
    expect(baseElement).toBeTruthy();
  });
});
