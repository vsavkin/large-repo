import { render } from '@testing-library/react';

import App18component44 from './app18component44';

describe('App18component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component44 />);
    expect(baseElement).toBeTruthy();
  });
});
