import { render } from '@testing-library/react';

import App18component48 from './app18component48';

describe('App18component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component48 />);
    expect(baseElement).toBeTruthy();
  });
});
