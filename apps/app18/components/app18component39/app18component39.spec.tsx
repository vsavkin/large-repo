import { render } from '@testing-library/react';

import App18component39 from './app18component39';

describe('App18component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component39 />);
    expect(baseElement).toBeTruthy();
  });
});
