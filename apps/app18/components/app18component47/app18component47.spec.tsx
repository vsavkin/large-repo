import { render } from '@testing-library/react';

import App18component47 from './app18component47';

describe('App18component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component47 />);
    expect(baseElement).toBeTruthy();
  });
});
