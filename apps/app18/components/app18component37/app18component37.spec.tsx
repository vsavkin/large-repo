import { render } from '@testing-library/react';

import App18component37 from './app18component37';

describe('App18component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component37 />);
    expect(baseElement).toBeTruthy();
  });
});
