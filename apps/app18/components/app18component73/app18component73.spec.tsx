import { render } from '@testing-library/react';

import App18component73 from './app18component73';

describe('App18component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component73 />);
    expect(baseElement).toBeTruthy();
  });
});
