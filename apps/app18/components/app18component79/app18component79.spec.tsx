import { render } from '@testing-library/react';

import App18component79 from './app18component79';

describe('App18component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component79 />);
    expect(baseElement).toBeTruthy();
  });
});
