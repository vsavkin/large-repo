import { render } from '@testing-library/react';

import App18component59 from './app18component59';

describe('App18component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component59 />);
    expect(baseElement).toBeTruthy();
  });
});
