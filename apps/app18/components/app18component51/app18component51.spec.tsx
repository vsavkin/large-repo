import { render } from '@testing-library/react';

import App18component51 from './app18component51';

describe('App18component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component51 />);
    expect(baseElement).toBeTruthy();
  });
});
