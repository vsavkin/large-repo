import { render } from '@testing-library/react';

import App18component62 from './app18component62';

describe('App18component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component62 />);
    expect(baseElement).toBeTruthy();
  });
});
