import { render } from '@testing-library/react';

import App18component29 from './app18component29';

describe('App18component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component29 />);
    expect(baseElement).toBeTruthy();
  });
});
