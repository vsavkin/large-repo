import { render } from '@testing-library/react';

import App18component88 from './app18component88';

describe('App18component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component88 />);
    expect(baseElement).toBeTruthy();
  });
});
