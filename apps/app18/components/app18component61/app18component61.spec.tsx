import { render } from '@testing-library/react';

import App18component61 from './app18component61';

describe('App18component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component61 />);
    expect(baseElement).toBeTruthy();
  });
});
