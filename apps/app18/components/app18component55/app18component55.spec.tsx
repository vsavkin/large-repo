import { render } from '@testing-library/react';

import App18component55 from './app18component55';

describe('App18component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component55 />);
    expect(baseElement).toBeTruthy();
  });
});
