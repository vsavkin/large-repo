import { render } from '@testing-library/react';

import App18component54 from './app18component54';

describe('App18component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component54 />);
    expect(baseElement).toBeTruthy();
  });
});
