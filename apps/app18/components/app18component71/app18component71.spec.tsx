import { render } from '@testing-library/react';

import App18component71 from './app18component71';

describe('App18component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component71 />);
    expect(baseElement).toBeTruthy();
  });
});
