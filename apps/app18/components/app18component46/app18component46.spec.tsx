import { render } from '@testing-library/react';

import App18component46 from './app18component46';

describe('App18component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component46 />);
    expect(baseElement).toBeTruthy();
  });
});
