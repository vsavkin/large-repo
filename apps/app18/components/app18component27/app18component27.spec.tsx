import { render } from '@testing-library/react';

import App18component27 from './app18component27';

describe('App18component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App18component27 />);
    expect(baseElement).toBeTruthy();
  });
});
