import { render } from '@testing-library/react';

import App14component73 from './app14component73';

describe('App14component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component73 />);
    expect(baseElement).toBeTruthy();
  });
});
