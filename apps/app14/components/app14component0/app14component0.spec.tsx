import { render } from '@testing-library/react';

import App14component0 from './app14component0';

describe('App14component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component0 />);
    expect(baseElement).toBeTruthy();
  });
});
