import { render } from '@testing-library/react';

import App14component87 from './app14component87';

describe('App14component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component87 />);
    expect(baseElement).toBeTruthy();
  });
});
