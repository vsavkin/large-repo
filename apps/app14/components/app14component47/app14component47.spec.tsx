import { render } from '@testing-library/react';

import App14component47 from './app14component47';

describe('App14component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component47 />);
    expect(baseElement).toBeTruthy();
  });
});
