import { render } from '@testing-library/react';

import App14component54 from './app14component54';

describe('App14component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component54 />);
    expect(baseElement).toBeTruthy();
  });
});
