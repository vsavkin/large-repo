import { render } from '@testing-library/react';

import App14component48 from './app14component48';

describe('App14component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component48 />);
    expect(baseElement).toBeTruthy();
  });
});
