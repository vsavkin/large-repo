import { render } from '@testing-library/react';

import App14component78 from './app14component78';

describe('App14component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component78 />);
    expect(baseElement).toBeTruthy();
  });
});
