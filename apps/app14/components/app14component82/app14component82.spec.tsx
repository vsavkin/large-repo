import { render } from '@testing-library/react';

import App14component82 from './app14component82';

describe('App14component82', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component82 />);
    expect(baseElement).toBeTruthy();
  });
});
