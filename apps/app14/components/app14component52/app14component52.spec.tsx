import { render } from '@testing-library/react';

import App14component52 from './app14component52';

describe('App14component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component52 />);
    expect(baseElement).toBeTruthy();
  });
});
