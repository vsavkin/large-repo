import { render } from '@testing-library/react';

import App14component92 from './app14component92';

describe('App14component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component92 />);
    expect(baseElement).toBeTruthy();
  });
});
