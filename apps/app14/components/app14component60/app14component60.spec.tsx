import { render } from '@testing-library/react';

import App14component60 from './app14component60';

describe('App14component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component60 />);
    expect(baseElement).toBeTruthy();
  });
});
