import { render } from '@testing-library/react';

import App14component80 from './app14component80';

describe('App14component80', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component80 />);
    expect(baseElement).toBeTruthy();
  });
});
