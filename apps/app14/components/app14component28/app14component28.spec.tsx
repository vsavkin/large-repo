import { render } from '@testing-library/react';

import App14component28 from './app14component28';

describe('App14component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component28 />);
    expect(baseElement).toBeTruthy();
  });
});
