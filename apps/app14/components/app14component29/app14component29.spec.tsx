import { render } from '@testing-library/react';

import App14component29 from './app14component29';

describe('App14component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component29 />);
    expect(baseElement).toBeTruthy();
  });
});
