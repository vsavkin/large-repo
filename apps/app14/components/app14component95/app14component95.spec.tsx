import { render } from '@testing-library/react';

import App14component95 from './app14component95';

describe('App14component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component95 />);
    expect(baseElement).toBeTruthy();
  });
});
