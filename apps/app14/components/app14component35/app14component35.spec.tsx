import { render } from '@testing-library/react';

import App14component35 from './app14component35';

describe('App14component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component35 />);
    expect(baseElement).toBeTruthy();
  });
});
