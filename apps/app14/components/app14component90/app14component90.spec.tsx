import { render } from '@testing-library/react';

import App14component90 from './app14component90';

describe('App14component90', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component90 />);
    expect(baseElement).toBeTruthy();
  });
});
