import { render } from '@testing-library/react';

import App14component62 from './app14component62';

describe('App14component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component62 />);
    expect(baseElement).toBeTruthy();
  });
});
