import { render } from '@testing-library/react';

import App14component81 from './app14component81';

describe('App14component81', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component81 />);
    expect(baseElement).toBeTruthy();
  });
});
