import { render } from '@testing-library/react';

import App14component30 from './app14component30';

describe('App14component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component30 />);
    expect(baseElement).toBeTruthy();
  });
});
