import { render } from '@testing-library/react';

import App14component26 from './app14component26';

describe('App14component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component26 />);
    expect(baseElement).toBeTruthy();
  });
});
