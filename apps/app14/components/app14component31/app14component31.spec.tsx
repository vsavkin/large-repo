import { render } from '@testing-library/react';

import App14component31 from './app14component31';

describe('App14component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component31 />);
    expect(baseElement).toBeTruthy();
  });
});
