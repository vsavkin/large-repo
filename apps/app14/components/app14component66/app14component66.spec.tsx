import { render } from '@testing-library/react';

import App14component66 from './app14component66';

describe('App14component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component66 />);
    expect(baseElement).toBeTruthy();
  });
});
