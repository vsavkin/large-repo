import { render } from '@testing-library/react';

import App14component61 from './app14component61';

describe('App14component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component61 />);
    expect(baseElement).toBeTruthy();
  });
});
