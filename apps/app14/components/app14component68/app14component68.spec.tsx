import { render } from '@testing-library/react';

import App14component68 from './app14component68';

describe('App14component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component68 />);
    expect(baseElement).toBeTruthy();
  });
});
