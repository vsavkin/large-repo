import { render } from '@testing-library/react';

import App14component13 from './app14component13';

describe('App14component13', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component13 />);
    expect(baseElement).toBeTruthy();
  });
});
