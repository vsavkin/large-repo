import { render } from '@testing-library/react';

import App14component58 from './app14component58';

describe('App14component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component58 />);
    expect(baseElement).toBeTruthy();
  });
});
