import { render } from '@testing-library/react';

import App14component53 from './app14component53';

describe('App14component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component53 />);
    expect(baseElement).toBeTruthy();
  });
});
