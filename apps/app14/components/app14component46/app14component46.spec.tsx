import { render } from '@testing-library/react';

import App14component46 from './app14component46';

describe('App14component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component46 />);
    expect(baseElement).toBeTruthy();
  });
});
