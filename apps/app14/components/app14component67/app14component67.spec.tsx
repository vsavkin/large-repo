import { render } from '@testing-library/react';

import App14component67 from './app14component67';

describe('App14component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component67 />);
    expect(baseElement).toBeTruthy();
  });
});
