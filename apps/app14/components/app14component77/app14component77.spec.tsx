import { render } from '@testing-library/react';

import App14component77 from './app14component77';

describe('App14component77', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component77 />);
    expect(baseElement).toBeTruthy();
  });
});
