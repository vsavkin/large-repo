import { render } from '@testing-library/react';

import App14component19 from './app14component19';

describe('App14component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component19 />);
    expect(baseElement).toBeTruthy();
  });
});
