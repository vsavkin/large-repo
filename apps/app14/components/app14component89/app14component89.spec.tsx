import { render } from '@testing-library/react';

import App14component89 from './app14component89';

describe('App14component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component89 />);
    expect(baseElement).toBeTruthy();
  });
});
