import { render } from '@testing-library/react';

import App14component39 from './app14component39';

describe('App14component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component39 />);
    expect(baseElement).toBeTruthy();
  });
});
