import { render } from '@testing-library/react';

import App14component27 from './app14component27';

describe('App14component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component27 />);
    expect(baseElement).toBeTruthy();
  });
});
