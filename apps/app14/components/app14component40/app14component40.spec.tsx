import { render } from '@testing-library/react';

import App14component40 from './app14component40';

describe('App14component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component40 />);
    expect(baseElement).toBeTruthy();
  });
});
