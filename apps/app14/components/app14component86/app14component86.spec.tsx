import { render } from '@testing-library/react';

import App14component86 from './app14component86';

describe('App14component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component86 />);
    expect(baseElement).toBeTruthy();
  });
});
