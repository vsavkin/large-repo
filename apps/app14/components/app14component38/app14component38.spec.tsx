import { render } from '@testing-library/react';

import App14component38 from './app14component38';

describe('App14component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component38 />);
    expect(baseElement).toBeTruthy();
  });
});
