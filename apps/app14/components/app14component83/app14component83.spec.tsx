import { render } from '@testing-library/react';

import App14component83 from './app14component83';

describe('App14component83', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component83 />);
    expect(baseElement).toBeTruthy();
  });
});
