import { render } from '@testing-library/react';

import App14component93 from './app14component93';

describe('App14component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component93 />);
    expect(baseElement).toBeTruthy();
  });
});
