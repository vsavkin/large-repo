import { render } from '@testing-library/react';

import App14component65 from './app14component65';

describe('App14component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component65 />);
    expect(baseElement).toBeTruthy();
  });
});
