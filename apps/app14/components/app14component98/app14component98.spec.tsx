import { render } from '@testing-library/react';

import App14component98 from './app14component98';

describe('App14component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component98 />);
    expect(baseElement).toBeTruthy();
  });
});
