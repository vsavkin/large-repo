import { render } from '@testing-library/react';

import App14component45 from './app14component45';

describe('App14component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component45 />);
    expect(baseElement).toBeTruthy();
  });
});
