import { render } from '@testing-library/react';

import App14component71 from './app14component71';

describe('App14component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component71 />);
    expect(baseElement).toBeTruthy();
  });
});
