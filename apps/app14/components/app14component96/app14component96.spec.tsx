import { render } from '@testing-library/react';

import App14component96 from './app14component96';

describe('App14component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component96 />);
    expect(baseElement).toBeTruthy();
  });
});
