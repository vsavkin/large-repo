import { render } from '@testing-library/react';

import App14component70 from './app14component70';

describe('App14component70', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component70 />);
    expect(baseElement).toBeTruthy();
  });
});
