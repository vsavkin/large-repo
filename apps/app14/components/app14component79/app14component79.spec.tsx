import { render } from '@testing-library/react';

import App14component79 from './app14component79';

describe('App14component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component79 />);
    expect(baseElement).toBeTruthy();
  });
});
