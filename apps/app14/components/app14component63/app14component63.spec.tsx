import { render } from '@testing-library/react';

import App14component63 from './app14component63';

describe('App14component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component63 />);
    expect(baseElement).toBeTruthy();
  });
});
