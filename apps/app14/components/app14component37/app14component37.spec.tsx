import { render } from '@testing-library/react';

import App14component37 from './app14component37';

describe('App14component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component37 />);
    expect(baseElement).toBeTruthy();
  });
});
