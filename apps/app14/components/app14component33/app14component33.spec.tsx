import { render } from '@testing-library/react';

import App14component33 from './app14component33';

describe('App14component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component33 />);
    expect(baseElement).toBeTruthy();
  });
});
