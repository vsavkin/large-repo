import { render } from '@testing-library/react';

import App14component88 from './app14component88';

describe('App14component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component88 />);
    expect(baseElement).toBeTruthy();
  });
});
