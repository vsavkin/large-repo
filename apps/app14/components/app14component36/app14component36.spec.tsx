import { render } from '@testing-library/react';

import App14component36 from './app14component36';

describe('App14component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component36 />);
    expect(baseElement).toBeTruthy();
  });
});
