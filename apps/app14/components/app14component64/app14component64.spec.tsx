import { render } from '@testing-library/react';

import App14component64 from './app14component64';

describe('App14component64', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component64 />);
    expect(baseElement).toBeTruthy();
  });
});
