import { render } from '@testing-library/react';

import App14component57 from './app14component57';

describe('App14component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component57 />);
    expect(baseElement).toBeTruthy();
  });
});
