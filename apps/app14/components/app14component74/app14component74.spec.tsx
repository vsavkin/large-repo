import { render } from '@testing-library/react';

import App14component74 from './app14component74';

describe('App14component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component74 />);
    expect(baseElement).toBeTruthy();
  });
});
