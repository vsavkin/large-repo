import { render } from '@testing-library/react';

import App14component72 from './app14component72';

describe('App14component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component72 />);
    expect(baseElement).toBeTruthy();
  });
});
