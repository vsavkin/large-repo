import { render } from '@testing-library/react';

import App14component76 from './app14component76';

describe('App14component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component76 />);
    expect(baseElement).toBeTruthy();
  });
});
