import { render } from '@testing-library/react';

import App14component51 from './app14component51';

describe('App14component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component51 />);
    expect(baseElement).toBeTruthy();
  });
});
