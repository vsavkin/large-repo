import { render } from '@testing-library/react';

import App14component44 from './app14component44';

describe('App14component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component44 />);
    expect(baseElement).toBeTruthy();
  });
});
