import { render } from '@testing-library/react';

import App14component15 from './app14component15';

describe('App14component15', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component15 />);
    expect(baseElement).toBeTruthy();
  });
});
