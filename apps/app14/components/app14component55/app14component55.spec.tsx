import { render } from '@testing-library/react';

import App14component55 from './app14component55';

describe('App14component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component55 />);
    expect(baseElement).toBeTruthy();
  });
});
