import { render } from '@testing-library/react';

import App14component49 from './app14component49';

describe('App14component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component49 />);
    expect(baseElement).toBeTruthy();
  });
});
