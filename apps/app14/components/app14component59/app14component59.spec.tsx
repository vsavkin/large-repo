import { render } from '@testing-library/react';

import App14component59 from './app14component59';

describe('App14component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component59 />);
    expect(baseElement).toBeTruthy();
  });
});
