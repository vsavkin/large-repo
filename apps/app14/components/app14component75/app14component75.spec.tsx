import { render } from '@testing-library/react';

import App14component75 from './app14component75';

describe('App14component75', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component75 />);
    expect(baseElement).toBeTruthy();
  });
});
