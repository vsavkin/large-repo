import { render } from '@testing-library/react';

import App14component84 from './app14component84';

describe('App14component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App14component84 />);
    expect(baseElement).toBeTruthy();
  });
});
