import { render } from '@testing-library/react';

import App2component52 from './app2component52';

describe('App2component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component52 />);
    expect(baseElement).toBeTruthy();
  });
});
