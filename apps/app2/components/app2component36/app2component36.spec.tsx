import { render } from '@testing-library/react';

import App2component36 from './app2component36';

describe('App2component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component36 />);
    expect(baseElement).toBeTruthy();
  });
});
