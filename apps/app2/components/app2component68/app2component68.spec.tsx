import { render } from '@testing-library/react';

import App2component68 from './app2component68';

describe('App2component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component68 />);
    expect(baseElement).toBeTruthy();
  });
});
