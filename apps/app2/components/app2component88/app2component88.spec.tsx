import { render } from '@testing-library/react';

import App2component88 from './app2component88';

describe('App2component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component88 />);
    expect(baseElement).toBeTruthy();
  });
});
