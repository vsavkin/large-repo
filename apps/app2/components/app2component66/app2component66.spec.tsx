import { render } from '@testing-library/react';

import App2component66 from './app2component66';

describe('App2component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component66 />);
    expect(baseElement).toBeTruthy();
  });
});
