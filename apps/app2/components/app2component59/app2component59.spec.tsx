import { render } from '@testing-library/react';

import App2component59 from './app2component59';

describe('App2component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component59 />);
    expect(baseElement).toBeTruthy();
  });
});
