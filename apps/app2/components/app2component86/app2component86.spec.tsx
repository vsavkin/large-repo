import { render } from '@testing-library/react';

import App2component86 from './app2component86';

describe('App2component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component86 />);
    expect(baseElement).toBeTruthy();
  });
});
