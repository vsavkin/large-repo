import { render } from '@testing-library/react';

import App2component93 from './app2component93';

describe('App2component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component93 />);
    expect(baseElement).toBeTruthy();
  });
});
