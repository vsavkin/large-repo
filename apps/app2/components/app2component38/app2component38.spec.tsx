import { render } from '@testing-library/react';

import App2component38 from './app2component38';

describe('App2component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component38 />);
    expect(baseElement).toBeTruthy();
  });
});
