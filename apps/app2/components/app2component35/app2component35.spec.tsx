import { render } from '@testing-library/react';

import App2component35 from './app2component35';

describe('App2component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component35 />);
    expect(baseElement).toBeTruthy();
  });
});
