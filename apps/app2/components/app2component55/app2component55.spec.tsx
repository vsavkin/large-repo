import { render } from '@testing-library/react';

import App2component55 from './app2component55';

describe('App2component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component55 />);
    expect(baseElement).toBeTruthy();
  });
});
