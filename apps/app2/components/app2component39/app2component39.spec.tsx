import { render } from '@testing-library/react';

import App2component39 from './app2component39';

describe('App2component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component39 />);
    expect(baseElement).toBeTruthy();
  });
});
