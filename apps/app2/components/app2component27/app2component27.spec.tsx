import { render } from '@testing-library/react';

import App2component27 from './app2component27';

describe('App2component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component27 />);
    expect(baseElement).toBeTruthy();
  });
});
