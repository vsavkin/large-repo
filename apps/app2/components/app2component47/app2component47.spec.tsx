import { render } from '@testing-library/react';

import App2component47 from './app2component47';

describe('App2component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component47 />);
    expect(baseElement).toBeTruthy();
  });
});
