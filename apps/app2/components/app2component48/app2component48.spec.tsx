import { render } from '@testing-library/react';

import App2component48 from './app2component48';

describe('App2component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component48 />);
    expect(baseElement).toBeTruthy();
  });
});
