import { render } from '@testing-library/react';

import App2component87 from './app2component87';

describe('App2component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component87 />);
    expect(baseElement).toBeTruthy();
  });
});
