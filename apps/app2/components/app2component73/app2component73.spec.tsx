import { render } from '@testing-library/react';

import App2component73 from './app2component73';

describe('App2component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component73 />);
    expect(baseElement).toBeTruthy();
  });
});
