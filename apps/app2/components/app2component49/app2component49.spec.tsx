import { render } from '@testing-library/react';

import App2component49 from './app2component49';

describe('App2component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component49 />);
    expect(baseElement).toBeTruthy();
  });
});
