import { render } from '@testing-library/react';

import App2component67 from './app2component67';

describe('App2component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component67 />);
    expect(baseElement).toBeTruthy();
  });
});
