import { render } from '@testing-library/react';

import App2component44 from './app2component44';

describe('App2component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component44 />);
    expect(baseElement).toBeTruthy();
  });
});
