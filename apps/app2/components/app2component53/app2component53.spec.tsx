import { render } from '@testing-library/react';

import App2component53 from './app2component53';

describe('App2component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component53 />);
    expect(baseElement).toBeTruthy();
  });
});
