import { render } from '@testing-library/react';

import App2component58 from './app2component58';

describe('App2component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component58 />);
    expect(baseElement).toBeTruthy();
  });
});
