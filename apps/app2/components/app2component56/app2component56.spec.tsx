import { render } from '@testing-library/react';

import App2component56 from './app2component56';

describe('App2component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component56 />);
    expect(baseElement).toBeTruthy();
  });
});
