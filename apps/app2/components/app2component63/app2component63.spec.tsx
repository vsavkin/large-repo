import { render } from '@testing-library/react';

import App2component63 from './app2component63';

describe('App2component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App2component63 />);
    expect(baseElement).toBeTruthy();
  });
});
