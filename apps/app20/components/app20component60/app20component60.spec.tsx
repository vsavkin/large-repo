import { render } from '@testing-library/react';

import App20component60 from './app20component60';

describe('App20component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component60 />);
    expect(baseElement).toBeTruthy();
  });
});
