import { render } from '@testing-library/react';

import App20component95 from './app20component95';

describe('App20component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component95 />);
    expect(baseElement).toBeTruthy();
  });
});
