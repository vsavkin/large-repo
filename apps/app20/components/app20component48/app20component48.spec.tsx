import { render } from '@testing-library/react';

import App20component48 from './app20component48';

describe('App20component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component48 />);
    expect(baseElement).toBeTruthy();
  });
});
