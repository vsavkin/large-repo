import { render } from '@testing-library/react';

import App20component71 from './app20component71';

describe('App20component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component71 />);
    expect(baseElement).toBeTruthy();
  });
});
