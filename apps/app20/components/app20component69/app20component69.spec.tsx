import { render } from '@testing-library/react';

import App20component69 from './app20component69';

describe('App20component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component69 />);
    expect(baseElement).toBeTruthy();
  });
});
