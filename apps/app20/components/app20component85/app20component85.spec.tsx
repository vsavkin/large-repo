import { render } from '@testing-library/react';

import App20component85 from './app20component85';

describe('App20component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component85 />);
    expect(baseElement).toBeTruthy();
  });
});
