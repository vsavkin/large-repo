import { render } from '@testing-library/react';

import App20component27 from './app20component27';

describe('App20component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component27 />);
    expect(baseElement).toBeTruthy();
  });
});
