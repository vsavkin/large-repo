import { render } from '@testing-library/react';

import App20component87 from './app20component87';

describe('App20component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component87 />);
    expect(baseElement).toBeTruthy();
  });
});
