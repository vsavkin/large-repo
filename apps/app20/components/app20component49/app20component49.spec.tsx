import { render } from '@testing-library/react';

import App20component49 from './app20component49';

describe('App20component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component49 />);
    expect(baseElement).toBeTruthy();
  });
});
