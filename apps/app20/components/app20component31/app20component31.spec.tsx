import { render } from '@testing-library/react';

import App20component31 from './app20component31';

describe('App20component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component31 />);
    expect(baseElement).toBeTruthy();
  });
});
