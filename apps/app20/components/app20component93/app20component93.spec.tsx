import { render } from '@testing-library/react';

import App20component93 from './app20component93';

describe('App20component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component93 />);
    expect(baseElement).toBeTruthy();
  });
});
