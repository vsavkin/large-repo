import { render } from '@testing-library/react';

import App20component68 from './app20component68';

describe('App20component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component68 />);
    expect(baseElement).toBeTruthy();
  });
});
