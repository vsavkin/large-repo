import { render } from '@testing-library/react';

import App20component76 from './app20component76';

describe('App20component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component76 />);
    expect(baseElement).toBeTruthy();
  });
});
