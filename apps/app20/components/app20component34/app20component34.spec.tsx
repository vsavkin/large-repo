import { render } from '@testing-library/react';

import App20component34 from './app20component34';

describe('App20component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component34 />);
    expect(baseElement).toBeTruthy();
  });
});
