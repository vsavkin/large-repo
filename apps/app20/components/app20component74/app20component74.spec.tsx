import { render } from '@testing-library/react';

import App20component74 from './app20component74';

describe('App20component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component74 />);
    expect(baseElement).toBeTruthy();
  });
});
