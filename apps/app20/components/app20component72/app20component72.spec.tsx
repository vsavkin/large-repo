import { render } from '@testing-library/react';

import App20component72 from './app20component72';

describe('App20component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component72 />);
    expect(baseElement).toBeTruthy();
  });
});
