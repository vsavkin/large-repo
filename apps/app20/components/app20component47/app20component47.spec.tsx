import { render } from '@testing-library/react';

import App20component47 from './app20component47';

describe('App20component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component47 />);
    expect(baseElement).toBeTruthy();
  });
});
