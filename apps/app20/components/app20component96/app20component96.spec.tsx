import { render } from '@testing-library/react';

import App20component96 from './app20component96';

describe('App20component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component96 />);
    expect(baseElement).toBeTruthy();
  });
});
