import { render } from '@testing-library/react';

import App20component52 from './app20component52';

describe('App20component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component52 />);
    expect(baseElement).toBeTruthy();
  });
});
