import { render } from '@testing-library/react';

import App20component37 from './app20component37';

describe('App20component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component37 />);
    expect(baseElement).toBeTruthy();
  });
});
