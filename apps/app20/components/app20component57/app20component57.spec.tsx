import { render } from '@testing-library/react';

import App20component57 from './app20component57';

describe('App20component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component57 />);
    expect(baseElement).toBeTruthy();
  });
});
