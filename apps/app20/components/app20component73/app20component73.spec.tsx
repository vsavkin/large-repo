import { render } from '@testing-library/react';

import App20component73 from './app20component73';

describe('App20component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component73 />);
    expect(baseElement).toBeTruthy();
  });
});
