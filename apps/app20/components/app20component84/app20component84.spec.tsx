import { render } from '@testing-library/react';

import App20component84 from './app20component84';

describe('App20component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component84 />);
    expect(baseElement).toBeTruthy();
  });
});
