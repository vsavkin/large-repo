import { render } from '@testing-library/react';

import App20component45 from './app20component45';

describe('App20component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component45 />);
    expect(baseElement).toBeTruthy();
  });
});
