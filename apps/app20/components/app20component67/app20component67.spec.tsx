import { render } from '@testing-library/react';

import App20component67 from './app20component67';

describe('App20component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component67 />);
    expect(baseElement).toBeTruthy();
  });
});
