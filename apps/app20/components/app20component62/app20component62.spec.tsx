import { render } from '@testing-library/react';

import App20component62 from './app20component62';

describe('App20component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component62 />);
    expect(baseElement).toBeTruthy();
  });
});
