import { render } from '@testing-library/react';

import App20component94 from './app20component94';

describe('App20component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component94 />);
    expect(baseElement).toBeTruthy();
  });
});
