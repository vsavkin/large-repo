import { render } from '@testing-library/react';

import App20component92 from './app20component92';

describe('App20component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component92 />);
    expect(baseElement).toBeTruthy();
  });
});
