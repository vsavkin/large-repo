import { render } from '@testing-library/react';

import App20component36 from './app20component36';

describe('App20component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component36 />);
    expect(baseElement).toBeTruthy();
  });
});
