import { render } from '@testing-library/react';

import App20component78 from './app20component78';

describe('App20component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component78 />);
    expect(baseElement).toBeTruthy();
  });
});
