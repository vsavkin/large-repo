import { render } from '@testing-library/react';

import App20component44 from './app20component44';

describe('App20component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component44 />);
    expect(baseElement).toBeTruthy();
  });
});
