import { render } from '@testing-library/react';

import App20component58 from './app20component58';

describe('App20component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component58 />);
    expect(baseElement).toBeTruthy();
  });
});
