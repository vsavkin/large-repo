import { render } from '@testing-library/react';

import App20component29 from './app20component29';

describe('App20component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component29 />);
    expect(baseElement).toBeTruthy();
  });
});
