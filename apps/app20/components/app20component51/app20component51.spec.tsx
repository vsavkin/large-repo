import { render } from '@testing-library/react';

import App20component51 from './app20component51';

describe('App20component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component51 />);
    expect(baseElement).toBeTruthy();
  });
});
