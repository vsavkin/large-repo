import { render } from '@testing-library/react';

import App20component63 from './app20component63';

describe('App20component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component63 />);
    expect(baseElement).toBeTruthy();
  });
});
