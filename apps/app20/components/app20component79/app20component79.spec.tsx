import { render } from '@testing-library/react';

import App20component79 from './app20component79';

describe('App20component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component79 />);
    expect(baseElement).toBeTruthy();
  });
});
