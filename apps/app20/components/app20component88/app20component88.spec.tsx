import { render } from '@testing-library/react';

import App20component88 from './app20component88';

describe('App20component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component88 />);
    expect(baseElement).toBeTruthy();
  });
});
