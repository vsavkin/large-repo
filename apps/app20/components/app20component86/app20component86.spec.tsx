import { render } from '@testing-library/react';

import App20component86 from './app20component86';

describe('App20component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component86 />);
    expect(baseElement).toBeTruthy();
  });
});
