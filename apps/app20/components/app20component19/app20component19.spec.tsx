import { render } from '@testing-library/react';

import App20component19 from './app20component19';

describe('App20component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component19 />);
    expect(baseElement).toBeTruthy();
  });
});
