import { render } from '@testing-library/react';

import App20component53 from './app20component53';

describe('App20component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component53 />);
    expect(baseElement).toBeTruthy();
  });
});
