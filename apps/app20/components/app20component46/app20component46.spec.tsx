import { render } from '@testing-library/react';

import App20component46 from './app20component46';

describe('App20component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component46 />);
    expect(baseElement).toBeTruthy();
  });
});
