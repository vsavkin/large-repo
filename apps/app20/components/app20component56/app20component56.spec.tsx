import { render } from '@testing-library/react';

import App20component56 from './app20component56';

describe('App20component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component56 />);
    expect(baseElement).toBeTruthy();
  });
});
