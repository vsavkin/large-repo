import { render } from '@testing-library/react';

import App20component39 from './app20component39';

describe('App20component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component39 />);
    expect(baseElement).toBeTruthy();
  });
});
