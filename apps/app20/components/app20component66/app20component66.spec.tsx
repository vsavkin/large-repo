import { render } from '@testing-library/react';

import App20component66 from './app20component66';

describe('App20component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component66 />);
    expect(baseElement).toBeTruthy();
  });
});
