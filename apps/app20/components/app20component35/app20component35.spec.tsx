import { render } from '@testing-library/react';

import App20component35 from './app20component35';

describe('App20component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component35 />);
    expect(baseElement).toBeTruthy();
  });
});
