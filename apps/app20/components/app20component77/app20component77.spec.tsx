import { render } from '@testing-library/react';

import App20component77 from './app20component77';

describe('App20component77', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component77 />);
    expect(baseElement).toBeTruthy();
  });
});
