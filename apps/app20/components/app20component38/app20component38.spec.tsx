import { render } from '@testing-library/react';

import App20component38 from './app20component38';

describe('App20component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component38 />);
    expect(baseElement).toBeTruthy();
  });
});
