import { render } from '@testing-library/react';

import App20component33 from './app20component33';

describe('App20component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component33 />);
    expect(baseElement).toBeTruthy();
  });
});
