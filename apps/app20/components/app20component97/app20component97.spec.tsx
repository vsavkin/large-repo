import { render } from '@testing-library/react';

import App20component97 from './app20component97';

describe('App20component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component97 />);
    expect(baseElement).toBeTruthy();
  });
});
