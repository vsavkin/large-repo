import { render } from '@testing-library/react';

import App20component28 from './app20component28';

describe('App20component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component28 />);
    expect(baseElement).toBeTruthy();
  });
});
