import { render } from '@testing-library/react';

import App20component98 from './app20component98';

describe('App20component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App20component98 />);
    expect(baseElement).toBeTruthy();
  });
});
