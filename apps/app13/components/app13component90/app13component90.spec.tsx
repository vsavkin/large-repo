import { render } from '@testing-library/react';

import App13component90 from './app13component90';

describe('App13component90', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component90 />);
    expect(baseElement).toBeTruthy();
  });
});
