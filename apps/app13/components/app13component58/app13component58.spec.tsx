import { render } from '@testing-library/react';

import App13component58 from './app13component58';

describe('App13component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component58 />);
    expect(baseElement).toBeTruthy();
  });
});
