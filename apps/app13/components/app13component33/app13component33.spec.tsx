import { render } from '@testing-library/react';

import App13component33 from './app13component33';

describe('App13component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component33 />);
    expect(baseElement).toBeTruthy();
  });
});
