import { render } from '@testing-library/react';

import App13component69 from './app13component69';

describe('App13component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component69 />);
    expect(baseElement).toBeTruthy();
  });
});
