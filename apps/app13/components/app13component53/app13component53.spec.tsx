import { render } from '@testing-library/react';

import App13component53 from './app13component53';

describe('App13component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component53 />);
    expect(baseElement).toBeTruthy();
  });
});
