import { render } from '@testing-library/react';

import App13component86 from './app13component86';

describe('App13component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component86 />);
    expect(baseElement).toBeTruthy();
  });
});
