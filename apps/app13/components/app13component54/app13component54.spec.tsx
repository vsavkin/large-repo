import { render } from '@testing-library/react';

import App13component54 from './app13component54';

describe('App13component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component54 />);
    expect(baseElement).toBeTruthy();
  });
});
