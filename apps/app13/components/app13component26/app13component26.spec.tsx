import { render } from '@testing-library/react';

import App13component26 from './app13component26';

describe('App13component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component26 />);
    expect(baseElement).toBeTruthy();
  });
});
