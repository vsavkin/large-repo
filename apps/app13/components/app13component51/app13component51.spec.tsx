import { render } from '@testing-library/react';

import App13component51 from './app13component51';

describe('App13component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component51 />);
    expect(baseElement).toBeTruthy();
  });
});
