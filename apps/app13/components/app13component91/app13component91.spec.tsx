import { render } from '@testing-library/react';

import App13component91 from './app13component91';

describe('App13component91', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component91 />);
    expect(baseElement).toBeTruthy();
  });
});
