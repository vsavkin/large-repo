import { render } from '@testing-library/react';

import App13component77 from './app13component77';

describe('App13component77', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component77 />);
    expect(baseElement).toBeTruthy();
  });
});
