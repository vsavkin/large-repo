import { render } from '@testing-library/react';

import App13component14 from './app13component14';

describe('App13component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component14 />);
    expect(baseElement).toBeTruthy();
  });
});
