import { render } from '@testing-library/react';

import App13component28 from './app13component28';

describe('App13component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component28 />);
    expect(baseElement).toBeTruthy();
  });
});
