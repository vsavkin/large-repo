import { render } from '@testing-library/react';

import App13component48 from './app13component48';

describe('App13component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component48 />);
    expect(baseElement).toBeTruthy();
  });
});
