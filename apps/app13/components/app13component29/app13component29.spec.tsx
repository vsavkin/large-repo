import { render } from '@testing-library/react';

import App13component29 from './app13component29';

describe('App13component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component29 />);
    expect(baseElement).toBeTruthy();
  });
});
