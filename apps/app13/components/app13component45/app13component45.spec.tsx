import { render } from '@testing-library/react';

import App13component45 from './app13component45';

describe('App13component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component45 />);
    expect(baseElement).toBeTruthy();
  });
});
