import { render } from '@testing-library/react';

import App13component40 from './app13component40';

describe('App13component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component40 />);
    expect(baseElement).toBeTruthy();
  });
});
