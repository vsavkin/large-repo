import { render } from '@testing-library/react';

import App13component56 from './app13component56';

describe('App13component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component56 />);
    expect(baseElement).toBeTruthy();
  });
});
