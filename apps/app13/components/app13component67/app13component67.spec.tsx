import { render } from '@testing-library/react';

import App13component67 from './app13component67';

describe('App13component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component67 />);
    expect(baseElement).toBeTruthy();
  });
});
