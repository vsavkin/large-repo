import { render } from '@testing-library/react';

import App13component73 from './app13component73';

describe('App13component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component73 />);
    expect(baseElement).toBeTruthy();
  });
});
