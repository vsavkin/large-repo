import { render } from '@testing-library/react';

import App13component39 from './app13component39';

describe('App13component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component39 />);
    expect(baseElement).toBeTruthy();
  });
});
