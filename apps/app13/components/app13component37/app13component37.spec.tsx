import { render } from '@testing-library/react';

import App13component37 from './app13component37';

describe('App13component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component37 />);
    expect(baseElement).toBeTruthy();
  });
});
