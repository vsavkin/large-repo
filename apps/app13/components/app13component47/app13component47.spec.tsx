import { render } from '@testing-library/react';

import App13component47 from './app13component47';

describe('App13component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component47 />);
    expect(baseElement).toBeTruthy();
  });
});
