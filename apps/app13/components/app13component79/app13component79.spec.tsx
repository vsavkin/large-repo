import { render } from '@testing-library/react';

import App13component79 from './app13component79';

describe('App13component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component79 />);
    expect(baseElement).toBeTruthy();
  });
});
