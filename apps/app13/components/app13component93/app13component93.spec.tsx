import { render } from '@testing-library/react';

import App13component93 from './app13component93';

describe('App13component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component93 />);
    expect(baseElement).toBeTruthy();
  });
});
