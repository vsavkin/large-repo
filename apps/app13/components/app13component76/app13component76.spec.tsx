import { render } from '@testing-library/react';

import App13component76 from './app13component76';

describe('App13component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component76 />);
    expect(baseElement).toBeTruthy();
  });
});
