import { render } from '@testing-library/react';

import App13component87 from './app13component87';

describe('App13component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component87 />);
    expect(baseElement).toBeTruthy();
  });
});
