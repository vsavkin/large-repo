import { render } from '@testing-library/react';

import App13component36 from './app13component36';

describe('App13component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component36 />);
    expect(baseElement).toBeTruthy();
  });
});
