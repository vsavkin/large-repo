import { render } from '@testing-library/react';

import App13component85 from './app13component85';

describe('App13component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component85 />);
    expect(baseElement).toBeTruthy();
  });
});
