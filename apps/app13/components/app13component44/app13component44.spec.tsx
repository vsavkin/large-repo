import { render } from '@testing-library/react';

import App13component44 from './app13component44';

describe('App13component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component44 />);
    expect(baseElement).toBeTruthy();
  });
});
