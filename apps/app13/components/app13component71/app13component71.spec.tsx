import { render } from '@testing-library/react';

import App13component71 from './app13component71';

describe('App13component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component71 />);
    expect(baseElement).toBeTruthy();
  });
});
