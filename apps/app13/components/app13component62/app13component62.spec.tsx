import { render } from '@testing-library/react';

import App13component62 from './app13component62';

describe('App13component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component62 />);
    expect(baseElement).toBeTruthy();
  });
});
