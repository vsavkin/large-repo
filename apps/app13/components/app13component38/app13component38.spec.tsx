import { render } from '@testing-library/react';

import App13component38 from './app13component38';

describe('App13component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component38 />);
    expect(baseElement).toBeTruthy();
  });
});
