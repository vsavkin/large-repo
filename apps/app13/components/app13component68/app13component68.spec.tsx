import { render } from '@testing-library/react';

import App13component68 from './app13component68';

describe('App13component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component68 />);
    expect(baseElement).toBeTruthy();
  });
});
