import { render } from '@testing-library/react';

import App13component97 from './app13component97';

describe('App13component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component97 />);
    expect(baseElement).toBeTruthy();
  });
});
