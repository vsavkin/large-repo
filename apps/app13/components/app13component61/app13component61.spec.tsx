import { render } from '@testing-library/react';

import App13component61 from './app13component61';

describe('App13component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component61 />);
    expect(baseElement).toBeTruthy();
  });
});
