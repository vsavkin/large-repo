import { render } from '@testing-library/react';

import App13component60 from './app13component60';

describe('App13component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component60 />);
    expect(baseElement).toBeTruthy();
  });
});
