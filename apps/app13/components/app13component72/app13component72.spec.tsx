import { render } from '@testing-library/react';

import App13component72 from './app13component72';

describe('App13component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component72 />);
    expect(baseElement).toBeTruthy();
  });
});
