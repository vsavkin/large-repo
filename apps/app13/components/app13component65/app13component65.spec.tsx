import { render } from '@testing-library/react';

import App13component65 from './app13component65';

describe('App13component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component65 />);
    expect(baseElement).toBeTruthy();
  });
});
