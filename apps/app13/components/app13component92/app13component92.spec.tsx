import { render } from '@testing-library/react';

import App13component92 from './app13component92';

describe('App13component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component92 />);
    expect(baseElement).toBeTruthy();
  });
});
