import { render } from '@testing-library/react';

import App13component49 from './app13component49';

describe('App13component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component49 />);
    expect(baseElement).toBeTruthy();
  });
});
