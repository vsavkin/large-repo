import { render } from '@testing-library/react';

import App13component84 from './app13component84';

describe('App13component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component84 />);
    expect(baseElement).toBeTruthy();
  });
});
