import { render } from '@testing-library/react';

import App13component78 from './app13component78';

describe('App13component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component78 />);
    expect(baseElement).toBeTruthy();
  });
});
