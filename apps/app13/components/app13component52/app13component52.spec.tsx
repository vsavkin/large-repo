import { render } from '@testing-library/react';

import App13component52 from './app13component52';

describe('App13component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component52 />);
    expect(baseElement).toBeTruthy();
  });
});
