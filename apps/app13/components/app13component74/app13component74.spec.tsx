import { render } from '@testing-library/react';

import App13component74 from './app13component74';

describe('App13component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component74 />);
    expect(baseElement).toBeTruthy();
  });
});
