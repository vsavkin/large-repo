import { render } from '@testing-library/react';

import App13component89 from './app13component89';

describe('App13component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component89 />);
    expect(baseElement).toBeTruthy();
  });
});
