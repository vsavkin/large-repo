import { render } from '@testing-library/react';

import App13component96 from './app13component96';

describe('App13component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component96 />);
    expect(baseElement).toBeTruthy();
  });
});
