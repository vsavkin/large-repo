import { render } from '@testing-library/react';

import App13component98 from './app13component98';

describe('App13component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component98 />);
    expect(baseElement).toBeTruthy();
  });
});
