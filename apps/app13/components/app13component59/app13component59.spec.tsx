import { render } from '@testing-library/react';

import App13component59 from './app13component59';

describe('App13component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component59 />);
    expect(baseElement).toBeTruthy();
  });
});
