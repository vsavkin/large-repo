import { render } from '@testing-library/react';

import App13component63 from './app13component63';

describe('App13component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component63 />);
    expect(baseElement).toBeTruthy();
  });
});
