import { render } from '@testing-library/react';

import App13component35 from './app13component35';

describe('App13component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component35 />);
    expect(baseElement).toBeTruthy();
  });
});
