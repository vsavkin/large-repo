import { render } from '@testing-library/react';

import App13component46 from './app13component46';

describe('App13component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App13component46 />);
    expect(baseElement).toBeTruthy();
  });
});
