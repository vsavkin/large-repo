import { render } from '@testing-library/react';

import App4component69 from './app4component69';

describe('App4component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component69 />);
    expect(baseElement).toBeTruthy();
  });
});
