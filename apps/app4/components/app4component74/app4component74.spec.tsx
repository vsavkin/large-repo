import { render } from '@testing-library/react';

import App4component74 from './app4component74';

describe('App4component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component74 />);
    expect(baseElement).toBeTruthy();
  });
});
