import { render } from '@testing-library/react';

import App4component52 from './app4component52';

describe('App4component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component52 />);
    expect(baseElement).toBeTruthy();
  });
});
