import { render } from '@testing-library/react';

import App4component36 from './app4component36';

describe('App4component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component36 />);
    expect(baseElement).toBeTruthy();
  });
});
