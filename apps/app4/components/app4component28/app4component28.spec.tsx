import { render } from '@testing-library/react';

import App4component28 from './app4component28';

describe('App4component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component28 />);
    expect(baseElement).toBeTruthy();
  });
});
