import { render } from '@testing-library/react';

import App4component38 from './app4component38';

describe('App4component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component38 />);
    expect(baseElement).toBeTruthy();
  });
});
