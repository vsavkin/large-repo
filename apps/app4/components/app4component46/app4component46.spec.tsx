import { render } from '@testing-library/react';

import App4component46 from './app4component46';

describe('App4component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component46 />);
    expect(baseElement).toBeTruthy();
  });
});
