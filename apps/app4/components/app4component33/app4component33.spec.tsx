import { render } from '@testing-library/react';

import App4component33 from './app4component33';

describe('App4component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component33 />);
    expect(baseElement).toBeTruthy();
  });
});
