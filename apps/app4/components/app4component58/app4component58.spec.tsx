import { render } from '@testing-library/react';

import App4component58 from './app4component58';

describe('App4component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component58 />);
    expect(baseElement).toBeTruthy();
  });
});
