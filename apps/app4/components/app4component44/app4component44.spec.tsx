import { render } from '@testing-library/react';

import App4component44 from './app4component44';

describe('App4component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component44 />);
    expect(baseElement).toBeTruthy();
  });
});
