import { render } from '@testing-library/react';

import App4component93 from './app4component93';

describe('App4component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component93 />);
    expect(baseElement).toBeTruthy();
  });
});
