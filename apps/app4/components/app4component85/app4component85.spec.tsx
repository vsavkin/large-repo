import { render } from '@testing-library/react';

import App4component85 from './app4component85';

describe('App4component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component85 />);
    expect(baseElement).toBeTruthy();
  });
});
