import { render } from '@testing-library/react';

import App4component79 from './app4component79';

describe('App4component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component79 />);
    expect(baseElement).toBeTruthy();
  });
});
