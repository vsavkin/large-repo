import { render } from '@testing-library/react';

import App4component63 from './app4component63';

describe('App4component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component63 />);
    expect(baseElement).toBeTruthy();
  });
});
