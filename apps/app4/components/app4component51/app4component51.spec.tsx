import { render } from '@testing-library/react';

import App4component51 from './app4component51';

describe('App4component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component51 />);
    expect(baseElement).toBeTruthy();
  });
});
