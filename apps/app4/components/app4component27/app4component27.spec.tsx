import { render } from '@testing-library/react';

import App4component27 from './app4component27';

describe('App4component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component27 />);
    expect(baseElement).toBeTruthy();
  });
});
