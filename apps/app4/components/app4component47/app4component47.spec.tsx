import { render } from '@testing-library/react';

import App4component47 from './app4component47';

describe('App4component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component47 />);
    expect(baseElement).toBeTruthy();
  });
});
