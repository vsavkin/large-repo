import { render } from '@testing-library/react';

import App4component67 from './app4component67';

describe('App4component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component67 />);
    expect(baseElement).toBeTruthy();
  });
});
