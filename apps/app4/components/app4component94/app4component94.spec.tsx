import { render } from '@testing-library/react';

import App4component94 from './app4component94';

describe('App4component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component94 />);
    expect(baseElement).toBeTruthy();
  });
});
