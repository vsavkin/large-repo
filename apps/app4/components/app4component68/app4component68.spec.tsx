import { render } from '@testing-library/react';

import App4component68 from './app4component68';

describe('App4component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component68 />);
    expect(baseElement).toBeTruthy();
  });
});
