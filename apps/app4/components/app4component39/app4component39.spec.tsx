import { render } from '@testing-library/react';

import App4component39 from './app4component39';

describe('App4component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component39 />);
    expect(baseElement).toBeTruthy();
  });
});
