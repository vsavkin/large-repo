import { render } from '@testing-library/react';

import App4component56 from './app4component56';

describe('App4component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component56 />);
    expect(baseElement).toBeTruthy();
  });
});
