import { render } from '@testing-library/react';

import App4component34 from './app4component34';

describe('App4component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component34 />);
    expect(baseElement).toBeTruthy();
  });
});
