import { render } from '@testing-library/react';

import App4component73 from './app4component73';

describe('App4component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component73 />);
    expect(baseElement).toBeTruthy();
  });
});
