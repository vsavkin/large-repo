import { render } from '@testing-library/react';

import App4component62 from './app4component62';

describe('App4component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component62 />);
    expect(baseElement).toBeTruthy();
  });
});
