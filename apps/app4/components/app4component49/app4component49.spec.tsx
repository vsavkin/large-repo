import { render } from '@testing-library/react';

import App4component49 from './app4component49';

describe('App4component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component49 />);
    expect(baseElement).toBeTruthy();
  });
});
