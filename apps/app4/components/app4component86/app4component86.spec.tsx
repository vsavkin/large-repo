import { render } from '@testing-library/react';

import App4component86 from './app4component86';

describe('App4component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component86 />);
    expect(baseElement).toBeTruthy();
  });
});
