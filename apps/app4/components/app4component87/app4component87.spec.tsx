import { render } from '@testing-library/react';

import App4component87 from './app4component87';

describe('App4component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component87 />);
    expect(baseElement).toBeTruthy();
  });
});
