import { render } from '@testing-library/react';

import App4component37 from './app4component37';

describe('App4component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component37 />);
    expect(baseElement).toBeTruthy();
  });
});
