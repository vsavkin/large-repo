import { render } from '@testing-library/react';

import App4component57 from './app4component57';

describe('App4component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component57 />);
    expect(baseElement).toBeTruthy();
  });
});
