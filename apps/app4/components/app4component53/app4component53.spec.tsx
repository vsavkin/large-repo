import { render } from '@testing-library/react';

import App4component53 from './app4component53';

describe('App4component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component53 />);
    expect(baseElement).toBeTruthy();
  });
});
