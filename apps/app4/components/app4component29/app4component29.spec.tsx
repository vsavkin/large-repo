import { render } from '@testing-library/react';

import App4component29 from './app4component29';

describe('App4component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component29 />);
    expect(baseElement).toBeTruthy();
  });
});
