import { render } from '@testing-library/react';

import App4component35 from './app4component35';

describe('App4component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component35 />);
    expect(baseElement).toBeTruthy();
  });
});
