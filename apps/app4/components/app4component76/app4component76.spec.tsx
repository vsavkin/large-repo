import { render } from '@testing-library/react';

import App4component76 from './app4component76';

describe('App4component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component76 />);
    expect(baseElement).toBeTruthy();
  });
});
