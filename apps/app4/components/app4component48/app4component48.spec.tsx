import { render } from '@testing-library/react';

import App4component48 from './app4component48';

describe('App4component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component48 />);
    expect(baseElement).toBeTruthy();
  });
});
