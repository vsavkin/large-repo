import { render } from '@testing-library/react';

import App4component55 from './app4component55';

describe('App4component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component55 />);
    expect(baseElement).toBeTruthy();
  });
});
