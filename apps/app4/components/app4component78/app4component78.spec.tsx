import { render } from '@testing-library/react';

import App4component78 from './app4component78';

describe('App4component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component78 />);
    expect(baseElement).toBeTruthy();
  });
});
