import { render } from '@testing-library/react';

import App4component97 from './app4component97';

describe('App4component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component97 />);
    expect(baseElement).toBeTruthy();
  });
});
