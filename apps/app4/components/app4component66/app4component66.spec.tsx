import { render } from '@testing-library/react';

import App4component66 from './app4component66';

describe('App4component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component66 />);
    expect(baseElement).toBeTruthy();
  });
});
