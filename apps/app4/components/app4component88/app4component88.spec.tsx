import { render } from '@testing-library/react';

import App4component88 from './app4component88';

describe('App4component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App4component88 />);
    expect(baseElement).toBeTruthy();
  });
});
