import { render } from '@testing-library/react';

import App5component46 from './app5component46';

describe('App5component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component46 />);
    expect(baseElement).toBeTruthy();
  });
});
