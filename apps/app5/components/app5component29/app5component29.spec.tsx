import { render } from '@testing-library/react';

import App5component29 from './app5component29';

describe('App5component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component29 />);
    expect(baseElement).toBeTruthy();
  });
});
