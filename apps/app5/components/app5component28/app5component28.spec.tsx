import { render } from '@testing-library/react';

import App5component28 from './app5component28';

describe('App5component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component28 />);
    expect(baseElement).toBeTruthy();
  });
});
