import { render } from '@testing-library/react';

import App5component57 from './app5component57';

describe('App5component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component57 />);
    expect(baseElement).toBeTruthy();
  });
});
