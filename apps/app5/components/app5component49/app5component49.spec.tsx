import { render } from '@testing-library/react';

import App5component49 from './app5component49';

describe('App5component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component49 />);
    expect(baseElement).toBeTruthy();
  });
});
