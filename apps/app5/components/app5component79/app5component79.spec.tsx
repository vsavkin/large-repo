import { render } from '@testing-library/react';

import App5component79 from './app5component79';

describe('App5component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component79 />);
    expect(baseElement).toBeTruthy();
  });
});
