import { render } from '@testing-library/react';

import App5component63 from './app5component63';

describe('App5component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component63 />);
    expect(baseElement).toBeTruthy();
  });
});
