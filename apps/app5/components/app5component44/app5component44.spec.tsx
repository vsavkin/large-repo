import { render } from '@testing-library/react';

import App5component44 from './app5component44';

describe('App5component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component44 />);
    expect(baseElement).toBeTruthy();
  });
});
