import { render } from '@testing-library/react';

import App5component88 from './app5component88';

describe('App5component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component88 />);
    expect(baseElement).toBeTruthy();
  });
});
