import { render } from '@testing-library/react';

import App5component35 from './app5component35';

describe('App5component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component35 />);
    expect(baseElement).toBeTruthy();
  });
});
