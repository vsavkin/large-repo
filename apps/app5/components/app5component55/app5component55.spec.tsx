import { render } from '@testing-library/react';

import App5component55 from './app5component55';

describe('App5component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component55 />);
    expect(baseElement).toBeTruthy();
  });
});
