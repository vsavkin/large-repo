import { render } from '@testing-library/react';

import App5component66 from './app5component66';

describe('App5component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component66 />);
    expect(baseElement).toBeTruthy();
  });
});
