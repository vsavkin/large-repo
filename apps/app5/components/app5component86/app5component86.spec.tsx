import { render } from '@testing-library/react';

import App5component86 from './app5component86';

describe('App5component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component86 />);
    expect(baseElement).toBeTruthy();
  });
});
