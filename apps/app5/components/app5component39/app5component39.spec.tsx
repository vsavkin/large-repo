import { render } from '@testing-library/react';

import App5component39 from './app5component39';

describe('App5component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component39 />);
    expect(baseElement).toBeTruthy();
  });
});
