import { render } from '@testing-library/react';

import App5component73 from './app5component73';

describe('App5component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component73 />);
    expect(baseElement).toBeTruthy();
  });
});
