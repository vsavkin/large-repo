import { render } from '@testing-library/react';

import App5component93 from './app5component93';

describe('App5component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component93 />);
    expect(baseElement).toBeTruthy();
  });
});
