import { render } from '@testing-library/react';

import App5component94 from './app5component94';

describe('App5component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component94 />);
    expect(baseElement).toBeTruthy();
  });
});
