import { render } from '@testing-library/react';

import App5component58 from './app5component58';

describe('App5component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component58 />);
    expect(baseElement).toBeTruthy();
  });
});
