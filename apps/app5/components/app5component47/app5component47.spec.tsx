import { render } from '@testing-library/react';

import App5component47 from './app5component47';

describe('App5component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component47 />);
    expect(baseElement).toBeTruthy();
  });
});
