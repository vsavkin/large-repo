import { render } from '@testing-library/react';

import App5component87 from './app5component87';

describe('App5component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component87 />);
    expect(baseElement).toBeTruthy();
  });
});
