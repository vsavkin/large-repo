import { render } from '@testing-library/react';

import App5component96 from './app5component96';

describe('App5component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component96 />);
    expect(baseElement).toBeTruthy();
  });
});
