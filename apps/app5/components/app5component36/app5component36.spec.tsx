import { render } from '@testing-library/react';

import App5component36 from './app5component36';

describe('App5component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component36 />);
    expect(baseElement).toBeTruthy();
  });
});
