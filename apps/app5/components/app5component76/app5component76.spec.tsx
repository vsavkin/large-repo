import { render } from '@testing-library/react';

import App5component76 from './app5component76';

describe('App5component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component76 />);
    expect(baseElement).toBeTruthy();
  });
});
