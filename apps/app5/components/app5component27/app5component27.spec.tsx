import { render } from '@testing-library/react';

import App5component27 from './app5component27';

describe('App5component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component27 />);
    expect(baseElement).toBeTruthy();
  });
});
