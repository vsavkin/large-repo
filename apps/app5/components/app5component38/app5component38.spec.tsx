import { render } from '@testing-library/react';

import App5component38 from './app5component38';

describe('App5component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component38 />);
    expect(baseElement).toBeTruthy();
  });
});
