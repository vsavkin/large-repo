import { render } from '@testing-library/react';

import App5component33 from './app5component33';

describe('App5component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component33 />);
    expect(baseElement).toBeTruthy();
  });
});
