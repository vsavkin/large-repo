import { render } from '@testing-library/react';

import App5component69 from './app5component69';

describe('App5component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component69 />);
    expect(baseElement).toBeTruthy();
  });
});
