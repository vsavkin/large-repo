import { render } from '@testing-library/react';

import App5component54 from './app5component54';

describe('App5component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component54 />);
    expect(baseElement).toBeTruthy();
  });
});
