import { render } from '@testing-library/react';

import App5component48 from './app5component48';

describe('App5component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component48 />);
    expect(baseElement).toBeTruthy();
  });
});
