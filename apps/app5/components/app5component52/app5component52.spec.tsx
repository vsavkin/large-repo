import { render } from '@testing-library/react';

import App5component52 from './app5component52';

describe('App5component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component52 />);
    expect(baseElement).toBeTruthy();
  });
});
