import { render } from '@testing-library/react';

import App5component53 from './app5component53';

describe('App5component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component53 />);
    expect(baseElement).toBeTruthy();
  });
});
