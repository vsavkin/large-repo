import { render } from '@testing-library/react';

import App5component67 from './app5component67';

describe('App5component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component67 />);
    expect(baseElement).toBeTruthy();
  });
});
