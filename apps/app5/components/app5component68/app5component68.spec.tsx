import { render } from '@testing-library/react';

import App5component68 from './app5component68';

describe('App5component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component68 />);
    expect(baseElement).toBeTruthy();
  });
});
