import { render } from '@testing-library/react';

import App5component37 from './app5component37';

describe('App5component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component37 />);
    expect(baseElement).toBeTruthy();
  });
});
