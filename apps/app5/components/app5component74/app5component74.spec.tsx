import { render } from '@testing-library/react';

import App5component74 from './app5component74';

describe('App5component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component74 />);
    expect(baseElement).toBeTruthy();
  });
});
