import { render } from '@testing-library/react';

import App5component97 from './app5component97';

describe('App5component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App5component97 />);
    expect(baseElement).toBeTruthy();
  });
});
