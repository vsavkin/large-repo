import { render } from '@testing-library/react';

import App12component72 from './app12component72';

describe('App12component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component72 />);
    expect(baseElement).toBeTruthy();
  });
});
