import { render } from '@testing-library/react';

import App12component82 from './app12component82';

describe('App12component82', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component82 />);
    expect(baseElement).toBeTruthy();
  });
});
