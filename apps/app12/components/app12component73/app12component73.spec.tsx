import { render } from '@testing-library/react';

import App12component73 from './app12component73';

describe('App12component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component73 />);
    expect(baseElement).toBeTruthy();
  });
});
