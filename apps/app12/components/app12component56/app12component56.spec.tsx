import { render } from '@testing-library/react';

import App12component56 from './app12component56';

describe('App12component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component56 />);
    expect(baseElement).toBeTruthy();
  });
});
