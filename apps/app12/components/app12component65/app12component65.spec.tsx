import { render } from '@testing-library/react';

import App12component65 from './app12component65';

describe('App12component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component65 />);
    expect(baseElement).toBeTruthy();
  });
});
