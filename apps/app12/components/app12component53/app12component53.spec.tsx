import { render } from '@testing-library/react';

import App12component53 from './app12component53';

describe('App12component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component53 />);
    expect(baseElement).toBeTruthy();
  });
});
