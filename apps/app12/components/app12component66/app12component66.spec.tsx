import { render } from '@testing-library/react';

import App12component66 from './app12component66';

describe('App12component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component66 />);
    expect(baseElement).toBeTruthy();
  });
});
