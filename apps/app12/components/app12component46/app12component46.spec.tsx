import { render } from '@testing-library/react';

import App12component46 from './app12component46';

describe('App12component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component46 />);
    expect(baseElement).toBeTruthy();
  });
});
