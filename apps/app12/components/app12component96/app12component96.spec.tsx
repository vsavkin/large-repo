import { render } from '@testing-library/react';

import App12component96 from './app12component96';

describe('App12component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component96 />);
    expect(baseElement).toBeTruthy();
  });
});
