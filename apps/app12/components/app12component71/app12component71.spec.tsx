import { render } from '@testing-library/react';

import App12component71 from './app12component71';

describe('App12component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component71 />);
    expect(baseElement).toBeTruthy();
  });
});
