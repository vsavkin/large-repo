import { render } from '@testing-library/react';

import App12component95 from './app12component95';

describe('App12component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component95 />);
    expect(baseElement).toBeTruthy();
  });
});
