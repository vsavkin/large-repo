import { render } from '@testing-library/react';

import App12component48 from './app12component48';

describe('App12component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component48 />);
    expect(baseElement).toBeTruthy();
  });
});
