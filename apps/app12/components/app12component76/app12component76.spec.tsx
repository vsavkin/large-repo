import { render } from '@testing-library/react';

import App12component76 from './app12component76';

describe('App12component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component76 />);
    expect(baseElement).toBeTruthy();
  });
});
