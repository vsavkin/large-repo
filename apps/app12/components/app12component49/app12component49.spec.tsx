import { render } from '@testing-library/react';

import App12component49 from './app12component49';

describe('App12component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component49 />);
    expect(baseElement).toBeTruthy();
  });
});
