import { render } from '@testing-library/react';

import App12component52 from './app12component52';

describe('App12component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component52 />);
    expect(baseElement).toBeTruthy();
  });
});
