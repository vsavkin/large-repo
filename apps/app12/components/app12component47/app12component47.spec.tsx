import { render } from '@testing-library/react';

import App12component47 from './app12component47';

describe('App12component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component47 />);
    expect(baseElement).toBeTruthy();
  });
});
