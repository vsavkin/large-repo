import { render } from '@testing-library/react';

import App12component74 from './app12component74';

describe('App12component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component74 />);
    expect(baseElement).toBeTruthy();
  });
});
