import { render } from '@testing-library/react';

import App12component29 from './app12component29';

describe('App12component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component29 />);
    expect(baseElement).toBeTruthy();
  });
});
