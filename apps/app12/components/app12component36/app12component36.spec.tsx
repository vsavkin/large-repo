import { render } from '@testing-library/react';

import App12component36 from './app12component36';

describe('App12component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component36 />);
    expect(baseElement).toBeTruthy();
  });
});
