import { render } from '@testing-library/react';

import App12component88 from './app12component88';

describe('App12component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component88 />);
    expect(baseElement).toBeTruthy();
  });
});
