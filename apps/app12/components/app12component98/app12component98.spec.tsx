import { render } from '@testing-library/react';

import App12component98 from './app12component98';

describe('App12component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component98 />);
    expect(baseElement).toBeTruthy();
  });
});
