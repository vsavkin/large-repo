import { render } from '@testing-library/react';

import App12component78 from './app12component78';

describe('App12component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component78 />);
    expect(baseElement).toBeTruthy();
  });
});
