import { render } from '@testing-library/react';

import App12component51 from './app12component51';

describe('App12component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component51 />);
    expect(baseElement).toBeTruthy();
  });
});
