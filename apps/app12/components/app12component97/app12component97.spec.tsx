import { render } from '@testing-library/react';

import App12component97 from './app12component97';

describe('App12component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component97 />);
    expect(baseElement).toBeTruthy();
  });
});
