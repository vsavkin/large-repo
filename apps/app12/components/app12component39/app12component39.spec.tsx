import { render } from '@testing-library/react';

import App12component39 from './app12component39';

describe('App12component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component39 />);
    expect(baseElement).toBeTruthy();
  });
});
