import { render } from '@testing-library/react';

import App12component40 from './app12component40';

describe('App12component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component40 />);
    expect(baseElement).toBeTruthy();
  });
});
