import { render } from '@testing-library/react';

import App12component27 from './app12component27';

describe('App12component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component27 />);
    expect(baseElement).toBeTruthy();
  });
});
