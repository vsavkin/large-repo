import { render } from '@testing-library/react';

import App12component14 from './app12component14';

describe('App12component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component14 />);
    expect(baseElement).toBeTruthy();
  });
});
