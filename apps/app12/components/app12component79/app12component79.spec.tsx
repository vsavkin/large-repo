import { render } from '@testing-library/react';

import App12component79 from './app12component79';

describe('App12component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component79 />);
    expect(baseElement).toBeTruthy();
  });
});
