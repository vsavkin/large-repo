import { render } from '@testing-library/react';

import App12component44 from './app12component44';

describe('App12component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component44 />);
    expect(baseElement).toBeTruthy();
  });
});
