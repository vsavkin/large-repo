import { render } from '@testing-library/react';

import App12component60 from './app12component60';

describe('App12component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component60 />);
    expect(baseElement).toBeTruthy();
  });
});
