import { render } from '@testing-library/react';

import App12component86 from './app12component86';

describe('App12component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component86 />);
    expect(baseElement).toBeTruthy();
  });
});
