import { render } from '@testing-library/react';

import App12component57 from './app12component57';

describe('App12component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component57 />);
    expect(baseElement).toBeTruthy();
  });
});
