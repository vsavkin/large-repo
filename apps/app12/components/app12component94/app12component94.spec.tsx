import { render } from '@testing-library/react';

import App12component94 from './app12component94';

describe('App12component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component94 />);
    expect(baseElement).toBeTruthy();
  });
});
