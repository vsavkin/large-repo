import { render } from '@testing-library/react';

import App12component77 from './app12component77';

describe('App12component77', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component77 />);
    expect(baseElement).toBeTruthy();
  });
});
