import { render } from '@testing-library/react';

import App12component62 from './app12component62';

describe('App12component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component62 />);
    expect(baseElement).toBeTruthy();
  });
});
