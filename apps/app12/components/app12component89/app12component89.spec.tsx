import { render } from '@testing-library/react';

import App12component89 from './app12component89';

describe('App12component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component89 />);
    expect(baseElement).toBeTruthy();
  });
});
