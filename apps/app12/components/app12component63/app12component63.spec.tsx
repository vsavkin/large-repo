import { render } from '@testing-library/react';

import App12component63 from './app12component63';

describe('App12component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component63 />);
    expect(baseElement).toBeTruthy();
  });
});
