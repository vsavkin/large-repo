import { render } from '@testing-library/react';

import App12component92 from './app12component92';

describe('App12component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component92 />);
    expect(baseElement).toBeTruthy();
  });
});
