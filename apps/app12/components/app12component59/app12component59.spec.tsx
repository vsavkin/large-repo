import { render } from '@testing-library/react';

import App12component59 from './app12component59';

describe('App12component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component59 />);
    expect(baseElement).toBeTruthy();
  });
});
