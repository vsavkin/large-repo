import { render } from '@testing-library/react';

import App12component33 from './app12component33';

describe('App12component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component33 />);
    expect(baseElement).toBeTruthy();
  });
});
