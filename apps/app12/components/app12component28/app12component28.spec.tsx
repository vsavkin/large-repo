import { render } from '@testing-library/react';

import App12component28 from './app12component28';

describe('App12component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component28 />);
    expect(baseElement).toBeTruthy();
  });
});
