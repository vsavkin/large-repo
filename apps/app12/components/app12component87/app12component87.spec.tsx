import { render } from '@testing-library/react';

import App12component87 from './app12component87';

describe('App12component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component87 />);
    expect(baseElement).toBeTruthy();
  });
});
