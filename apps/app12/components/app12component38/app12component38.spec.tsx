import { render } from '@testing-library/react';

import App12component38 from './app12component38';

describe('App12component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component38 />);
    expect(baseElement).toBeTruthy();
  });
});
