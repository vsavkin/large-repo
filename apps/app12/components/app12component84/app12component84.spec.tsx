import { render } from '@testing-library/react';

import App12component84 from './app12component84';

describe('App12component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component84 />);
    expect(baseElement).toBeTruthy();
  });
});
