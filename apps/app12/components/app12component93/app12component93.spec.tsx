import { render } from '@testing-library/react';

import App12component93 from './app12component93';

describe('App12component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component93 />);
    expect(baseElement).toBeTruthy();
  });
});
