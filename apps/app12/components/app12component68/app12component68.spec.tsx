import { render } from '@testing-library/react';

import App12component68 from './app12component68';

describe('App12component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component68 />);
    expect(baseElement).toBeTruthy();
  });
});
