import { render } from '@testing-library/react';

import App12component35 from './app12component35';

describe('App12component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component35 />);
    expect(baseElement).toBeTruthy();
  });
});
