import { render } from '@testing-library/react';

import App12component67 from './app12component67';

describe('App12component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component67 />);
    expect(baseElement).toBeTruthy();
  });
});
