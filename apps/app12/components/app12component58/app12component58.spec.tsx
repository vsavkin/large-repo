import { render } from '@testing-library/react';

import App12component58 from './app12component58';

describe('App12component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component58 />);
    expect(baseElement).toBeTruthy();
  });
});
