import { render } from '@testing-library/react';

import App12component91 from './app12component91';

describe('App12component91', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component91 />);
    expect(baseElement).toBeTruthy();
  });
});
