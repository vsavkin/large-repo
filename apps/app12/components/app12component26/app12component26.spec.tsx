import { render } from '@testing-library/react';

import App12component26 from './app12component26';

describe('App12component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component26 />);
    expect(baseElement).toBeTruthy();
  });
});
