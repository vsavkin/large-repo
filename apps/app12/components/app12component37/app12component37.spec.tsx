import { render } from '@testing-library/react';

import App12component37 from './app12component37';

describe('App12component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App12component37 />);
    expect(baseElement).toBeTruthy();
  });
});
