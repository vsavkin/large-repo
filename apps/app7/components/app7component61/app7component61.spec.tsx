import { render } from '@testing-library/react';

import App7component61 from './app7component61';

describe('App7component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component61 />);
    expect(baseElement).toBeTruthy();
  });
});
