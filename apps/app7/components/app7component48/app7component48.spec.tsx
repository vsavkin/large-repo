import { render } from '@testing-library/react';

import App7component48 from './app7component48';

describe('App7component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component48 />);
    expect(baseElement).toBeTruthy();
  });
});
