import { render } from '@testing-library/react';

import App7component95 from './app7component95';

describe('App7component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component95 />);
    expect(baseElement).toBeTruthy();
  });
});
