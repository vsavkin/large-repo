import { render } from '@testing-library/react';

import App7component91 from './app7component91';

describe('App7component91', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component91 />);
    expect(baseElement).toBeTruthy();
  });
});
