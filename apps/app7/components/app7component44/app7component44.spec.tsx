import { render } from '@testing-library/react';

import App7component44 from './app7component44';

describe('App7component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component44 />);
    expect(baseElement).toBeTruthy();
  });
});
