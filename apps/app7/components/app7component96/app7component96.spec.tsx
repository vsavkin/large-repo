import { render } from '@testing-library/react';

import App7component96 from './app7component96';

describe('App7component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component96 />);
    expect(baseElement).toBeTruthy();
  });
});
