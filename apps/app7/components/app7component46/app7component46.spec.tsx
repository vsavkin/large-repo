import { render } from '@testing-library/react';

import App7component46 from './app7component46';

describe('App7component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component46 />);
    expect(baseElement).toBeTruthy();
  });
});
