import { render } from '@testing-library/react';

import App7component45 from './app7component45';

describe('App7component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component45 />);
    expect(baseElement).toBeTruthy();
  });
});
