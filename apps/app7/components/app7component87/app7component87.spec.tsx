import { render } from '@testing-library/react';

import App7component87 from './app7component87';

describe('App7component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component87 />);
    expect(baseElement).toBeTruthy();
  });
});
