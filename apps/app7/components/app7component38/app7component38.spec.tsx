import { render } from '@testing-library/react';

import App7component38 from './app7component38';

describe('App7component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component38 />);
    expect(baseElement).toBeTruthy();
  });
});
