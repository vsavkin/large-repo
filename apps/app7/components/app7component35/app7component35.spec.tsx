import { render } from '@testing-library/react';

import App7component35 from './app7component35';

describe('App7component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component35 />);
    expect(baseElement).toBeTruthy();
  });
});
