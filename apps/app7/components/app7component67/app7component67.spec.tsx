import { render } from '@testing-library/react';

import App7component67 from './app7component67';

describe('App7component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component67 />);
    expect(baseElement).toBeTruthy();
  });
});
