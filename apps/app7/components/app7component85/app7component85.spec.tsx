import { render } from '@testing-library/react';

import App7component85 from './app7component85';

describe('App7component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component85 />);
    expect(baseElement).toBeTruthy();
  });
});
