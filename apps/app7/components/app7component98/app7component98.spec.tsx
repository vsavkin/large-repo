import { render } from '@testing-library/react';

import App7component98 from './app7component98';

describe('App7component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component98 />);
    expect(baseElement).toBeTruthy();
  });
});
