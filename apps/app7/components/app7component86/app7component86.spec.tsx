import { render } from '@testing-library/react';

import App7component86 from './app7component86';

describe('App7component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component86 />);
    expect(baseElement).toBeTruthy();
  });
});
