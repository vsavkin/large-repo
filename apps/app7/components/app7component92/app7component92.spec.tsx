import { render } from '@testing-library/react';

import App7component92 from './app7component92';

describe('App7component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component92 />);
    expect(baseElement).toBeTruthy();
  });
});
