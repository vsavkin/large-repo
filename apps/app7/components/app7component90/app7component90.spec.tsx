import { render } from '@testing-library/react';

import App7component90 from './app7component90';

describe('App7component90', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component90 />);
    expect(baseElement).toBeTruthy();
  });
});
