import { render } from '@testing-library/react';

import App7component53 from './app7component53';

describe('App7component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component53 />);
    expect(baseElement).toBeTruthy();
  });
});
