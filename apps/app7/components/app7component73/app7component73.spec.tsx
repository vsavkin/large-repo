import { render } from '@testing-library/react';

import App7component73 from './app7component73';

describe('App7component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component73 />);
    expect(baseElement).toBeTruthy();
  });
});
