import { render } from '@testing-library/react';

import App7component31 from './app7component31';

describe('App7component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component31 />);
    expect(baseElement).toBeTruthy();
  });
});
