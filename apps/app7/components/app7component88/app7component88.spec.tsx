import { render } from '@testing-library/react';

import App7component88 from './app7component88';

describe('App7component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component88 />);
    expect(baseElement).toBeTruthy();
  });
});
