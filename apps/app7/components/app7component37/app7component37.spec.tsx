import { render } from '@testing-library/react';

import App7component37 from './app7component37';

describe('App7component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component37 />);
    expect(baseElement).toBeTruthy();
  });
});
