import { render } from '@testing-library/react';

import App7component40 from './app7component40';

describe('App7component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component40 />);
    expect(baseElement).toBeTruthy();
  });
});
