import { render } from '@testing-library/react';

import App7component26 from './app7component26';

describe('App7component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component26 />);
    expect(baseElement).toBeTruthy();
  });
});
