import { render } from '@testing-library/react';

import App7component57 from './app7component57';

describe('App7component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component57 />);
    expect(baseElement).toBeTruthy();
  });
});
