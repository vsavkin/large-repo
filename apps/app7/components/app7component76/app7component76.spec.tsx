import { render } from '@testing-library/react';

import App7component76 from './app7component76';

describe('App7component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component76 />);
    expect(baseElement).toBeTruthy();
  });
});
