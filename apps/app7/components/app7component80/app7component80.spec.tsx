import { render } from '@testing-library/react';

import App7component80 from './app7component80';

describe('App7component80', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component80 />);
    expect(baseElement).toBeTruthy();
  });
});
