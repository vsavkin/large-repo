import { render } from '@testing-library/react';

import App7component83 from './app7component83';

describe('App7component83', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component83 />);
    expect(baseElement).toBeTruthy();
  });
});
