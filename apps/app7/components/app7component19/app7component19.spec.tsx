import { render } from '@testing-library/react';

import App7component19 from './app7component19';

describe('App7component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component19 />);
    expect(baseElement).toBeTruthy();
  });
});
