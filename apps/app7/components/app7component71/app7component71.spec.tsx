import { render } from '@testing-library/react';

import App7component71 from './app7component71';

describe('App7component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component71 />);
    expect(baseElement).toBeTruthy();
  });
});
