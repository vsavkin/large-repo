import { render } from '@testing-library/react';

import App7component30 from './app7component30';

describe('App7component30', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component30 />);
    expect(baseElement).toBeTruthy();
  });
});
