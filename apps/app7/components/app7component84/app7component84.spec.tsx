import { render } from '@testing-library/react';

import App7component84 from './app7component84';

describe('App7component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component84 />);
    expect(baseElement).toBeTruthy();
  });
});
