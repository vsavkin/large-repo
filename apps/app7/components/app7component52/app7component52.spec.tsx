import { render } from '@testing-library/react';

import App7component52 from './app7component52';

describe('App7component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component52 />);
    expect(baseElement).toBeTruthy();
  });
});
