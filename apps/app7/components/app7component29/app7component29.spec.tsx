import { render } from '@testing-library/react';

import App7component29 from './app7component29';

describe('App7component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component29 />);
    expect(baseElement).toBeTruthy();
  });
});
