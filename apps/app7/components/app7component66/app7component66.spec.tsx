import { render } from '@testing-library/react';

import App7component66 from './app7component66';

describe('App7component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component66 />);
    expect(baseElement).toBeTruthy();
  });
});
