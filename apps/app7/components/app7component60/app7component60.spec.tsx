import { render } from '@testing-library/react';

import App7component60 from './app7component60';

describe('App7component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component60 />);
    expect(baseElement).toBeTruthy();
  });
});
