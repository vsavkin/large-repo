import { render } from '@testing-library/react';

import App7component27 from './app7component27';

describe('App7component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component27 />);
    expect(baseElement).toBeTruthy();
  });
});
