import { render } from '@testing-library/react';

import App7component28 from './app7component28';

describe('App7component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component28 />);
    expect(baseElement).toBeTruthy();
  });
});
