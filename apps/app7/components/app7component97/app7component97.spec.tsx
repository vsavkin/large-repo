import { render } from '@testing-library/react';

import App7component97 from './app7component97';

describe('App7component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component97 />);
    expect(baseElement).toBeTruthy();
  });
});
