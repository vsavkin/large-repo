import { render } from '@testing-library/react';

import App7component94 from './app7component94';

describe('App7component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component94 />);
    expect(baseElement).toBeTruthy();
  });
});
