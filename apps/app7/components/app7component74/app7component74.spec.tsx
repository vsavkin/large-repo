import { render } from '@testing-library/react';

import App7component74 from './app7component74';

describe('App7component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component74 />);
    expect(baseElement).toBeTruthy();
  });
});
