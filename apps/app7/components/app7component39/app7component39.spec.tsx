import { render } from '@testing-library/react';

import App7component39 from './app7component39';

describe('App7component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component39 />);
    expect(baseElement).toBeTruthy();
  });
});
