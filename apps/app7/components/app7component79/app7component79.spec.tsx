import { render } from '@testing-library/react';

import App7component79 from './app7component79';

describe('App7component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component79 />);
    expect(baseElement).toBeTruthy();
  });
});
