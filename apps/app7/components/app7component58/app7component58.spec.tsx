import { render } from '@testing-library/react';

import App7component58 from './app7component58';

describe('App7component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component58 />);
    expect(baseElement).toBeTruthy();
  });
});
