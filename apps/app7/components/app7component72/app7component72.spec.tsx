import { render } from '@testing-library/react';

import App7component72 from './app7component72';

describe('App7component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component72 />);
    expect(baseElement).toBeTruthy();
  });
});
