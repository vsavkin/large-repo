import { render } from '@testing-library/react';

import App7component62 from './app7component62';

describe('App7component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component62 />);
    expect(baseElement).toBeTruthy();
  });
});
