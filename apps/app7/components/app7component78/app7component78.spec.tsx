import { render } from '@testing-library/react';

import App7component78 from './app7component78';

describe('App7component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component78 />);
    expect(baseElement).toBeTruthy();
  });
});
