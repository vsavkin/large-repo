import { render } from '@testing-library/react';

import App7component15 from './app7component15';

describe('App7component15', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component15 />);
    expect(baseElement).toBeTruthy();
  });
});
