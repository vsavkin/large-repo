import { render } from '@testing-library/react';

import App7component0 from './app7component0';

describe('App7component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component0 />);
    expect(baseElement).toBeTruthy();
  });
});
