import { render } from '@testing-library/react';

import App7component47 from './app7component47';

describe('App7component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component47 />);
    expect(baseElement).toBeTruthy();
  });
});
