import { render } from '@testing-library/react';

import App7component33 from './app7component33';

describe('App7component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component33 />);
    expect(baseElement).toBeTruthy();
  });
});
