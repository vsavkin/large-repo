import { render } from '@testing-library/react';

import App7component36 from './app7component36';

describe('App7component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component36 />);
    expect(baseElement).toBeTruthy();
  });
});
