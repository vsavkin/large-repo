import { render } from '@testing-library/react';

import App7component51 from './app7component51';

describe('App7component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component51 />);
    expect(baseElement).toBeTruthy();
  });
});
