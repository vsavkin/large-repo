import { render } from '@testing-library/react';

import App7component68 from './app7component68';

describe('App7component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component68 />);
    expect(baseElement).toBeTruthy();
  });
});
