import { render } from '@testing-library/react';

import App7component63 from './app7component63';

describe('App7component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component63 />);
    expect(baseElement).toBeTruthy();
  });
});
