import { render } from '@testing-library/react';

import App7component17 from './app7component17';

describe('App7component17', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component17 />);
    expect(baseElement).toBeTruthy();
  });
});
