import { render } from '@testing-library/react';

import App7component65 from './app7component65';

describe('App7component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component65 />);
    expect(baseElement).toBeTruthy();
  });
});
