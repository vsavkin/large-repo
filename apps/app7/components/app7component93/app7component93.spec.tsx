import { render } from '@testing-library/react';

import App7component93 from './app7component93';

describe('App7component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App7component93 />);
    expect(baseElement).toBeTruthy();
  });
});
