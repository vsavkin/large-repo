import { render } from '@testing-library/react';

import App11component84 from './app11component84';

describe('App11component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component84 />);
    expect(baseElement).toBeTruthy();
  });
});
