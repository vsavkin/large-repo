import { render } from '@testing-library/react';

import App11component86 from './app11component86';

describe('App11component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component86 />);
    expect(baseElement).toBeTruthy();
  });
});
