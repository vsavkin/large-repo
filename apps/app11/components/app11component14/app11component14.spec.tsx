import { render } from '@testing-library/react';

import App11component14 from './app11component14';

describe('App11component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component14 />);
    expect(baseElement).toBeTruthy();
  });
});
