import { render } from '@testing-library/react';

import App11component36 from './app11component36';

describe('App11component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component36 />);
    expect(baseElement).toBeTruthy();
  });
});
