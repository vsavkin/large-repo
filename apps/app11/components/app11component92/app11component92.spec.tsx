import { render } from '@testing-library/react';

import App11component92 from './app11component92';

describe('App11component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component92 />);
    expect(baseElement).toBeTruthy();
  });
});
