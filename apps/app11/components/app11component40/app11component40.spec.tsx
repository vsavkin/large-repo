import { render } from '@testing-library/react';

import App11component40 from './app11component40';

describe('App11component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component40 />);
    expect(baseElement).toBeTruthy();
  });
});
