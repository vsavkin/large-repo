import { render } from '@testing-library/react';

import App11component68 from './app11component68';

describe('App11component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component68 />);
    expect(baseElement).toBeTruthy();
  });
});
