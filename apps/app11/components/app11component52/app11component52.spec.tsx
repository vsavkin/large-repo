import { render } from '@testing-library/react';

import App11component52 from './app11component52';

describe('App11component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component52 />);
    expect(baseElement).toBeTruthy();
  });
});
