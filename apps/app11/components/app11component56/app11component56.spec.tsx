import { render } from '@testing-library/react';

import App11component56 from './app11component56';

describe('App11component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component56 />);
    expect(baseElement).toBeTruthy();
  });
});
