import { render } from '@testing-library/react';

import App11component66 from './app11component66';

describe('App11component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component66 />);
    expect(baseElement).toBeTruthy();
  });
});
