import { render } from '@testing-library/react';

import App11component98 from './app11component98';

describe('App11component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component98 />);
    expect(baseElement).toBeTruthy();
  });
});
