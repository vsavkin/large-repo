import { render } from '@testing-library/react';

import App11component28 from './app11component28';

describe('App11component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component28 />);
    expect(baseElement).toBeTruthy();
  });
});
