import { render } from '@testing-library/react';

import App11component88 from './app11component88';

describe('App11component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component88 />);
    expect(baseElement).toBeTruthy();
  });
});
