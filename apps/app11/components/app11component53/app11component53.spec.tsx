import { render } from '@testing-library/react';

import App11component53 from './app11component53';

describe('App11component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component53 />);
    expect(baseElement).toBeTruthy();
  });
});
