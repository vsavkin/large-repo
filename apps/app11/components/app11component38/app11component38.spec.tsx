import { render } from '@testing-library/react';

import App11component38 from './app11component38';

describe('App11component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component38 />);
    expect(baseElement).toBeTruthy();
  });
});
