import { render } from '@testing-library/react';

import App11component73 from './app11component73';

describe('App11component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component73 />);
    expect(baseElement).toBeTruthy();
  });
});
