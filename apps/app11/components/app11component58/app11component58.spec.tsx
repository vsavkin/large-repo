import { render } from '@testing-library/react';

import App11component58 from './app11component58';

describe('App11component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component58 />);
    expect(baseElement).toBeTruthy();
  });
});
