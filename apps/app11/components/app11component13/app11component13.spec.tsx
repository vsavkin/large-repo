import { render } from '@testing-library/react';

import App11component13 from './app11component13';

describe('App11component13', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component13 />);
    expect(baseElement).toBeTruthy();
  });
});
