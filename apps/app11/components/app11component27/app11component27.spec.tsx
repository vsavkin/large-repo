import { render } from '@testing-library/react';

import App11component27 from './app11component27';

describe('App11component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component27 />);
    expect(baseElement).toBeTruthy();
  });
});
