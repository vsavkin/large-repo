import { render } from '@testing-library/react';

import App11component97 from './app11component97';

describe('App11component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component97 />);
    expect(baseElement).toBeTruthy();
  });
});
