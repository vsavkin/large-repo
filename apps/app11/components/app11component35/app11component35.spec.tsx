import { render } from '@testing-library/react';

import App11component35 from './app11component35';

describe('App11component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component35 />);
    expect(baseElement).toBeTruthy();
  });
});
