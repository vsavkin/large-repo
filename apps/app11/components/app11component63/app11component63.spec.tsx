import { render } from '@testing-library/react';

import App11component63 from './app11component63';

describe('App11component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component63 />);
    expect(baseElement).toBeTruthy();
  });
});
