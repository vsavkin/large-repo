import { render } from '@testing-library/react';

import App11component46 from './app11component46';

describe('App11component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component46 />);
    expect(baseElement).toBeTruthy();
  });
});
