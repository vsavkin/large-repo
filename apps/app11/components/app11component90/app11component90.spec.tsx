import { render } from '@testing-library/react';

import App11component90 from './app11component90';

describe('App11component90', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component90 />);
    expect(baseElement).toBeTruthy();
  });
});
