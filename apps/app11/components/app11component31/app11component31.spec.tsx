import { render } from '@testing-library/react';

import App11component31 from './app11component31';

describe('App11component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component31 />);
    expect(baseElement).toBeTruthy();
  });
});
