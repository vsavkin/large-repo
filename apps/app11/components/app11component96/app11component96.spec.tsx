import { render } from '@testing-library/react';

import App11component96 from './app11component96';

describe('App11component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component96 />);
    expect(baseElement).toBeTruthy();
  });
});
