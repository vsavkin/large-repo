import { render } from '@testing-library/react';

import App11component47 from './app11component47';

describe('App11component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component47 />);
    expect(baseElement).toBeTruthy();
  });
});
