import { render } from '@testing-library/react';

import App11component39 from './app11component39';

describe('App11component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component39 />);
    expect(baseElement).toBeTruthy();
  });
});
