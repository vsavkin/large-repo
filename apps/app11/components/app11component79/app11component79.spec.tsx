import { render } from '@testing-library/react';

import App11component79 from './app11component79';

describe('App11component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component79 />);
    expect(baseElement).toBeTruthy();
  });
});
