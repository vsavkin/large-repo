import { render } from '@testing-library/react';

import App11component44 from './app11component44';

describe('App11component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component44 />);
    expect(baseElement).toBeTruthy();
  });
});
