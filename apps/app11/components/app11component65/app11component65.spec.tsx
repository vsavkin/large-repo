import { render } from '@testing-library/react';

import App11component65 from './app11component65';

describe('App11component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component65 />);
    expect(baseElement).toBeTruthy();
  });
});
