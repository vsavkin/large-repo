import { render } from '@testing-library/react';

import App11component95 from './app11component95';

describe('App11component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component95 />);
    expect(baseElement).toBeTruthy();
  });
});
