import { render } from '@testing-library/react';

import App11component74 from './app11component74';

describe('App11component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component74 />);
    expect(baseElement).toBeTruthy();
  });
});
