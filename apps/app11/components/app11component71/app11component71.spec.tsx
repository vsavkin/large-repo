import { render } from '@testing-library/react';

import App11component71 from './app11component71';

describe('App11component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component71 />);
    expect(baseElement).toBeTruthy();
  });
});
