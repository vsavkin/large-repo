import { render } from '@testing-library/react';

import App11component60 from './app11component60';

describe('App11component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component60 />);
    expect(baseElement).toBeTruthy();
  });
});
