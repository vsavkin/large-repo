import { render } from '@testing-library/react';

import App11component67 from './app11component67';

describe('App11component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component67 />);
    expect(baseElement).toBeTruthy();
  });
});
