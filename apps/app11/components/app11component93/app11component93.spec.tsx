import { render } from '@testing-library/react';

import App11component93 from './app11component93';

describe('App11component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component93 />);
    expect(baseElement).toBeTruthy();
  });
});
