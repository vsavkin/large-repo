import { render } from '@testing-library/react';

import App11component33 from './app11component33';

describe('App11component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App11component33 />);
    expect(baseElement).toBeTruthy();
  });
});
