import { render } from '@testing-library/react';

import App16component37 from './app16component37';

describe('App16component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component37 />);
    expect(baseElement).toBeTruthy();
  });
});
