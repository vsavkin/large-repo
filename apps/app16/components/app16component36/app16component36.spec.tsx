import { render } from '@testing-library/react';

import App16component36 from './app16component36';

describe('App16component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component36 />);
    expect(baseElement).toBeTruthy();
  });
});
