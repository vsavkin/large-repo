import { render } from '@testing-library/react';

import App16component31 from './app16component31';

describe('App16component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component31 />);
    expect(baseElement).toBeTruthy();
  });
});
