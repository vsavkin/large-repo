import { render } from '@testing-library/react';

import App16component28 from './app16component28';

describe('App16component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component28 />);
    expect(baseElement).toBeTruthy();
  });
});
