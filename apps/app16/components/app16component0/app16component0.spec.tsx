import { render } from '@testing-library/react';

import App16component0 from './app16component0';

describe('App16component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component0 />);
    expect(baseElement).toBeTruthy();
  });
});
