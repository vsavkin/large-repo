import { render } from '@testing-library/react';

import App16component45 from './app16component45';

describe('App16component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component45 />);
    expect(baseElement).toBeTruthy();
  });
});
