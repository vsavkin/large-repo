import { render } from '@testing-library/react';

import App16component71 from './app16component71';

describe('App16component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component71 />);
    expect(baseElement).toBeTruthy();
  });
});
