import { render } from '@testing-library/react';

import App16component47 from './app16component47';

describe('App16component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component47 />);
    expect(baseElement).toBeTruthy();
  });
});
