import { render } from '@testing-library/react';

import App16component65 from './app16component65';

describe('App16component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component65 />);
    expect(baseElement).toBeTruthy();
  });
});
