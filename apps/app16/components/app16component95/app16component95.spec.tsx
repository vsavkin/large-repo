import { render } from '@testing-library/react';

import App16component95 from './app16component95';

describe('App16component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component95 />);
    expect(baseElement).toBeTruthy();
  });
});
