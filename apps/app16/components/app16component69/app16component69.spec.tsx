import { render } from '@testing-library/react';

import App16component69 from './app16component69';

describe('App16component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component69 />);
    expect(baseElement).toBeTruthy();
  });
});
