import { render } from '@testing-library/react';

import App16component87 from './app16component87';

describe('App16component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component87 />);
    expect(baseElement).toBeTruthy();
  });
});
