import { render } from '@testing-library/react';

import App16component89 from './app16component89';

describe('App16component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component89 />);
    expect(baseElement).toBeTruthy();
  });
});
