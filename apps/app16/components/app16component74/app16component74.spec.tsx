import { render } from '@testing-library/react';

import App16component74 from './app16component74';

describe('App16component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component74 />);
    expect(baseElement).toBeTruthy();
  });
});
