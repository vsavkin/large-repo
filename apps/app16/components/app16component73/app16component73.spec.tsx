import { render } from '@testing-library/react';

import App16component73 from './app16component73';

describe('App16component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component73 />);
    expect(baseElement).toBeTruthy();
  });
});
