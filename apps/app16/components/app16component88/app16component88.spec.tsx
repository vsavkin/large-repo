import { render } from '@testing-library/react';

import App16component88 from './app16component88';

describe('App16component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component88 />);
    expect(baseElement).toBeTruthy();
  });
});
