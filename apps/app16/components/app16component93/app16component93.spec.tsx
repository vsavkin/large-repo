import { render } from '@testing-library/react';

import App16component93 from './app16component93';

describe('App16component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component93 />);
    expect(baseElement).toBeTruthy();
  });
});
