import { render } from '@testing-library/react';

import App16component34 from './app16component34';

describe('App16component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component34 />);
    expect(baseElement).toBeTruthy();
  });
});
