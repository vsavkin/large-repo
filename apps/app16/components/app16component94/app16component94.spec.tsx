import { render } from '@testing-library/react';

import App16component94 from './app16component94';

describe('App16component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component94 />);
    expect(baseElement).toBeTruthy();
  });
});
