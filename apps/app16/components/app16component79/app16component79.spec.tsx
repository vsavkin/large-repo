import { render } from '@testing-library/react';

import App16component79 from './app16component79';

describe('App16component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component79 />);
    expect(baseElement).toBeTruthy();
  });
});
