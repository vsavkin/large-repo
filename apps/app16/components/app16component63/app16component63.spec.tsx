import { render } from '@testing-library/react';

import App16component63 from './app16component63';

describe('App16component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component63 />);
    expect(baseElement).toBeTruthy();
  });
});
