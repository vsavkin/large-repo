import { render } from '@testing-library/react';

import App16component78 from './app16component78';

describe('App16component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component78 />);
    expect(baseElement).toBeTruthy();
  });
});
