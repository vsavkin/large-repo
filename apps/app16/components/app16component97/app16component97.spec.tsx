import { render } from '@testing-library/react';

import App16component97 from './app16component97';

describe('App16component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component97 />);
    expect(baseElement).toBeTruthy();
  });
});
