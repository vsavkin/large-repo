import { render } from '@testing-library/react';

import App16component27 from './app16component27';

describe('App16component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component27 />);
    expect(baseElement).toBeTruthy();
  });
});
