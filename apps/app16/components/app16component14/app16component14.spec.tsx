import { render } from '@testing-library/react';

import App16component14 from './app16component14';

describe('App16component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component14 />);
    expect(baseElement).toBeTruthy();
  });
});
