import { render } from '@testing-library/react';

import App16component52 from './app16component52';

describe('App16component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component52 />);
    expect(baseElement).toBeTruthy();
  });
});
