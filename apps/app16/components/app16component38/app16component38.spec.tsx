import { render } from '@testing-library/react';

import App16component38 from './app16component38';

describe('App16component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component38 />);
    expect(baseElement).toBeTruthy();
  });
});
