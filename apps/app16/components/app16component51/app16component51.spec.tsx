import { render } from '@testing-library/react';

import App16component51 from './app16component51';

describe('App16component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component51 />);
    expect(baseElement).toBeTruthy();
  });
});
