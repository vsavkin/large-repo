import { render } from '@testing-library/react';

import App16component46 from './app16component46';

describe('App16component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component46 />);
    expect(baseElement).toBeTruthy();
  });
});
