import { render } from '@testing-library/react';

import App16component66 from './app16component66';

describe('App16component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component66 />);
    expect(baseElement).toBeTruthy();
  });
});
