import { render } from '@testing-library/react';

import App16component53 from './app16component53';

describe('App16component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component53 />);
    expect(baseElement).toBeTruthy();
  });
});
