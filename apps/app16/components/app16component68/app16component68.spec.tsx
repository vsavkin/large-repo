import { render } from '@testing-library/react';

import App16component68 from './app16component68';

describe('App16component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component68 />);
    expect(baseElement).toBeTruthy();
  });
});
