import { render } from '@testing-library/react';

import App16component76 from './app16component76';

describe('App16component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component76 />);
    expect(baseElement).toBeTruthy();
  });
});
