import { render } from '@testing-library/react';

import App16component86 from './app16component86';

describe('App16component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component86 />);
    expect(baseElement).toBeTruthy();
  });
});
