import { render } from '@testing-library/react';

import App16component33 from './app16component33';

describe('App16component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component33 />);
    expect(baseElement).toBeTruthy();
  });
});
