import { render } from '@testing-library/react';

import App16component35 from './app16component35';

describe('App16component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component35 />);
    expect(baseElement).toBeTruthy();
  });
});
