import { render } from '@testing-library/react';

import App16component72 from './app16component72';

describe('App16component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component72 />);
    expect(baseElement).toBeTruthy();
  });
});
