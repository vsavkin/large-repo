import { render } from '@testing-library/react';

import App16component58 from './app16component58';

describe('App16component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component58 />);
    expect(baseElement).toBeTruthy();
  });
});
