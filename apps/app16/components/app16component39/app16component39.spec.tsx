import { render } from '@testing-library/react';

import App16component39 from './app16component39';

describe('App16component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component39 />);
    expect(baseElement).toBeTruthy();
  });
});
