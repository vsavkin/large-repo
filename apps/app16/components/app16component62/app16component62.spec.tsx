import { render } from '@testing-library/react';

import App16component62 from './app16component62';

describe('App16component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component62 />);
    expect(baseElement).toBeTruthy();
  });
});
