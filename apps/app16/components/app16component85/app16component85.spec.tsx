import { render } from '@testing-library/react';

import App16component85 from './app16component85';

describe('App16component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App16component85 />);
    expect(baseElement).toBeTruthy();
  });
});
