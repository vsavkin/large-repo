import { render } from '@testing-library/react';

import App6component69 from './app6component69';

describe('App6component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component69 />);
    expect(baseElement).toBeTruthy();
  });
});
