import { render } from '@testing-library/react';

import App6component14 from './app6component14';

describe('App6component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component14 />);
    expect(baseElement).toBeTruthy();
  });
});
