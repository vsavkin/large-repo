import { render } from '@testing-library/react';

import App6component83 from './app6component83';

describe('App6component83', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component83 />);
    expect(baseElement).toBeTruthy();
  });
});
