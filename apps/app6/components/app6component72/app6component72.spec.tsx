import { render } from '@testing-library/react';

import App6component72 from './app6component72';

describe('App6component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component72 />);
    expect(baseElement).toBeTruthy();
  });
});
