import { render } from '@testing-library/react';

import App6component58 from './app6component58';

describe('App6component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component58 />);
    expect(baseElement).toBeTruthy();
  });
});
