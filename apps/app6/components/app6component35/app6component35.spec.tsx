import { render } from '@testing-library/react';

import App6component35 from './app6component35';

describe('App6component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component35 />);
    expect(baseElement).toBeTruthy();
  });
});
