import { render } from '@testing-library/react';

import App6component88 from './app6component88';

describe('App6component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component88 />);
    expect(baseElement).toBeTruthy();
  });
});
