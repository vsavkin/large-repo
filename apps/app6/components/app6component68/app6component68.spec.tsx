import { render } from '@testing-library/react';

import App6component68 from './app6component68';

describe('App6component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component68 />);
    expect(baseElement).toBeTruthy();
  });
});
