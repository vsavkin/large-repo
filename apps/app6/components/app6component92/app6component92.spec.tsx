import { render } from '@testing-library/react';

import App6component92 from './app6component92';

describe('App6component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component92 />);
    expect(baseElement).toBeTruthy();
  });
});
