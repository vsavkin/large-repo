import { render } from '@testing-library/react';

import App6component87 from './app6component87';

describe('App6component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component87 />);
    expect(baseElement).toBeTruthy();
  });
});
