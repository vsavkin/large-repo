import { render } from '@testing-library/react';

import App6component63 from './app6component63';

describe('App6component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component63 />);
    expect(baseElement).toBeTruthy();
  });
});
