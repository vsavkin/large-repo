import { render } from '@testing-library/react';

import App6component79 from './app6component79';

describe('App6component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component79 />);
    expect(baseElement).toBeTruthy();
  });
});
