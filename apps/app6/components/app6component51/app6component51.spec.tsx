import { render } from '@testing-library/react';

import App6component51 from './app6component51';

describe('App6component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component51 />);
    expect(baseElement).toBeTruthy();
  });
});
