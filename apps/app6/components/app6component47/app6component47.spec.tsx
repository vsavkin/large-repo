import { render } from '@testing-library/react';

import App6component47 from './app6component47';

describe('App6component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component47 />);
    expect(baseElement).toBeTruthy();
  });
});
