import { render } from '@testing-library/react';

import App6component76 from './app6component76';

describe('App6component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component76 />);
    expect(baseElement).toBeTruthy();
  });
});
