import { render } from '@testing-library/react';

import App6component27 from './app6component27';

describe('App6component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component27 />);
    expect(baseElement).toBeTruthy();
  });
});
