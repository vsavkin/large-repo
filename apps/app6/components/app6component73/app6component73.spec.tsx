import { render } from '@testing-library/react';

import App6component73 from './app6component73';

describe('App6component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component73 />);
    expect(baseElement).toBeTruthy();
  });
});
