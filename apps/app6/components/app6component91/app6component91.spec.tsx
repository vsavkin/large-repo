import { render } from '@testing-library/react';

import App6component91 from './app6component91';

describe('App6component91', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component91 />);
    expect(baseElement).toBeTruthy();
  });
});
