import { render } from '@testing-library/react';

import App6component95 from './app6component95';

describe('App6component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component95 />);
    expect(baseElement).toBeTruthy();
  });
});
