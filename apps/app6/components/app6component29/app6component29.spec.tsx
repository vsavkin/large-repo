import { render } from '@testing-library/react';

import App6component29 from './app6component29';

describe('App6component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component29 />);
    expect(baseElement).toBeTruthy();
  });
});
