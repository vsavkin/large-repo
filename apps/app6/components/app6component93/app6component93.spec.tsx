import { render } from '@testing-library/react';

import App6component93 from './app6component93';

describe('App6component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component93 />);
    expect(baseElement).toBeTruthy();
  });
});
