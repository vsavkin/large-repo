import { render } from '@testing-library/react';

import App6component53 from './app6component53';

describe('App6component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component53 />);
    expect(baseElement).toBeTruthy();
  });
});
