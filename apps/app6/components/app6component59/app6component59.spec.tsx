import { render } from '@testing-library/react';

import App6component59 from './app6component59';

describe('App6component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component59 />);
    expect(baseElement).toBeTruthy();
  });
});
