import { render } from '@testing-library/react';

import App6component46 from './app6component46';

describe('App6component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component46 />);
    expect(baseElement).toBeTruthy();
  });
});
