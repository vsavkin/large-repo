import { render } from '@testing-library/react';

import App6component28 from './app6component28';

describe('App6component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component28 />);
    expect(baseElement).toBeTruthy();
  });
});
