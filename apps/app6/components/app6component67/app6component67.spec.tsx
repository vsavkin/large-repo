import { render } from '@testing-library/react';

import App6component67 from './app6component67';

describe('App6component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component67 />);
    expect(baseElement).toBeTruthy();
  });
});
