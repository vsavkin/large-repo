import { render } from '@testing-library/react';

import App6component71 from './app6component71';

describe('App6component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component71 />);
    expect(baseElement).toBeTruthy();
  });
});
