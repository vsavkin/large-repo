import { render } from '@testing-library/react';

import App6component78 from './app6component78';

describe('App6component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component78 />);
    expect(baseElement).toBeTruthy();
  });
});
