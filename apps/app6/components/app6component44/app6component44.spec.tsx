import { render } from '@testing-library/react';

import App6component44 from './app6component44';

describe('App6component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component44 />);
    expect(baseElement).toBeTruthy();
  });
});
