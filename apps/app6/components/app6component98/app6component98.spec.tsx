import { render } from '@testing-library/react';

import App6component98 from './app6component98';

describe('App6component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component98 />);
    expect(baseElement).toBeTruthy();
  });
});
