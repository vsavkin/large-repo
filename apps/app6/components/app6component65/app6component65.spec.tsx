import { render } from '@testing-library/react';

import App6component65 from './app6component65';

describe('App6component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component65 />);
    expect(baseElement).toBeTruthy();
  });
});
