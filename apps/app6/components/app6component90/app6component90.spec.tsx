import { render } from '@testing-library/react';

import App6component90 from './app6component90';

describe('App6component90', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component90 />);
    expect(baseElement).toBeTruthy();
  });
});
