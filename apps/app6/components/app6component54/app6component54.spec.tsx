import { render } from '@testing-library/react';

import App6component54 from './app6component54';

describe('App6component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component54 />);
    expect(baseElement).toBeTruthy();
  });
});
