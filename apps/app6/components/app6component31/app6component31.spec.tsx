import { render } from '@testing-library/react';

import App6component31 from './app6component31';

describe('App6component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component31 />);
    expect(baseElement).toBeTruthy();
  });
});
