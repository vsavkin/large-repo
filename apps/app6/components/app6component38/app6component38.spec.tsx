import { render } from '@testing-library/react';

import App6component38 from './app6component38';

describe('App6component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component38 />);
    expect(baseElement).toBeTruthy();
  });
});
