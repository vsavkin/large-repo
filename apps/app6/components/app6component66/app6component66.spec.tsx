import { render } from '@testing-library/react';

import App6component66 from './app6component66';

describe('App6component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component66 />);
    expect(baseElement).toBeTruthy();
  });
});
