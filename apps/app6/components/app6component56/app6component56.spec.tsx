import { render } from '@testing-library/react';

import App6component56 from './app6component56';

describe('App6component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component56 />);
    expect(baseElement).toBeTruthy();
  });
});
