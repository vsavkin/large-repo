import { render } from '@testing-library/react';

import App6component94 from './app6component94';

describe('App6component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component94 />);
    expect(baseElement).toBeTruthy();
  });
});
