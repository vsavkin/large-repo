import { render } from '@testing-library/react';

import App6component86 from './app6component86';

describe('App6component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component86 />);
    expect(baseElement).toBeTruthy();
  });
});
