import { render } from '@testing-library/react';

import App6component84 from './app6component84';

describe('App6component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component84 />);
    expect(baseElement).toBeTruthy();
  });
});
