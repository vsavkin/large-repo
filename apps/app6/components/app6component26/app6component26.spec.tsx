import { render } from '@testing-library/react';

import App6component26 from './app6component26';

describe('App6component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component26 />);
    expect(baseElement).toBeTruthy();
  });
});
