import { render } from '@testing-library/react';

import App6component49 from './app6component49';

describe('App6component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component49 />);
    expect(baseElement).toBeTruthy();
  });
});
