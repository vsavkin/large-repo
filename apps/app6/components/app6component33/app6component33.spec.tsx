import { render } from '@testing-library/react';

import App6component33 from './app6component33';

describe('App6component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component33 />);
    expect(baseElement).toBeTruthy();
  });
});
