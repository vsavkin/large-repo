import { render } from '@testing-library/react';

import App6component52 from './app6component52';

describe('App6component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component52 />);
    expect(baseElement).toBeTruthy();
  });
});
