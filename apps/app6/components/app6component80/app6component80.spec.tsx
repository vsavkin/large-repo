import { render } from '@testing-library/react';

import App6component80 from './app6component80';

describe('App6component80', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component80 />);
    expect(baseElement).toBeTruthy();
  });
});
