import { render } from '@testing-library/react';

import App6component55 from './app6component55';

describe('App6component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component55 />);
    expect(baseElement).toBeTruthy();
  });
});
