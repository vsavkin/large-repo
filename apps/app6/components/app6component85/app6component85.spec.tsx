import { render } from '@testing-library/react';

import App6component85 from './app6component85';

describe('App6component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component85 />);
    expect(baseElement).toBeTruthy();
  });
});
