import { render } from '@testing-library/react';

import App6component37 from './app6component37';

describe('App6component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component37 />);
    expect(baseElement).toBeTruthy();
  });
});
