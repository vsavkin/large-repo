import { render } from '@testing-library/react';

import App6component19 from './app6component19';

describe('App6component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component19 />);
    expect(baseElement).toBeTruthy();
  });
});
