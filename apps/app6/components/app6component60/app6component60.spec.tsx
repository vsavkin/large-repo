import { render } from '@testing-library/react';

import App6component60 from './app6component60';

describe('App6component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component60 />);
    expect(baseElement).toBeTruthy();
  });
});
