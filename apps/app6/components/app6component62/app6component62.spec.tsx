import { render } from '@testing-library/react';

import App6component62 from './app6component62';

describe('App6component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component62 />);
    expect(baseElement).toBeTruthy();
  });
});
