import { render } from '@testing-library/react';

import App6component34 from './app6component34';

describe('App6component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component34 />);
    expect(baseElement).toBeTruthy();
  });
});
