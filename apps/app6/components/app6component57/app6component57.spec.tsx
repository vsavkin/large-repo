import { render } from '@testing-library/react';

import App6component57 from './app6component57';

describe('App6component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component57 />);
    expect(baseElement).toBeTruthy();
  });
});
