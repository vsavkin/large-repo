import { render } from '@testing-library/react';

import App6component96 from './app6component96';

describe('App6component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component96 />);
    expect(baseElement).toBeTruthy();
  });
});
