import { render } from '@testing-library/react';

import App6component89 from './app6component89';

describe('App6component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component89 />);
    expect(baseElement).toBeTruthy();
  });
});
