import { render } from '@testing-library/react';

import App6component39 from './app6component39';

describe('App6component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component39 />);
    expect(baseElement).toBeTruthy();
  });
});
