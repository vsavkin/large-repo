import { render } from '@testing-library/react';

import App6component97 from './app6component97';

describe('App6component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component97 />);
    expect(baseElement).toBeTruthy();
  });
});
