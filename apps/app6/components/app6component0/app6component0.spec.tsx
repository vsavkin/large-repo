import { render } from '@testing-library/react';

import App6component0 from './app6component0';

describe('App6component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component0 />);
    expect(baseElement).toBeTruthy();
  });
});
