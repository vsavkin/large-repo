import { render } from '@testing-library/react';

import App6component36 from './app6component36';

describe('App6component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component36 />);
    expect(baseElement).toBeTruthy();
  });
});
