import { render } from '@testing-library/react';

import App6component48 from './app6component48';

describe('App6component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component48 />);
    expect(baseElement).toBeTruthy();
  });
});
