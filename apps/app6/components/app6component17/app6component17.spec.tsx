import { render } from '@testing-library/react';

import App6component17 from './app6component17';

describe('App6component17', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component17 />);
    expect(baseElement).toBeTruthy();
  });
});
