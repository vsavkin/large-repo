import { render } from '@testing-library/react';

import App6component45 from './app6component45';

describe('App6component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component45 />);
    expect(baseElement).toBeTruthy();
  });
});
