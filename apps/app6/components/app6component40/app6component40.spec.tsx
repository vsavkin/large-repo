import { render } from '@testing-library/react';

import App6component40 from './app6component40';

describe('App6component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component40 />);
    expect(baseElement).toBeTruthy();
  });
});
