import { render } from '@testing-library/react';

import App6component82 from './app6component82';

describe('App6component82', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component82 />);
    expect(baseElement).toBeTruthy();
  });
});
