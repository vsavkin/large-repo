import { render } from '@testing-library/react';

import App6component74 from './app6component74';

describe('App6component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App6component74 />);
    expect(baseElement).toBeTruthy();
  });
});
