import { render } from '@testing-library/react';

import App17component67 from './app17component67';

describe('App17component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component67 />);
    expect(baseElement).toBeTruthy();
  });
});
