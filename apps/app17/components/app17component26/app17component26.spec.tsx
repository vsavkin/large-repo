import { render } from '@testing-library/react';

import App17component26 from './app17component26';

describe('App17component26', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component26 />);
    expect(baseElement).toBeTruthy();
  });
});
