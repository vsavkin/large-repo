import { render } from '@testing-library/react';

import App17component60 from './app17component60';

describe('App17component60', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component60 />);
    expect(baseElement).toBeTruthy();
  });
});
