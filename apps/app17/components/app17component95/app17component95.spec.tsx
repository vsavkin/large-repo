import { render } from '@testing-library/react';

import App17component95 from './app17component95';

describe('App17component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component95 />);
    expect(baseElement).toBeTruthy();
  });
});
