import { render } from '@testing-library/react';

import App17component63 from './app17component63';

describe('App17component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component63 />);
    expect(baseElement).toBeTruthy();
  });
});
