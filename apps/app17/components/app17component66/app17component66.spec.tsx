import { render } from '@testing-library/react';

import App17component66 from './app17component66';

describe('App17component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component66 />);
    expect(baseElement).toBeTruthy();
  });
});
