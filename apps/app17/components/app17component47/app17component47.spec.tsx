import { render } from '@testing-library/react';

import App17component47 from './app17component47';

describe('App17component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component47 />);
    expect(baseElement).toBeTruthy();
  });
});
