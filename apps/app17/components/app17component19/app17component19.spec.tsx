import { render } from '@testing-library/react';

import App17component19 from './app17component19';

describe('App17component19', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component19 />);
    expect(baseElement).toBeTruthy();
  });
});
