import { render } from '@testing-library/react';

import App17component39 from './app17component39';

describe('App17component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component39 />);
    expect(baseElement).toBeTruthy();
  });
});
