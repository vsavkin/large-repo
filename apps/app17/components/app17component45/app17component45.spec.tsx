import { render } from '@testing-library/react';

import App17component45 from './app17component45';

describe('App17component45', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component45 />);
    expect(baseElement).toBeTruthy();
  });
});
