import { render } from '@testing-library/react';

import App17component85 from './app17component85';

describe('App17component85', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component85 />);
    expect(baseElement).toBeTruthy();
  });
});
