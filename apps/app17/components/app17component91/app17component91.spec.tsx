import { render } from '@testing-library/react';

import App17component91 from './app17component91';

describe('App17component91', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component91 />);
    expect(baseElement).toBeTruthy();
  });
});
