import { render } from '@testing-library/react';

import App17component97 from './app17component97';

describe('App17component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component97 />);
    expect(baseElement).toBeTruthy();
  });
});
