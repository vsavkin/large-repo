import { render } from '@testing-library/react';

import App17component58 from './app17component58';

describe('App17component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component58 />);
    expect(baseElement).toBeTruthy();
  });
});
