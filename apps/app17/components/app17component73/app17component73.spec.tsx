import { render } from '@testing-library/react';

import App17component73 from './app17component73';

describe('App17component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component73 />);
    expect(baseElement).toBeTruthy();
  });
});
