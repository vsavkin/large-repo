import { render } from '@testing-library/react';

import App17component52 from './app17component52';

describe('App17component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component52 />);
    expect(baseElement).toBeTruthy();
  });
});
