import { render } from '@testing-library/react';

import App17component77 from './app17component77';

describe('App17component77', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component77 />);
    expect(baseElement).toBeTruthy();
  });
});
