import { render } from '@testing-library/react';

import App17component46 from './app17component46';

describe('App17component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component46 />);
    expect(baseElement).toBeTruthy();
  });
});
