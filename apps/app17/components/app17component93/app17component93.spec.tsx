import { render } from '@testing-library/react';

import App17component93 from './app17component93';

describe('App17component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component93 />);
    expect(baseElement).toBeTruthy();
  });
});
