import { render } from '@testing-library/react';

import App17component74 from './app17component74';

describe('App17component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component74 />);
    expect(baseElement).toBeTruthy();
  });
});
