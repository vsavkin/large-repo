import { render } from '@testing-library/react';

import App17component94 from './app17component94';

describe('App17component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component94 />);
    expect(baseElement).toBeTruthy();
  });
});
