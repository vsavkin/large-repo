import { render } from '@testing-library/react';

import App17component54 from './app17component54';

describe('App17component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component54 />);
    expect(baseElement).toBeTruthy();
  });
});
