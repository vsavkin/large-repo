import { render } from '@testing-library/react';

import App17component40 from './app17component40';

describe('App17component40', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component40 />);
    expect(baseElement).toBeTruthy();
  });
});
