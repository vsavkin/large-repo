import { render } from '@testing-library/react';

import App17component38 from './app17component38';

describe('App17component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component38 />);
    expect(baseElement).toBeTruthy();
  });
});
