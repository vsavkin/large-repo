import { render } from '@testing-library/react';

import App17component71 from './app17component71';

describe('App17component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component71 />);
    expect(baseElement).toBeTruthy();
  });
});
