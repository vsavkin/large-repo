import { render } from '@testing-library/react';

import App17component69 from './app17component69';

describe('App17component69', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component69 />);
    expect(baseElement).toBeTruthy();
  });
});
