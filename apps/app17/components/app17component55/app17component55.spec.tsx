import { render } from '@testing-library/react';

import App17component55 from './app17component55';

describe('App17component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component55 />);
    expect(baseElement).toBeTruthy();
  });
});
