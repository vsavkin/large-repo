import { render } from '@testing-library/react';

import App17component48 from './app17component48';

describe('App17component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component48 />);
    expect(baseElement).toBeTruthy();
  });
});
