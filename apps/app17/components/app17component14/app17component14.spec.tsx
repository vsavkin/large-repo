import { render } from '@testing-library/react';

import App17component14 from './app17component14';

describe('App17component14', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component14 />);
    expect(baseElement).toBeTruthy();
  });
});
