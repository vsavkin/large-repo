import { render } from '@testing-library/react';

import App17component79 from './app17component79';

describe('App17component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component79 />);
    expect(baseElement).toBeTruthy();
  });
});
