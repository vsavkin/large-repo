import { render } from '@testing-library/react';

import App17component37 from './app17component37';

describe('App17component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component37 />);
    expect(baseElement).toBeTruthy();
  });
});
