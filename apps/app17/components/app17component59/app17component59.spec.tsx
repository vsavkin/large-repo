import { render } from '@testing-library/react';

import App17component59 from './app17component59';

describe('App17component59', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component59 />);
    expect(baseElement).toBeTruthy();
  });
});
