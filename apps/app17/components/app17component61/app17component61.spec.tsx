import { render } from '@testing-library/react';

import App17component61 from './app17component61';

describe('App17component61', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component61 />);
    expect(baseElement).toBeTruthy();
  });
});
