import { render } from '@testing-library/react';

import App17component78 from './app17component78';

describe('App17component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component78 />);
    expect(baseElement).toBeTruthy();
  });
});
