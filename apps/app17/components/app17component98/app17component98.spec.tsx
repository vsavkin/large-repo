import { render } from '@testing-library/react';

import App17component98 from './app17component98';

describe('App17component98', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component98 />);
    expect(baseElement).toBeTruthy();
  });
});
