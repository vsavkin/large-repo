import { render } from '@testing-library/react';

import App17component44 from './app17component44';

describe('App17component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component44 />);
    expect(baseElement).toBeTruthy();
  });
});
