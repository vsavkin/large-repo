import { render } from '@testing-library/react';

import App17component86 from './app17component86';

describe('App17component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component86 />);
    expect(baseElement).toBeTruthy();
  });
});
