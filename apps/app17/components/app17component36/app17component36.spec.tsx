import { render } from '@testing-library/react';

import App17component36 from './app17component36';

describe('App17component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component36 />);
    expect(baseElement).toBeTruthy();
  });
});
