import { render } from '@testing-library/react';

import App17component62 from './app17component62';

describe('App17component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component62 />);
    expect(baseElement).toBeTruthy();
  });
});
