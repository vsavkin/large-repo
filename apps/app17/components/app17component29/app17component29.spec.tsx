import { render } from '@testing-library/react';

import App17component29 from './app17component29';

describe('App17component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component29 />);
    expect(baseElement).toBeTruthy();
  });
});
