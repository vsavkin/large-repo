import { render } from '@testing-library/react';

import App17component28 from './app17component28';

describe('App17component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component28 />);
    expect(baseElement).toBeTruthy();
  });
});
