import { render } from '@testing-library/react';

import App17component27 from './app17component27';

describe('App17component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component27 />);
    expect(baseElement).toBeTruthy();
  });
});
