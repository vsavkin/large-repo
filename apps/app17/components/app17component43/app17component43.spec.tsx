import { render } from '@testing-library/react';

import App17component43 from './app17component43';

describe('App17component43', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component43 />);
    expect(baseElement).toBeTruthy();
  });
});
