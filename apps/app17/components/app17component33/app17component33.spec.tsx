import { render } from '@testing-library/react';

import App17component33 from './app17component33';

describe('App17component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component33 />);
    expect(baseElement).toBeTruthy();
  });
});
