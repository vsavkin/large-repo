import { render } from '@testing-library/react';

import App17component65 from './app17component65';

describe('App17component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component65 />);
    expect(baseElement).toBeTruthy();
  });
});
