import { render } from '@testing-library/react';

import App17component96 from './app17component96';

describe('App17component96', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component96 />);
    expect(baseElement).toBeTruthy();
  });
});
