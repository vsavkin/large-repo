import { render } from '@testing-library/react';

import App17component49 from './app17component49';

describe('App17component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component49 />);
    expect(baseElement).toBeTruthy();
  });
});
