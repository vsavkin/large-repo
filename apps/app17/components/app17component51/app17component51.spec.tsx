import { render } from '@testing-library/react';

import App17component51 from './app17component51';

describe('App17component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component51 />);
    expect(baseElement).toBeTruthy();
  });
});
