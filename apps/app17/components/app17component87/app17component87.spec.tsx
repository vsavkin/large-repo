import { render } from '@testing-library/react';

import App17component87 from './app17component87';

describe('App17component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component87 />);
    expect(baseElement).toBeTruthy();
  });
});
