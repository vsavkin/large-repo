import { render } from '@testing-library/react';

import App17component89 from './app17component89';

describe('App17component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component89 />);
    expect(baseElement).toBeTruthy();
  });
});
