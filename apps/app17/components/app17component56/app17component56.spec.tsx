import { render } from '@testing-library/react';

import App17component56 from './app17component56';

describe('App17component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component56 />);
    expect(baseElement).toBeTruthy();
  });
});
