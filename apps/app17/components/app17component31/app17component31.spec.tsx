import { render } from '@testing-library/react';

import App17component31 from './app17component31';

describe('App17component31', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component31 />);
    expect(baseElement).toBeTruthy();
  });
});
