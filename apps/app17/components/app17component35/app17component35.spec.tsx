import { render } from '@testing-library/react';

import App17component35 from './app17component35';

describe('App17component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component35 />);
    expect(baseElement).toBeTruthy();
  });
});
