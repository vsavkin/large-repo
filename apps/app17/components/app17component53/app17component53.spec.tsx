import { render } from '@testing-library/react';

import App17component53 from './app17component53';

describe('App17component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component53 />);
    expect(baseElement).toBeTruthy();
  });
});
