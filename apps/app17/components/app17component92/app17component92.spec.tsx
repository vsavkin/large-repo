import { render } from '@testing-library/react';

import App17component92 from './app17component92';

describe('App17component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component92 />);
    expect(baseElement).toBeTruthy();
  });
});
