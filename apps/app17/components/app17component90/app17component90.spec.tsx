import { render } from '@testing-library/react';

import App17component90 from './app17component90';

describe('App17component90', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component90 />);
    expect(baseElement).toBeTruthy();
  });
});
