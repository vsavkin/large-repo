import { render } from '@testing-library/react';

import App17component0 from './app17component0';

describe('App17component0', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component0 />);
    expect(baseElement).toBeTruthy();
  });
});
