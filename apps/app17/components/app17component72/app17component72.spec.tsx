import { render } from '@testing-library/react';

import App17component72 from './app17component72';

describe('App17component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component72 />);
    expect(baseElement).toBeTruthy();
  });
});
