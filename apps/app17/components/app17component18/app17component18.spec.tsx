import { render } from '@testing-library/react';

import App17component18 from './app17component18';

describe('App17component18', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component18 />);
    expect(baseElement).toBeTruthy();
  });
});
