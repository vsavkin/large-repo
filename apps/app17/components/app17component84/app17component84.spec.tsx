import { render } from '@testing-library/react';

import App17component84 from './app17component84';

describe('App17component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component84 />);
    expect(baseElement).toBeTruthy();
  });
});
