import { render } from '@testing-library/react';

import App17component76 from './app17component76';

describe('App17component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component76 />);
    expect(baseElement).toBeTruthy();
  });
});
