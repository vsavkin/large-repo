import { render } from '@testing-library/react';

import App17component34 from './app17component34';

describe('App17component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component34 />);
    expect(baseElement).toBeTruthy();
  });
});
