import { render } from '@testing-library/react';

import App17component57 from './app17component57';

describe('App17component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component57 />);
    expect(baseElement).toBeTruthy();
  });
});
