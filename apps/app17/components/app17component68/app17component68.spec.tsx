import { render } from '@testing-library/react';

import App17component68 from './app17component68';

describe('App17component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component68 />);
    expect(baseElement).toBeTruthy();
  });
});
