import { render } from '@testing-library/react';

import App17component88 from './app17component88';

describe('App17component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App17component88 />);
    expect(baseElement).toBeTruthy();
  });
});
