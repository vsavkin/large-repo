import { render } from '@testing-library/react';

import App15component97 from './app15component97';

describe('App15component97', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component97 />);
    expect(baseElement).toBeTruthy();
  });
});
