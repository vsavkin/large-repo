import { render } from '@testing-library/react';

import App15component51 from './app15component51';

describe('App15component51', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component51 />);
    expect(baseElement).toBeTruthy();
  });
});
