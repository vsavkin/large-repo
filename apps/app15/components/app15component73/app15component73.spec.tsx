import { render } from '@testing-library/react';

import App15component73 from './app15component73';

describe('App15component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component73 />);
    expect(baseElement).toBeTruthy();
  });
});
