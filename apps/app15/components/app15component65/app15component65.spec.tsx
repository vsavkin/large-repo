import { render } from '@testing-library/react';

import App15component65 from './app15component65';

describe('App15component65', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component65 />);
    expect(baseElement).toBeTruthy();
  });
});
