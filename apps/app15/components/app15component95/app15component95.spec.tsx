import { render } from '@testing-library/react';

import App15component95 from './app15component95';

describe('App15component95', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component95 />);
    expect(baseElement).toBeTruthy();
  });
});
