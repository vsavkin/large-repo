import { render } from '@testing-library/react';

import App15component33 from './app15component33';

describe('App15component33', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component33 />);
    expect(baseElement).toBeTruthy();
  });
});
