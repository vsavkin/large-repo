import { render } from '@testing-library/react';

import App15component94 from './app15component94';

describe('App15component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component94 />);
    expect(baseElement).toBeTruthy();
  });
});
