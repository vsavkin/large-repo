import { render } from '@testing-library/react';

import App15component58 from './app15component58';

describe('App15component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component58 />);
    expect(baseElement).toBeTruthy();
  });
});
