import { render } from '@testing-library/react';

import App15component29 from './app15component29';

describe('App15component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component29 />);
    expect(baseElement).toBeTruthy();
  });
});
