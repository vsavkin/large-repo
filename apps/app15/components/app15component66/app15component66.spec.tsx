import { render } from '@testing-library/react';

import App15component66 from './app15component66';

describe('App15component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component66 />);
    expect(baseElement).toBeTruthy();
  });
});
