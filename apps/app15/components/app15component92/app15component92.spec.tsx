import { render } from '@testing-library/react';

import App15component92 from './app15component92';

describe('App15component92', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component92 />);
    expect(baseElement).toBeTruthy();
  });
});
