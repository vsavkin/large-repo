import { render } from '@testing-library/react';

import App15component76 from './app15component76';

describe('App15component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component76 />);
    expect(baseElement).toBeTruthy();
  });
});
