import { render } from '@testing-library/react';

import App15component53 from './app15component53';

describe('App15component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component53 />);
    expect(baseElement).toBeTruthy();
  });
});
