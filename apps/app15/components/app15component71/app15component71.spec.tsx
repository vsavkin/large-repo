import { render } from '@testing-library/react';

import App15component71 from './app15component71';

describe('App15component71', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component71 />);
    expect(baseElement).toBeTruthy();
  });
});
