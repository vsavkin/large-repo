import { render } from '@testing-library/react';

import App15component62 from './app15component62';

describe('App15component62', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component62 />);
    expect(baseElement).toBeTruthy();
  });
});
