import { render } from '@testing-library/react';

import App15component44 from './app15component44';

describe('App15component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component44 />);
    expect(baseElement).toBeTruthy();
  });
});
