import { render } from '@testing-library/react';

import App15component84 from './app15component84';

describe('App15component84', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component84 />);
    expect(baseElement).toBeTruthy();
  });
});
