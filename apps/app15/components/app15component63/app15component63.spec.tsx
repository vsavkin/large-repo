import { render } from '@testing-library/react';

import App15component63 from './app15component63';

describe('App15component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component63 />);
    expect(baseElement).toBeTruthy();
  });
});
