import { render } from '@testing-library/react';

import App15component57 from './app15component57';

describe('App15component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component57 />);
    expect(baseElement).toBeTruthy();
  });
});
