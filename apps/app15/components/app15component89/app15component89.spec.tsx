import { render } from '@testing-library/react';

import App15component89 from './app15component89';

describe('App15component89', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component89 />);
    expect(baseElement).toBeTruthy();
  });
});
