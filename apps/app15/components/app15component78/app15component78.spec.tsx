import { render } from '@testing-library/react';

import App15component78 from './app15component78';

describe('App15component78', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component78 />);
    expect(baseElement).toBeTruthy();
  });
});
