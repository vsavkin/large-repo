import { render } from '@testing-library/react';

import App15component39 from './app15component39';

describe('App15component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component39 />);
    expect(baseElement).toBeTruthy();
  });
});
