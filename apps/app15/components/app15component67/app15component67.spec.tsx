import { render } from '@testing-library/react';

import App15component67 from './app15component67';

describe('App15component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component67 />);
    expect(baseElement).toBeTruthy();
  });
});
