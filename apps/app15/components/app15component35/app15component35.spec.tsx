import { render } from '@testing-library/react';

import App15component35 from './app15component35';

describe('App15component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component35 />);
    expect(baseElement).toBeTruthy();
  });
});
