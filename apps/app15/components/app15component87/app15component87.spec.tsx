import { render } from '@testing-library/react';

import App15component87 from './app15component87';

describe('App15component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component87 />);
    expect(baseElement).toBeTruthy();
  });
});
