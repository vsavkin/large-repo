import { render } from '@testing-library/react';

import App15component93 from './app15component93';

describe('App15component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component93 />);
    expect(baseElement).toBeTruthy();
  });
});
