import { render } from '@testing-library/react';

import App15component47 from './app15component47';

describe('App15component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component47 />);
    expect(baseElement).toBeTruthy();
  });
});
