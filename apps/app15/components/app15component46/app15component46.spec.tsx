import { render } from '@testing-library/react';

import App15component46 from './app15component46';

describe('App15component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component46 />);
    expect(baseElement).toBeTruthy();
  });
});
