import { render } from '@testing-library/react';

import App15component72 from './app15component72';

describe('App15component72', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component72 />);
    expect(baseElement).toBeTruthy();
  });
});
