import { render } from '@testing-library/react';

import App15component27 from './app15component27';

describe('App15component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component27 />);
    expect(baseElement).toBeTruthy();
  });
});
