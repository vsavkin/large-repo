import { render } from '@testing-library/react';

import App15component28 from './app15component28';

describe('App15component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component28 />);
    expect(baseElement).toBeTruthy();
  });
});
