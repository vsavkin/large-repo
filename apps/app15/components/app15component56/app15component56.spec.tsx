import { render } from '@testing-library/react';

import App15component56 from './app15component56';

describe('App15component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component56 />);
    expect(baseElement).toBeTruthy();
  });
});
