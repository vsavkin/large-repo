import { render } from '@testing-library/react';

import App15component38 from './app15component38';

describe('App15component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component38 />);
    expect(baseElement).toBeTruthy();
  });
});
