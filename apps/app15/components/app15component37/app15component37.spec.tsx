import { render } from '@testing-library/react';

import App15component37 from './app15component37';

describe('App15component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component37 />);
    expect(baseElement).toBeTruthy();
  });
});
