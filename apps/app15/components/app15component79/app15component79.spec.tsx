import { render } from '@testing-library/react';

import App15component79 from './app15component79';

describe('App15component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component79 />);
    expect(baseElement).toBeTruthy();
  });
});
