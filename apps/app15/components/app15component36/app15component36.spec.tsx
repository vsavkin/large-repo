import { render } from '@testing-library/react';

import App15component36 from './app15component36';

describe('App15component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component36 />);
    expect(baseElement).toBeTruthy();
  });
});
