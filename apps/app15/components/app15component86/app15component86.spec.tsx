import { render } from '@testing-library/react';

import App15component86 from './app15component86';

describe('App15component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component86 />);
    expect(baseElement).toBeTruthy();
  });
});
