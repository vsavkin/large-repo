import { render } from '@testing-library/react';

import App15component52 from './app15component52';

describe('App15component52', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App15component52 />);
    expect(baseElement).toBeTruthy();
  });
});
