import { render } from '@testing-library/react';

import App1component66 from './app1component66';

describe('App1component66', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component66 />);
    expect(baseElement).toBeTruthy();
  });
});
