import { render } from '@testing-library/react';

import App1component58 from './app1component58';

describe('App1component58', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component58 />);
    expect(baseElement).toBeTruthy();
  });
});
