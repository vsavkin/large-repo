import { render } from '@testing-library/react';

import App1component39 from './app1component39';

describe('App1component39', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component39 />);
    expect(baseElement).toBeTruthy();
  });
});
