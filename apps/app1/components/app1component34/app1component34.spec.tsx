import { render } from '@testing-library/react';

import App1component34 from './app1component34';

describe('App1component34', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component34 />);
    expect(baseElement).toBeTruthy();
  });
});
