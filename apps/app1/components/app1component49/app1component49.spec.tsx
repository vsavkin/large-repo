import { render } from '@testing-library/react';

import App1component49 from './app1component49';

describe('App1component49', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component49 />);
    expect(baseElement).toBeTruthy();
  });
});
