import { render } from '@testing-library/react';

import App1component47 from './app1component47';

describe('App1component47', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component47 />);
    expect(baseElement).toBeTruthy();
  });
});
