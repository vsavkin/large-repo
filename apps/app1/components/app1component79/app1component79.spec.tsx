import { render } from '@testing-library/react';

import App1component79 from './app1component79';

describe('App1component79', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component79 />);
    expect(baseElement).toBeTruthy();
  });
});
