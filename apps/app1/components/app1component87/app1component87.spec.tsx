import { render } from '@testing-library/react';

import App1component87 from './app1component87';

describe('App1component87', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component87 />);
    expect(baseElement).toBeTruthy();
  });
});
