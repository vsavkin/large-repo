import { render } from '@testing-library/react';

import App1component46 from './app1component46';

describe('App1component46', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component46 />);
    expect(baseElement).toBeTruthy();
  });
});
