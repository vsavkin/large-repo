import { render } from '@testing-library/react';

import App1component57 from './app1component57';

describe('App1component57', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component57 />);
    expect(baseElement).toBeTruthy();
  });
});
