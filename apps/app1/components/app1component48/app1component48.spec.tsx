import { render } from '@testing-library/react';

import App1component48 from './app1component48';

describe('App1component48', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component48 />);
    expect(baseElement).toBeTruthy();
  });
});
