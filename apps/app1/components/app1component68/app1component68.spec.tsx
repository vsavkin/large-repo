import { render } from '@testing-library/react';

import App1component68 from './app1component68';

describe('App1component68', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component68 />);
    expect(baseElement).toBeTruthy();
  });
});
