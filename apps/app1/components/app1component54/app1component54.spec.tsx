import { render } from '@testing-library/react';

import App1component54 from './app1component54';

describe('App1component54', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component54 />);
    expect(baseElement).toBeTruthy();
  });
});
