import { render } from '@testing-library/react';

import App1component44 from './app1component44';

describe('App1component44', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component44 />);
    expect(baseElement).toBeTruthy();
  });
});
