import { render } from '@testing-library/react';

import App1component27 from './app1component27';

describe('App1component27', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component27 />);
    expect(baseElement).toBeTruthy();
  });
});
