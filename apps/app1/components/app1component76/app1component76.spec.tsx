import { render } from '@testing-library/react';

import App1component76 from './app1component76';

describe('App1component76', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component76 />);
    expect(baseElement).toBeTruthy();
  });
});
