import { render } from '@testing-library/react';

import App1component38 from './app1component38';

describe('App1component38', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component38 />);
    expect(baseElement).toBeTruthy();
  });
});
