import { render } from '@testing-library/react';

import App1component63 from './app1component63';

describe('App1component63', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component63 />);
    expect(baseElement).toBeTruthy();
  });
});
