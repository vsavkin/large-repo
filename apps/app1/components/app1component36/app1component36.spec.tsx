import { render } from '@testing-library/react';

import App1component36 from './app1component36';

describe('App1component36', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component36 />);
    expect(baseElement).toBeTruthy();
  });
});
