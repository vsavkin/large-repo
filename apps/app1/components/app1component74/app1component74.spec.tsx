import { render } from '@testing-library/react';

import App1component74 from './app1component74';

describe('App1component74', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component74 />);
    expect(baseElement).toBeTruthy();
  });
});
