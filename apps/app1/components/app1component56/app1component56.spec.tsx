import { render } from '@testing-library/react';

import App1component56 from './app1component56';

describe('App1component56', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component56 />);
    expect(baseElement).toBeTruthy();
  });
});
