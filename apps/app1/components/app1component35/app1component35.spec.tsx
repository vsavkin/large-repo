import { render } from '@testing-library/react';

import App1component35 from './app1component35';

describe('App1component35', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component35 />);
    expect(baseElement).toBeTruthy();
  });
});
