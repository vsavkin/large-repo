import { render } from '@testing-library/react';

import App1component55 from './app1component55';

describe('App1component55', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component55 />);
    expect(baseElement).toBeTruthy();
  });
});
