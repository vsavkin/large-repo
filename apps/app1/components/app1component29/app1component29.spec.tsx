import { render } from '@testing-library/react';

import App1component29 from './app1component29';

describe('App1component29', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component29 />);
    expect(baseElement).toBeTruthy();
  });
});
