import { render } from '@testing-library/react';

import App1component88 from './app1component88';

describe('App1component88', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component88 />);
    expect(baseElement).toBeTruthy();
  });
});
