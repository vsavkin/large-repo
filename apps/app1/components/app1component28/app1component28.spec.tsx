import { render } from '@testing-library/react';

import App1component28 from './app1component28';

describe('App1component28', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component28 />);
    expect(baseElement).toBeTruthy();
  });
});
