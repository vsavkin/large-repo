import { render } from '@testing-library/react';

import App1component37 from './app1component37';

describe('App1component37', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component37 />);
    expect(baseElement).toBeTruthy();
  });
});
