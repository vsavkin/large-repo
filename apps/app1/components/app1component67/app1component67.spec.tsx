import { render } from '@testing-library/react';

import App1component67 from './app1component67';

describe('App1component67', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component67 />);
    expect(baseElement).toBeTruthy();
  });
});
