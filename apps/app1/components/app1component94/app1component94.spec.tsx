import { render } from '@testing-library/react';

import App1component94 from './app1component94';

describe('App1component94', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component94 />);
    expect(baseElement).toBeTruthy();
  });
});
