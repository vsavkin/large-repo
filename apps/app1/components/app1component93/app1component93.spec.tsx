import { render } from '@testing-library/react';

import App1component93 from './app1component93';

describe('App1component93', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component93 />);
    expect(baseElement).toBeTruthy();
  });
});
