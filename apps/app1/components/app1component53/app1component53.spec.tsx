import { render } from '@testing-library/react';

import App1component53 from './app1component53';

describe('App1component53', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component53 />);
    expect(baseElement).toBeTruthy();
  });
});
