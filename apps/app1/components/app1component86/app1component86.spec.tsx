import { render } from '@testing-library/react';

import App1component86 from './app1component86';

describe('App1component86', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component86 />);
    expect(baseElement).toBeTruthy();
  });
});
