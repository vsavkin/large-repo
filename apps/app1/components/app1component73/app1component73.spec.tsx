import { render } from '@testing-library/react';

import App1component73 from './app1component73';

describe('App1component73', () => {
  it('should render successfully', () => {
    const { baseElement } = render(<App1component73 />);
    expect(baseElement).toBeTruthy();
  });
});
